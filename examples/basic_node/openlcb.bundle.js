/* OpenLcbJSLib v0.1.0 — https://github.com/... — generated bundle, do not edit by hand */
var OpenLCB = (() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // src/index.js
  var index_exports = {};
  __export(index_exports, {
    ADDRESS_SPACE_ENCODING: () => ADDRESS_SPACE_ENCODING,
    AliasMappingListener: () => AliasMappingListener,
    AliasMappings: () => AliasMappings,
    BROADCAST_TIME_EVENT_TYPE: () => BROADCAST_TIME_EVENT_TYPE,
    CONFIG_MEM_STREAM_PHASE: () => CONFIG_MEM_STREAM_PHASE,
    CanBufferFifo: () => CanBufferFifo,
    CanLoginMessageHandler: () => CanLoginMessageHandler,
    CanLoginStatemachine: () => CanLoginStatemachine,
    CanMainStatemachine: () => CanMainStatemachine,
    CanRxMessageHandler: () => CanRxMessageHandler,
    CanRxStatemachine: () => CanRxStatemachine,
    CanTxMessageHandler: () => CanTxMessageHandler,
    EVENT_RANGE_COUNT: () => EVENT_RANGE_COUNT,
    EVENT_STATUS: () => EVENT_STATUS,
    GridConnectParser: () => GridConnectParser,
    LocalStorageConfigMemory: () => LocalStorageConfigMemory,
    MessageFifo: () => MessageFifo,
    NodePool: () => NodePool,
    OpenLcbApplication: () => OpenLcbApplication,
    OpenLcbApplicationBroadcastTime: () => OpenLcbApplicationBroadcastTime,
    OpenLcbApplicationTrain: () => OpenLcbApplicationTrain,
    OpenLcbConfig: () => OpenLcbConfig,
    OpenLcbLoginStatemachine: () => OpenLcbLoginStatemachine,
    OpenLcbMainStatemachine: () => OpenLcbMainStatemachine,
    PAYLOAD_TYPE: () => PAYLOAD_TYPE,
    PAYLOAD_TYPE_LEN: () => PAYLOAD_TYPE_LEN,
    ProtocolBroadcastTimeHandler: () => ProtocolBroadcastTimeHandler,
    ProtocolConfigMemOperations: () => ProtocolConfigMemOperations,
    ProtocolConfigMemRead: () => ProtocolConfigMemRead,
    ProtocolConfigMemStream: () => ProtocolConfigMemStream,
    ProtocolConfigMemWrite: () => ProtocolConfigMemWrite,
    ProtocolDatagramHandler: () => ProtocolDatagramHandler,
    ProtocolEventTransport: () => ProtocolEventTransport,
    ProtocolMessageNetwork: () => ProtocolMessageNetwork,
    ProtocolSnip: () => ProtocolSnip,
    ProtocolStreamHandler: () => ProtocolStreamHandler,
    ProtocolTrainHandler: () => ProtocolTrainHandler,
    ProtocolTrainSearchHandler: () => ProtocolTrainSearchHandler,
    REGISTRATION_FULL: () => REGISTRATION_FULL,
    STREAM_STATE: () => STREAM_STATE,
    TRAIN_EMERGENCY_TYPE: () => TRAIN_EMERGENCY_TYPE,
    WS_STATE: () => WS_STATE,
    WebSocketTransport: () => WebSocketTransport,
    broadcastTimeExtractClockId: () => extractClockId,
    broadcastTimeExtractDate: () => extractDate,
    broadcastTimeExtractRate: () => extractRate,
    broadcastTimeExtractTime: () => extractTime,
    broadcastTimeExtractYear: () => extractYear,
    broadcastTimeGetEventType: () => getEventType,
    canUtilities: () => utilities_exports2,
    createAliasMapping: () => createAliasMapping,
    createCanMsg: () => createCanMsg,
    createCommandEventId: () => createCommandEventId,
    createDateEventId: () => createDateEventId,
    createEvent: () => createEvent,
    createEventRange: () => createEventRange,
    createListenerAliasEntry: () => createListenerAliasEntry,
    createMessage: () => createMessage,
    createNode: () => createNode,
    createRateEventId: () => createRateEventId,
    createTimeEventId: () => createTimeEventId,
    createYearEventId: () => createYearEventId,
    defines: () => defines_exports,
    float16: () => float16_exports,
    generateAlias: () => generateAlias,
    generateSeed: () => generateSeed,
    gridconnectFromCanMsg: () => fromCanMsg,
    gridconnectToCanMsg: () => toCanMsg,
    isSearchEvent: () => isSearchEvent,
    trainSearchCreateEventId: () => createEventId,
    trainSearchDigitsToAddress: () => digitsToAddress,
    trainSearchExtractDigits: () => extractDigits,
    trainSearchExtractFlags: () => extractFlags,
    utilities: () => utilities_exports
  });

  // src/openlcb/defines.js
  var defines_exports = {};
  __export(defines_exports, {
    BROADCAST_TIME_DATE_ROLLOVER: () => BROADCAST_TIME_DATE_ROLLOVER,
    BROADCAST_TIME_ID_ALTERNATE_CLOCK_1: () => BROADCAST_TIME_ID_ALTERNATE_CLOCK_1,
    BROADCAST_TIME_ID_ALTERNATE_CLOCK_2: () => BROADCAST_TIME_ID_ALTERNATE_CLOCK_2,
    BROADCAST_TIME_ID_DEFAULT_FAST_CLOCK: () => BROADCAST_TIME_ID_DEFAULT_FAST_CLOCK,
    BROADCAST_TIME_ID_DEFAULT_REALTIME_CLOCK: () => BROADCAST_TIME_ID_DEFAULT_REALTIME_CLOCK,
    BROADCAST_TIME_MASK_CLOCK_ID: () => BROADCAST_TIME_MASK_CLOCK_ID,
    BROADCAST_TIME_MASK_COMMAND_DATA: () => BROADCAST_TIME_MASK_COMMAND_DATA,
    BROADCAST_TIME_QUERY: () => BROADCAST_TIME_QUERY,
    BROADCAST_TIME_REPORT_DATE_BASE: () => BROADCAST_TIME_REPORT_DATE_BASE,
    BROADCAST_TIME_REPORT_RATE_BASE: () => BROADCAST_TIME_REPORT_RATE_BASE,
    BROADCAST_TIME_REPORT_TIME_BASE: () => BROADCAST_TIME_REPORT_TIME_BASE,
    BROADCAST_TIME_REPORT_YEAR_BASE: () => BROADCAST_TIME_REPORT_YEAR_BASE,
    BROADCAST_TIME_SET_COMMAND_OFFSET: () => BROADCAST_TIME_SET_COMMAND_OFFSET,
    BROADCAST_TIME_SET_DATE_BASE: () => BROADCAST_TIME_SET_DATE_BASE,
    BROADCAST_TIME_SET_RATE_BASE: () => BROADCAST_TIME_SET_RATE_BASE,
    BROADCAST_TIME_SET_TIME_BASE: () => BROADCAST_TIME_SET_TIME_BASE,
    BROADCAST_TIME_SET_YEAR_BASE: () => BROADCAST_TIME_SET_YEAR_BASE,
    BROADCAST_TIME_START: () => BROADCAST_TIME_START,
    BROADCAST_TIME_STOP: () => BROADCAST_TIME_STOP,
    CAN_CONTROL_FRAME_AMD: () => CAN_CONTROL_FRAME_AMD,
    CAN_CONTROL_FRAME_AME: () => CAN_CONTROL_FRAME_AME,
    CAN_CONTROL_FRAME_AMR: () => CAN_CONTROL_FRAME_AMR,
    CAN_CONTROL_FRAME_CID1: () => CAN_CONTROL_FRAME_CID1,
    CAN_CONTROL_FRAME_CID2: () => CAN_CONTROL_FRAME_CID2,
    CAN_CONTROL_FRAME_CID3: () => CAN_CONTROL_FRAME_CID3,
    CAN_CONTROL_FRAME_CID4: () => CAN_CONTROL_FRAME_CID4,
    CAN_CONTROL_FRAME_CID5: () => CAN_CONTROL_FRAME_CID5,
    CAN_CONTROL_FRAME_CID6: () => CAN_CONTROL_FRAME_CID6,
    CAN_CONTROL_FRAME_CID7: () => CAN_CONTROL_FRAME_CID7,
    CAN_CONTROL_FRAME_ERROR_INFO_REPORT_0: () => CAN_CONTROL_FRAME_ERROR_INFO_REPORT_0,
    CAN_CONTROL_FRAME_ERROR_INFO_REPORT_1: () => CAN_CONTROL_FRAME_ERROR_INFO_REPORT_1,
    CAN_CONTROL_FRAME_ERROR_INFO_REPORT_2: () => CAN_CONTROL_FRAME_ERROR_INFO_REPORT_2,
    CAN_CONTROL_FRAME_ERROR_INFO_REPORT_3: () => CAN_CONTROL_FRAME_ERROR_INFO_REPORT_3,
    CAN_CONTROL_FRAME_RID: () => CAN_CONTROL_FRAME_RID,
    CAN_FRAME_TYPE_DATAGRAM_FINAL: () => CAN_FRAME_TYPE_DATAGRAM_FINAL,
    CAN_FRAME_TYPE_DATAGRAM_FIRST: () => CAN_FRAME_TYPE_DATAGRAM_FIRST,
    CAN_FRAME_TYPE_DATAGRAM_MIDDLE: () => CAN_FRAME_TYPE_DATAGRAM_MIDDLE,
    CAN_FRAME_TYPE_DATAGRAM_ONLY: () => CAN_FRAME_TYPE_DATAGRAM_ONLY,
    CAN_FRAME_TYPE_RESERVED: () => CAN_FRAME_TYPE_RESERVED,
    CAN_FRAME_TYPE_STREAM: () => CAN_FRAME_TYPE_STREAM,
    CAN_OPENLCB_MSG: () => CAN_OPENLCB_MSG,
    CAN_STATEMACHINE_NODE_ENUMRATOR_KEY: () => CAN_STATEMACHINE_NODE_ENUMRATOR_KEY,
    CONFIG_MEM_ACDI_HARDWARE_VERSION_ADDRESS: () => CONFIG_MEM_ACDI_HARDWARE_VERSION_ADDRESS,
    CONFIG_MEM_ACDI_HARDWARE_VERSION_LEN: () => CONFIG_MEM_ACDI_HARDWARE_VERSION_LEN,
    CONFIG_MEM_ACDI_MANUFACTURER_ADDRESS: () => CONFIG_MEM_ACDI_MANUFACTURER_ADDRESS,
    CONFIG_MEM_ACDI_MANUFACTURER_LEN: () => CONFIG_MEM_ACDI_MANUFACTURER_LEN,
    CONFIG_MEM_ACDI_MANUFACTURER_VERSION_ADDRESS: () => CONFIG_MEM_ACDI_MANUFACTURER_VERSION_ADDRESS,
    CONFIG_MEM_ACDI_MODEL_ADDRESS: () => CONFIG_MEM_ACDI_MODEL_ADDRESS,
    CONFIG_MEM_ACDI_MODEL_LEN: () => CONFIG_MEM_ACDI_MODEL_LEN,
    CONFIG_MEM_ACDI_SOFTWARE_VERSION_ADDRESS: () => CONFIG_MEM_ACDI_SOFTWARE_VERSION_ADDRESS,
    CONFIG_MEM_ACDI_SOFTWARE_VERSION_LEN: () => CONFIG_MEM_ACDI_SOFTWARE_VERSION_LEN,
    CONFIG_MEM_ACDI_USER_DESCRIPTION_ADDRESS: () => CONFIG_MEM_ACDI_USER_DESCRIPTION_ADDRESS,
    CONFIG_MEM_ACDI_USER_DESCRIPTION_LEN: () => CONFIG_MEM_ACDI_USER_DESCRIPTION_LEN,
    CONFIG_MEM_ACDI_USER_NAME_ADDRESS: () => CONFIG_MEM_ACDI_USER_NAME_ADDRESS,
    CONFIG_MEM_ACDI_USER_NAME_LEN: () => CONFIG_MEM_ACDI_USER_NAME_LEN,
    CONFIG_MEM_ACDI_USER_VERSION_ADDRESS: () => CONFIG_MEM_ACDI_USER_VERSION_ADDRESS,
    CONFIG_MEM_ACDI_USER_VERSION_LEN: () => CONFIG_MEM_ACDI_USER_VERSION_LEN,
    CONFIG_MEM_ACDI_VERSION_LEN: () => CONFIG_MEM_ACDI_VERSION_LEN,
    CONFIG_MEM_CONFIGURATION: () => CONFIG_MEM_CONFIGURATION,
    CONFIG_MEM_CONFIG_USER_DESCRIPTION_OFFSET: () => CONFIG_MEM_CONFIG_USER_DESCRIPTION_OFFSET,
    CONFIG_MEM_CONFIG_USER_NAME_OFFSET: () => CONFIG_MEM_CONFIG_USER_NAME_OFFSET,
    CONFIG_MEM_FACTORY_RESET: () => CONFIG_MEM_FACTORY_RESET,
    CONFIG_MEM_FREEZE: () => CONFIG_MEM_FREEZE,
    CONFIG_MEM_GET_ADDRESS_SPACE_INFO_CMD: () => CONFIG_MEM_GET_ADDRESS_SPACE_INFO_CMD,
    CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_NOT_PRESENT: () => CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_NOT_PRESENT,
    CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_PRESENT: () => CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_PRESENT,
    CONFIG_MEM_GET_UNIQUE_ID: () => CONFIG_MEM_GET_UNIQUE_ID,
    CONFIG_MEM_GET_UNIQUE_ID_REPLY: () => CONFIG_MEM_GET_UNIQUE_ID_REPLY,
    CONFIG_MEM_OPTIONS_CMD: () => CONFIG_MEM_OPTIONS_CMD,
    CONFIG_MEM_OPTIONS_REPLY: () => CONFIG_MEM_OPTIONS_REPLY,
    CONFIG_MEM_READ_REPLY_FAIL_SPACE_FD: () => CONFIG_MEM_READ_REPLY_FAIL_SPACE_FD,
    CONFIG_MEM_READ_REPLY_FAIL_SPACE_FE: () => CONFIG_MEM_READ_REPLY_FAIL_SPACE_FE,
    CONFIG_MEM_READ_REPLY_FAIL_SPACE_FF: () => CONFIG_MEM_READ_REPLY_FAIL_SPACE_FF,
    CONFIG_MEM_READ_REPLY_FAIL_SPACE_IN_BYTE_6: () => CONFIG_MEM_READ_REPLY_FAIL_SPACE_IN_BYTE_6,
    CONFIG_MEM_READ_REPLY_OK_SPACE_FD: () => CONFIG_MEM_READ_REPLY_OK_SPACE_FD,
    CONFIG_MEM_READ_REPLY_OK_SPACE_FE: () => CONFIG_MEM_READ_REPLY_OK_SPACE_FE,
    CONFIG_MEM_READ_REPLY_OK_SPACE_FF: () => CONFIG_MEM_READ_REPLY_OK_SPACE_FF,
    CONFIG_MEM_READ_REPLY_OK_SPACE_IN_BYTE_6: () => CONFIG_MEM_READ_REPLY_OK_SPACE_IN_BYTE_6,
    CONFIG_MEM_READ_SPACE_FD: () => CONFIG_MEM_READ_SPACE_FD,
    CONFIG_MEM_READ_SPACE_FE: () => CONFIG_MEM_READ_SPACE_FE,
    CONFIG_MEM_READ_SPACE_FF: () => CONFIG_MEM_READ_SPACE_FF,
    CONFIG_MEM_READ_SPACE_IN_BYTE_6: () => CONFIG_MEM_READ_SPACE_IN_BYTE_6,
    CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FD: () => CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FD,
    CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FE: () => CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FE,
    CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FF: () => CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FF,
    CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_IN_BYTE_6: () => CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_IN_BYTE_6,
    CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FD: () => CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FD,
    CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FE: () => CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FE,
    CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FF: () => CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FF,
    CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_IN_BYTE_6: () => CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_IN_BYTE_6,
    CONFIG_MEM_READ_STREAM_SPACE_FD: () => CONFIG_MEM_READ_STREAM_SPACE_FD,
    CONFIG_MEM_READ_STREAM_SPACE_FE: () => CONFIG_MEM_READ_STREAM_SPACE_FE,
    CONFIG_MEM_READ_STREAM_SPACE_FF: () => CONFIG_MEM_READ_STREAM_SPACE_FF,
    CONFIG_MEM_READ_STREAM_SPACE_IN_BYTE_6: () => CONFIG_MEM_READ_STREAM_SPACE_IN_BYTE_6,
    CONFIG_MEM_REPLY_FAIL_OFFSET: () => CONFIG_MEM_REPLY_FAIL_OFFSET,
    CONFIG_MEM_REPLY_OK_OFFSET: () => CONFIG_MEM_REPLY_OK_OFFSET,
    CONFIG_MEM_RESERVE_LOCK: () => CONFIG_MEM_RESERVE_LOCK,
    CONFIG_MEM_RESERVE_LOCK_REPLY: () => CONFIG_MEM_RESERVE_LOCK_REPLY,
    CONFIG_MEM_RESET_REBOOT: () => CONFIG_MEM_RESET_REBOOT,
    CONFIG_MEM_SPACE_ACDI_MANUFACTURER_ACCESS: () => CONFIG_MEM_SPACE_ACDI_MANUFACTURER_ACCESS,
    CONFIG_MEM_SPACE_ACDI_USER_ACCESS: () => CONFIG_MEM_SPACE_ACDI_USER_ACCESS,
    CONFIG_MEM_SPACE_ALL: () => CONFIG_MEM_SPACE_ALL,
    CONFIG_MEM_SPACE_CONFIGURATION_DEFINITION_INFO: () => CONFIG_MEM_SPACE_CONFIGURATION_DEFINITION_INFO,
    CONFIG_MEM_SPACE_CONFIGURATION_MEMORY: () => CONFIG_MEM_SPACE_CONFIGURATION_MEMORY,
    CONFIG_MEM_SPACE_FIRMWARE: () => CONFIG_MEM_SPACE_FIRMWARE,
    CONFIG_MEM_SPACE_TRAIN_FUNCTION_CONFIGURATION_MEMORY: () => CONFIG_MEM_SPACE_TRAIN_FUNCTION_CONFIGURATION_MEMORY,
    CONFIG_MEM_SPACE_TRAIN_FUNCTION_DEFINITION_INFO: () => CONFIG_MEM_SPACE_TRAIN_FUNCTION_DEFINITION_INFO,
    CONFIG_MEM_UNFREEZE: () => CONFIG_MEM_UNFREEZE,
    CONFIG_MEM_UPDATE_COMPLETE: () => CONFIG_MEM_UPDATE_COMPLETE,
    CONFIG_MEM_USER_DESCRIPTION_ADDRESS: () => CONFIG_MEM_USER_DESCRIPTION_ADDRESS,
    CONFIG_MEM_USER_MODEL_ADDRESS: () => CONFIG_MEM_USER_MODEL_ADDRESS,
    CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FD: () => CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FD,
    CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FE: () => CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FE,
    CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FF: () => CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FF,
    CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_IN_BYTE_6: () => CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_IN_BYTE_6,
    CONFIG_MEM_WRITE_REPLY_OK_SPACE_FD: () => CONFIG_MEM_WRITE_REPLY_OK_SPACE_FD,
    CONFIG_MEM_WRITE_REPLY_OK_SPACE_FE: () => CONFIG_MEM_WRITE_REPLY_OK_SPACE_FE,
    CONFIG_MEM_WRITE_REPLY_OK_SPACE_FF: () => CONFIG_MEM_WRITE_REPLY_OK_SPACE_FF,
    CONFIG_MEM_WRITE_REPLY_OK_SPACE_IN_BYTE_6: () => CONFIG_MEM_WRITE_REPLY_OK_SPACE_IN_BYTE_6,
    CONFIG_MEM_WRITE_SPACE_FD: () => CONFIG_MEM_WRITE_SPACE_FD,
    CONFIG_MEM_WRITE_SPACE_FE: () => CONFIG_MEM_WRITE_SPACE_FE,
    CONFIG_MEM_WRITE_SPACE_FF: () => CONFIG_MEM_WRITE_SPACE_FF,
    CONFIG_MEM_WRITE_SPACE_IN_BYTE_6: () => CONFIG_MEM_WRITE_SPACE_IN_BYTE_6,
    CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FD: () => CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FD,
    CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FE: () => CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FE,
    CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FF: () => CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FF,
    CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_IN_BYTE_6: () => CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_IN_BYTE_6,
    CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FD: () => CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FD,
    CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FE: () => CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FE,
    CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FF: () => CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FF,
    CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_IN_BYTE_6: () => CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_IN_BYTE_6,
    CONFIG_MEM_WRITE_STREAM_SPACE_FD: () => CONFIG_MEM_WRITE_STREAM_SPACE_FD,
    CONFIG_MEM_WRITE_STREAM_SPACE_FE: () => CONFIG_MEM_WRITE_STREAM_SPACE_FE,
    CONFIG_MEM_WRITE_STREAM_SPACE_FF: () => CONFIG_MEM_WRITE_STREAM_SPACE_FF,
    CONFIG_MEM_WRITE_STREAM_SPACE_IN_BYTE_6: () => CONFIG_MEM_WRITE_STREAM_SPACE_IN_BYTE_6,
    CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FD: () => CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FD,
    CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FE: () => CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FE,
    CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FF: () => CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FF,
    CONFIG_MEM_WRITE_UNDER_MASK_SPACE_IN_BYTE_6: () => CONFIG_MEM_WRITE_UNDER_MASK_SPACE_IN_BYTE_6,
    CONFIG_OPTIONS_COMMANDS_ACDI_MANUFACTURER_READ: () => CONFIG_OPTIONS_COMMANDS_ACDI_MANUFACTURER_READ,
    CONFIG_OPTIONS_COMMANDS_ACDI_USER_READ: () => CONFIG_OPTIONS_COMMANDS_ACDI_USER_READ,
    CONFIG_OPTIONS_COMMANDS_ACDI_USER_WRITE: () => CONFIG_OPTIONS_COMMANDS_ACDI_USER_WRITE,
    CONFIG_OPTIONS_COMMANDS_UNALIGNED_READS: () => CONFIG_OPTIONS_COMMANDS_UNALIGNED_READS,
    CONFIG_OPTIONS_COMMANDS_UNALIGNED_WRITES: () => CONFIG_OPTIONS_COMMANDS_UNALIGNED_WRITES,
    CONFIG_OPTIONS_COMMANDS_WRITE_UNDER_MASK: () => CONFIG_OPTIONS_COMMANDS_WRITE_UNDER_MASK,
    CONFIG_OPTIONS_SPACE_INFO_FLAG_READ_ONLY: () => CONFIG_OPTIONS_SPACE_INFO_FLAG_READ_ONLY,
    CONFIG_OPTIONS_SPACE_INFO_FLAG_USE_LOW_ADDRESS: () => CONFIG_OPTIONS_SPACE_INFO_FLAG_USE_LOW_ADDRESS,
    CONFIG_OPTIONS_WRITE_LENGTH_RESERVED: () => CONFIG_OPTIONS_WRITE_LENGTH_RESERVED,
    CONFIG_OPTIONS_WRITE_LENGTH_STREAM_READ_WRITE: () => CONFIG_OPTIONS_WRITE_LENGTH_STREAM_READ_WRITE,
    DATAGRAM_OK_REPLY_PENDING: () => DATAGRAM_OK_REPLY_PENDING,
    DATAGRAM_TIMEOUT_ENUM_KEY: () => DATAGRAM_TIMEOUT_ENUM_KEY,
    ERROR_CODE_PERMANENT_COUNT_OUT_OF_RANGE: () => ERROR_CODE_PERMANENT_COUNT_OUT_OF_RANGE,
    ERROR_PERMANENT: () => ERROR_PERMANENT,
    ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN: () => ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN,
    ERROR_PERMANENT_CONFIG_MEM_ADDRESS_WRITE_TO_READ_ONLY: () => ERROR_PERMANENT_CONFIG_MEM_ADDRESS_WRITE_TO_READ_ONLY,
    ERROR_PERMANENT_CONFIG_MEM_OUT_OF_BOUNDS_INVALID_ADDRESS: () => ERROR_PERMANENT_CONFIG_MEM_OUT_OF_BOUNDS_INVALID_ADDRESS,
    ERROR_PERMANENT_INVALID_ARGUMENTS: () => ERROR_PERMANENT_INVALID_ARGUMENTS,
    ERROR_PERMANENT_NOT_IMPLEMENTED: () => ERROR_PERMANENT_NOT_IMPLEMENTED,
    ERROR_PERMANENT_NOT_IMPLEMENTED_COMMAND_UNKNOWN: () => ERROR_PERMANENT_NOT_IMPLEMENTED_COMMAND_UNKNOWN,
    ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN: () => ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN,
    ERROR_PERMANENT_NOT_IMPLEMENTED_UNKNOWN_MTI_OR_TRANPORT_PROTOCOL: () => ERROR_PERMANENT_NOT_IMPLEMENTED_UNKNOWN_MTI_OR_TRANPORT_PROTOCOL,
    ERROR_PERMANENT_SOURCE_NOT_PERMITTED: () => ERROR_PERMANENT_SOURCE_NOT_PERMITTED,
    ERROR_PERMANENT_STREAMS_NOT_SUPPORTED: () => ERROR_PERMANENT_STREAMS_NOT_SUPPORTED,
    ERROR_TEMPORARY: () => ERROR_TEMPORARY,
    ERROR_TEMPORARY_BUFFER_UNAVAILABLE: () => ERROR_TEMPORARY_BUFFER_UNAVAILABLE,
    ERROR_TEMPORARY_NOT_EXPECTED_OUT_OF_ORDER: () => ERROR_TEMPORARY_NOT_EXPECTED_OUT_OF_ORDER,
    ERROR_TEMPORARY_OUT_OF_ORDER_MIDDLE_END_WITH_NO_START: () => ERROR_TEMPORARY_OUT_OF_ORDER_MIDDLE_END_WITH_NO_START,
    ERROR_TEMPORARY_OUT_OF_ORDER_START_BEFORE_LAST_END: () => ERROR_TEMPORARY_OUT_OF_ORDER_START_BEFORE_LAST_END,
    ERROR_TEMPORARY_TIME_OUT: () => ERROR_TEMPORARY_TIME_OUT,
    ERROR_TEMPORARY_TRANSFER_ERROR: () => ERROR_TEMPORARY_TRANSFER_ERROR,
    EVENT_ID_CBUS_OFF_SPACE: () => EVENT_ID_CBUS_OFF_SPACE,
    EVENT_ID_CBUS_ON_SPACE: () => EVENT_ID_CBUS_ON_SPACE,
    EVENT_ID_CLEAR_EMERGENCY_OFF: () => EVENT_ID_CLEAR_EMERGENCY_OFF,
    EVENT_ID_CLEAR_EMERGENCY_STOP: () => EVENT_ID_CLEAR_EMERGENCY_STOP,
    EVENT_ID_DCC_ACCESSORY_ACTIVATE: () => EVENT_ID_DCC_ACCESSORY_ACTIVATE,
    EVENT_ID_DCC_ACCESSORY_DEACTIVATE: () => EVENT_ID_DCC_ACCESSORY_DEACTIVATE,
    EVENT_ID_DCC_EXTENDED_ACCESSORY_CMD_SPACE: () => EVENT_ID_DCC_EXTENDED_ACCESSORY_CMD_SPACE,
    EVENT_ID_DCC_SENSOR_FEEDBACK_HIGH: () => EVENT_ID_DCC_SENSOR_FEEDBACK_HIGH,
    EVENT_ID_DCC_SENSOR_FEEDBACK_LO: () => EVENT_ID_DCC_SENSOR_FEEDBACK_LO,
    EVENT_ID_DCC_TURNOUT_FEEDBACK_HIGH: () => EVENT_ID_DCC_TURNOUT_FEEDBACK_HIGH,
    EVENT_ID_DCC_TURNOUT_FEEDBACK_LOW: () => EVENT_ID_DCC_TURNOUT_FEEDBACK_LOW,
    EVENT_ID_DUPLICATE_NODE_DETECTED: () => EVENT_ID_DUPLICATE_NODE_DETECTED,
    EVENT_ID_EMERGENCY_OFF: () => EVENT_ID_EMERGENCY_OFF,
    EVENT_ID_EMERGENCY_STOP: () => EVENT_ID_EMERGENCY_STOP,
    EVENT_ID_FIRMWARE_CORRUPTED: () => EVENT_ID_FIRMWARE_CORRUPTED,
    EVENT_ID_FIRMWARE_UPGRADE_BY_HARDWARE_SWITCH: () => EVENT_ID_FIRMWARE_UPGRADE_BY_HARDWARE_SWITCH,
    EVENT_ID_IDENT_BUTTON_COMBO_PRESSED: () => EVENT_ID_IDENT_BUTTON_COMBO_PRESSED,
    EVENT_ID_LINK_ERROR_CODE_1: () => EVENT_ID_LINK_ERROR_CODE_1,
    EVENT_ID_LINK_ERROR_CODE_2: () => EVENT_ID_LINK_ERROR_CODE_2,
    EVENT_ID_LINK_ERROR_CODE_3: () => EVENT_ID_LINK_ERROR_CODE_3,
    EVENT_ID_LINK_ERROR_CODE_4: () => EVENT_ID_LINK_ERROR_CODE_4,
    EVENT_ID_NODE_RECORDED_NEW_LOG: () => EVENT_ID_NODE_RECORDED_NEW_LOG,
    EVENT_ID_POWER_SUPPLY_BROWN_OUT_NODE: () => EVENT_ID_POWER_SUPPLY_BROWN_OUT_NODE,
    EVENT_ID_POWER_SUPPLY_BROWN_OUT_STANDARD: () => EVENT_ID_POWER_SUPPLY_BROWN_OUT_STANDARD,
    EVENT_ID_TRAIN: () => EVENT_ID_TRAIN,
    EVENT_ID_TRAIN_PROXY: () => EVENT_ID_TRAIN_PROXY,
    EVENT_TRAIN_SEARCH_SPACE: () => EVENT_TRAIN_SEARCH_SPACE,
    LEN_CONFIG_MEM_ADDRESS_SPACE_DESCRIPTION: () => LEN_CONFIG_MEM_ADDRESS_SPACE_DESCRIPTION,
    LEN_CONFIG_MEM_OPTIONS_DESCRIPTION: () => LEN_CONFIG_MEM_OPTIONS_DESCRIPTION,
    LEN_DATAGRAM_MAX_PAYLOAD: () => LEN_DATAGRAM_MAX_PAYLOAD,
    LEN_EVENT_ID: () => LEN_EVENT_ID,
    LEN_EVENT_PAYLOAD: () => LEN_EVENT_PAYLOAD,
    LEN_MESSAGE_BYTES_BASIC: () => LEN_MESSAGE_BYTES_BASIC,
    LEN_MESSAGE_BYTES_DATAGRAM: () => LEN_MESSAGE_BYTES_DATAGRAM,
    LEN_MESSAGE_BYTES_SNIP: () => LEN_MESSAGE_BYTES_SNIP,
    LEN_MESSAGE_BYTES_STREAM: () => LEN_MESSAGE_BYTES_STREAM,
    LEN_MESSAGE_BYTES_WORKER: () => LEN_MESSAGE_BYTES_WORKER,
    LEN_SNIP_HARDWARE_VERSION_BUFFER: () => LEN_SNIP_HARDWARE_VERSION_BUFFER,
    LEN_SNIP_MODEL_BUFFER: () => LEN_SNIP_MODEL_BUFFER,
    LEN_SNIP_NAME_BUFFER: () => LEN_SNIP_NAME_BUFFER,
    LEN_SNIP_SOFTWARE_VERSION_BUFFER: () => LEN_SNIP_SOFTWARE_VERSION_BUFFER,
    LEN_SNIP_STRUCTURE: () => LEN_SNIP_STRUCTURE,
    LEN_SNIP_USER_DATA: () => LEN_SNIP_USER_DATA,
    LEN_SNIP_USER_DESCRIPTION_BUFFER: () => LEN_SNIP_USER_DESCRIPTION_BUFFER,
    LEN_SNIP_USER_NAME_BUFFER: () => LEN_SNIP_USER_NAME_BUFFER,
    LEN_SNIP_USER_VERSION: () => LEN_SNIP_USER_VERSION,
    LEN_SNIP_VERSION: () => LEN_SNIP_VERSION,
    MASK_CAN_DEST_ADDRESS_PRESENT: () => MASK_CAN_DEST_ADDRESS_PRESENT,
    MASK_CAN_EVENT_PRESENT: () => MASK_CAN_EVENT_PRESENT,
    MASK_CAN_FRAME_SEQUENCE_NUMBER: () => MASK_CAN_FRAME_SEQUENCE_NUMBER,
    MASK_CAN_FRAME_TYPE: () => MASK_CAN_FRAME_TYPE,
    MASK_CAN_PRIORITY: () => MASK_CAN_PRIORITY,
    MASK_CAN_PRIORITY_MODIFIER: () => MASK_CAN_PRIORITY_MODIFIER,
    MASK_CAN_SIMPLE_PROTOCOL: () => MASK_CAN_SIMPLE_PROTOCOL,
    MASK_CAN_SOURCE_ALIAS: () => MASK_CAN_SOURCE_ALIAS,
    MASK_CAN_STREAM_OR_DATAGRAM: () => MASK_CAN_STREAM_OR_DATAGRAM,
    MASK_CAN_VARIABLE_FIELD: () => MASK_CAN_VARIABLE_FIELD,
    MASK_DEST_ADDRESS_PRESENT: () => MASK_DEST_ADDRESS_PRESENT,
    MASK_EVENT_PRESENT: () => MASK_EVENT_PRESENT,
    MASK_MULTIFRAME_BITS: () => MASK_MULTIFRAME_BITS,
    MASK_PRIORITY: () => MASK_PRIORITY,
    MASK_PRIORITY_MODIFIER: () => MASK_PRIORITY_MODIFIER,
    MASK_SIMPLE_PROTOCOL: () => MASK_SIMPLE_PROTOCOL,
    MASK_STREAM_OR_DATAGRAM: () => MASK_STREAM_OR_DATAGRAM,
    MAX_INTERNAL_ENUM_KEYS_VALUES: () => MAX_INTERNAL_ENUM_KEYS_VALUES,
    MAX_NODE_ENUM_KEY_VALUES: () => MAX_NODE_ENUM_KEY_VALUES,
    MAX_USER_ENUM_KEYS_VALUES: () => MAX_USER_ENUM_KEYS_VALUES,
    MTI_CONSUMER_IDENTIFIED_CLEAR: () => MTI_CONSUMER_IDENTIFIED_CLEAR,
    MTI_CONSUMER_IDENTIFIED_RESERVED: () => MTI_CONSUMER_IDENTIFIED_RESERVED,
    MTI_CONSUMER_IDENTIFIED_SET: () => MTI_CONSUMER_IDENTIFIED_SET,
    MTI_CONSUMER_IDENTIFIED_UNKNOWN: () => MTI_CONSUMER_IDENTIFIED_UNKNOWN,
    MTI_CONSUMER_IDENTIFY: () => MTI_CONSUMER_IDENTIFY,
    MTI_CONSUMER_RANGE_IDENTIFIED: () => MTI_CONSUMER_RANGE_IDENTIFIED,
    MTI_DATAGRAM: () => MTI_DATAGRAM,
    MTI_DATAGRAM_OK_REPLY: () => MTI_DATAGRAM_OK_REPLY,
    MTI_DATAGRAM_REJECTED_REPLY: () => MTI_DATAGRAM_REJECTED_REPLY,
    MTI_EVENTS_IDENTIFY: () => MTI_EVENTS_IDENTIFY,
    MTI_EVENTS_IDENTIFY_DEST: () => MTI_EVENTS_IDENTIFY_DEST,
    MTI_EVENT_LEARN: () => MTI_EVENT_LEARN,
    MTI_FRAME_TYPE_CAN_STREAM_SEND: () => MTI_FRAME_TYPE_CAN_STREAM_SEND,
    MTI_INITIALIZATION_COMPLETE: () => MTI_INITIALIZATION_COMPLETE,
    MTI_INITIALIZATION_COMPLETE_SIMPLE: () => MTI_INITIALIZATION_COMPLETE_SIMPLE,
    MTI_OPTIONAL_INTERACTION_REJECTED: () => MTI_OPTIONAL_INTERACTION_REJECTED,
    MTI_PC_EVENT_REPORT: () => MTI_PC_EVENT_REPORT,
    MTI_PC_EVENT_REPORT_WITH_PAYLOAD: () => MTI_PC_EVENT_REPORT_WITH_PAYLOAD,
    MTI_PRODUCER_IDENTIFIED_CLEAR: () => MTI_PRODUCER_IDENTIFIED_CLEAR,
    MTI_PRODUCER_IDENTIFIED_RESERVED: () => MTI_PRODUCER_IDENTIFIED_RESERVED,
    MTI_PRODUCER_IDENTIFIED_SET: () => MTI_PRODUCER_IDENTIFIED_SET,
    MTI_PRODUCER_IDENTIFIED_UNKNOWN: () => MTI_PRODUCER_IDENTIFIED_UNKNOWN,
    MTI_PRODUCER_IDENTIFY: () => MTI_PRODUCER_IDENTIFY,
    MTI_PRODUCER_RANGE_IDENTIFIED: () => MTI_PRODUCER_RANGE_IDENTIFIED,
    MTI_PROTOCOL_SUPPORT_INQUIRY: () => MTI_PROTOCOL_SUPPORT_INQUIRY,
    MTI_PROTOCOL_SUPPORT_REPLY: () => MTI_PROTOCOL_SUPPORT_REPLY,
    MTI_SIMPLE_NODE_INFO_REPLY: () => MTI_SIMPLE_NODE_INFO_REPLY,
    MTI_SIMPLE_NODE_INFO_REQUEST: () => MTI_SIMPLE_NODE_INFO_REQUEST,
    MTI_SIMPLE_TRAIN_INFO_REPLY: () => MTI_SIMPLE_TRAIN_INFO_REPLY,
    MTI_SIMPLE_TRAIN_INFO_REQUEST: () => MTI_SIMPLE_TRAIN_INFO_REQUEST,
    MTI_STREAM_COMPLETE: () => MTI_STREAM_COMPLETE,
    MTI_STREAM_INIT_REPLY: () => MTI_STREAM_INIT_REPLY,
    MTI_STREAM_INIT_REQUEST: () => MTI_STREAM_INIT_REQUEST,
    MTI_STREAM_PROCEED: () => MTI_STREAM_PROCEED,
    MTI_STREAM_SEND: () => MTI_STREAM_SEND,
    MTI_TERMINATE_DUE_TO_ERROR: () => MTI_TERMINATE_DUE_TO_ERROR,
    MTI_TRAIN_PROTOCOL: () => MTI_TRAIN_PROTOCOL,
    MTI_TRAIN_REPLY: () => MTI_TRAIN_REPLY,
    MTI_VERIFIED_NODE_ID: () => MTI_VERIFIED_NODE_ID,
    MTI_VERIFIED_NODE_ID_SIMPLE: () => MTI_VERIFIED_NODE_ID_SIMPLE,
    MTI_VERIFY_NODE_ID_ADDRESSED: () => MTI_VERIFY_NODE_ID_ADDRESSED,
    MTI_VERIFY_NODE_ID_GLOBAL: () => MTI_VERIFY_NODE_ID_GLOBAL,
    MULTIFRAME_FINAL: () => MULTIFRAME_FINAL,
    MULTIFRAME_FIRST: () => MULTIFRAME_FIRST,
    MULTIFRAME_MIDDLE: () => MULTIFRAME_MIDDLE,
    MULTIFRAME_ONLY: () => MULTIFRAME_ONLY,
    NULL_EVENT_ID: () => NULL_EVENT_ID,
    NULL_NODE_ID: () => NULL_NODE_ID,
    OPENLCB_JS_LIB_VERSION: () => OPENLCB_JS_LIB_VERSION,
    OPENLCB_JS_LIB_VERSION_MAJOR: () => OPENLCB_JS_LIB_VERSION_MAJOR,
    OPENLCB_JS_LIB_VERSION_MINOR: () => OPENLCB_JS_LIB_VERSION_MINOR,
    OPENLCB_JS_LIB_VERSION_PATCH: () => OPENLCB_JS_LIB_VERSION_PATCH,
    OPENLCB_LOGIN_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX: () => OPENLCB_LOGIN_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX,
    OPENLCB_LOGIN_STATMACHINE_NODE_ENUMERATOR_INDEX: () => OPENLCB_LOGIN_STATMACHINE_NODE_ENUMERATOR_INDEX,
    OPENLCB_MAIN_STATMACHINE_NODE_ENUMERATOR_INDEX: () => OPENLCB_MAIN_STATMACHINE_NODE_ENUMERATOR_INDEX,
    OPENLCB_MESSAGE_STANDARD_FRAME_TYPE: () => OPENLCB_MESSAGE_STANDARD_FRAME_TYPE,
    OPENLCB_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX: () => OPENLCB_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX,
    PSI_ABBREVIATED_DEFAULT_CDI: () => PSI_ABBREVIATED_DEFAULT_CDI,
    PSI_CONFIGURATION_DESCRIPTION_INFO: () => PSI_CONFIGURATION_DESCRIPTION_INFO,
    PSI_DATAGRAM: () => PSI_DATAGRAM,
    PSI_DISPLAY: () => PSI_DISPLAY,
    PSI_EVENT_EXCHANGE: () => PSI_EVENT_EXCHANGE,
    PSI_FIRMWARE_UPGRADE: () => PSI_FIRMWARE_UPGRADE,
    PSI_FIRMWARE_UPGRADE_ACTIVE: () => PSI_FIRMWARE_UPGRADE_ACTIVE,
    PSI_FUNCTION_CONFIGURATION: () => PSI_FUNCTION_CONFIGURATION,
    PSI_FUNCTION_DESCRIPTION: () => PSI_FUNCTION_DESCRIPTION,
    PSI_IDENTIFICATION: () => PSI_IDENTIFICATION,
    PSI_MEMORY_CONFIGURATION: () => PSI_MEMORY_CONFIGURATION,
    PSI_REMOTE_BUTTON: () => PSI_REMOTE_BUTTON,
    PSI_RESERVATION: () => PSI_RESERVATION,
    PSI_RESERVED_0: () => PSI_RESERVED_0,
    PSI_RESERVED_1: () => PSI_RESERVED_1,
    PSI_SIMPLE: () => PSI_SIMPLE,
    PSI_SIMPLE_NODE_INFORMATION: () => PSI_SIMPLE_NODE_INFORMATION,
    PSI_STREAM: () => PSI_STREAM,
    PSI_TEACHING_LEARNING: () => PSI_TEACHING_LEARNING,
    PSI_TRAIN_CONTROL: () => PSI_TRAIN_CONTROL,
    RESERVED_TOP_BIT: () => RESERVED_TOP_BIT,
    RUNSTATE_GENERATE_ALIAS: () => RUNSTATE_GENERATE_ALIAS,
    RUNSTATE_GENERATE_SEED: () => RUNSTATE_GENERATE_SEED,
    RUNSTATE_INIT: () => RUNSTATE_INIT,
    RUNSTATE_LOAD_ALIAS_MAP_DEFINITION: () => RUNSTATE_LOAD_ALIAS_MAP_DEFINITION,
    RUNSTATE_LOAD_CHECK_ID_04: () => RUNSTATE_LOAD_CHECK_ID_04,
    RUNSTATE_LOAD_CHECK_ID_05: () => RUNSTATE_LOAD_CHECK_ID_05,
    RUNSTATE_LOAD_CHECK_ID_06: () => RUNSTATE_LOAD_CHECK_ID_06,
    RUNSTATE_LOAD_CHECK_ID_07: () => RUNSTATE_LOAD_CHECK_ID_07,
    RUNSTATE_LOAD_CONSUMER_EVENTS: () => RUNSTATE_LOAD_CONSUMER_EVENTS,
    RUNSTATE_LOAD_INITIALIZATION_COMPLETE: () => RUNSTATE_LOAD_INITIALIZATION_COMPLETE,
    RUNSTATE_LOAD_PRODUCER_EVENTS: () => RUNSTATE_LOAD_PRODUCER_EVENTS,
    RUNSTATE_LOAD_RESERVE_ID: () => RUNSTATE_LOAD_RESERVE_ID,
    RUNSTATE_LOGIN_COMPLETE: () => RUNSTATE_LOGIN_COMPLETE,
    RUNSTATE_RUN: () => RUNSTATE_RUN,
    RUNSTATE_WAIT_200ms: () => RUNSTATE_WAIT_200ms,
    STREAM_ID_RESERVED: () => STREAM_ID_RESERVED,
    STREAM_REPLY_ACCEPT: () => STREAM_REPLY_ACCEPT,
    S_OK: () => S_OK,
    TRAIN_CONTROLLER_ASSIGN: () => TRAIN_CONTROLLER_ASSIGN,
    TRAIN_CONTROLLER_CHANGED: () => TRAIN_CONTROLLER_CHANGED,
    TRAIN_CONTROLLER_CONFIG: () => TRAIN_CONTROLLER_CONFIG,
    TRAIN_CONTROLLER_QUERY: () => TRAIN_CONTROLLER_QUERY,
    TRAIN_CONTROLLER_RELEASE: () => TRAIN_CONTROLLER_RELEASE,
    TRAIN_EMERGENCY_STOP: () => TRAIN_EMERGENCY_STOP,
    TRAIN_INSTRUCTION_P_BIT: () => TRAIN_INSTRUCTION_P_BIT,
    TRAIN_LISTENER_ATTACH: () => TRAIN_LISTENER_ATTACH,
    TRAIN_LISTENER_CONFIG: () => TRAIN_LISTENER_CONFIG,
    TRAIN_LISTENER_DETACH: () => TRAIN_LISTENER_DETACH,
    TRAIN_LISTENER_FLAG_HIDE: () => TRAIN_LISTENER_FLAG_HIDE,
    TRAIN_LISTENER_FLAG_LINK_F0: () => TRAIN_LISTENER_FLAG_LINK_F0,
    TRAIN_LISTENER_FLAG_LINK_FN: () => TRAIN_LISTENER_FLAG_LINK_FN,
    TRAIN_LISTENER_FLAG_REVERSE: () => TRAIN_LISTENER_FLAG_REVERSE,
    TRAIN_LISTENER_QUERY: () => TRAIN_LISTENER_QUERY,
    TRAIN_MANAGEMENT: () => TRAIN_MANAGEMENT,
    TRAIN_MAX_DCC_SHORT_ADDRESS: () => TRAIN_MAX_DCC_SHORT_ADDRESS,
    TRAIN_MGMT_NOOP: () => TRAIN_MGMT_NOOP,
    TRAIN_MGMT_RELEASE: () => TRAIN_MGMT_RELEASE,
    TRAIN_MGMT_RESERVE: () => TRAIN_MGMT_RESERVE,
    TRAIN_QUERY_FUNCTION: () => TRAIN_QUERY_FUNCTION,
    TRAIN_QUERY_SPEEDS: () => TRAIN_QUERY_SPEEDS,
    TRAIN_SEARCH_FLAG_ADDRESS_ONLY: () => TRAIN_SEARCH_FLAG_ADDRESS_ONLY,
    TRAIN_SEARCH_FLAG_ALLOCATE: () => TRAIN_SEARCH_FLAG_ALLOCATE,
    TRAIN_SEARCH_FLAG_DCC: () => TRAIN_SEARCH_FLAG_DCC,
    TRAIN_SEARCH_FLAG_EXACT: () => TRAIN_SEARCH_FLAG_EXACT,
    TRAIN_SEARCH_FLAG_LONG_ADDR: () => TRAIN_SEARCH_FLAG_LONG_ADDR,
    TRAIN_SEARCH_MASK: () => TRAIN_SEARCH_MASK,
    TRAIN_SEARCH_SPEED_STEP_MASK: () => TRAIN_SEARCH_SPEED_STEP_MASK,
    TRAIN_SET_FUNCTION: () => TRAIN_SET_FUNCTION,
    TRAIN_SET_SPEED_DIRECTION: () => TRAIN_SET_SPEED_DIRECTION,
    USER_ENUM_KEYS_VALUES_1: () => USER_ENUM_KEYS_VALUES_1,
    USER_ENUM_KEYS_VALUES_2: () => USER_ENUM_KEYS_VALUES_2,
    USER_ENUM_KEYS_VALUES_3: () => USER_ENUM_KEYS_VALUES_3,
    USER_ENUM_KEYS_VALUES_4: () => USER_ENUM_KEYS_VALUES_4
  });
  var OPENLCB_JS_LIB_VERSION_MAJOR = 1;
  var OPENLCB_JS_LIB_VERSION_MINOR = 1;
  var OPENLCB_JS_LIB_VERSION_PATCH = 0;
  var OPENLCB_JS_LIB_VERSION = "1.1.0";
  var RUNSTATE_INIT = 0;
  var RUNSTATE_GENERATE_SEED = 1;
  var RUNSTATE_GENERATE_ALIAS = 2;
  var RUNSTATE_LOAD_CHECK_ID_07 = 3;
  var RUNSTATE_LOAD_CHECK_ID_06 = 4;
  var RUNSTATE_LOAD_CHECK_ID_05 = 5;
  var RUNSTATE_LOAD_CHECK_ID_04 = 6;
  var RUNSTATE_WAIT_200ms = 7;
  var RUNSTATE_LOAD_RESERVE_ID = 8;
  var RUNSTATE_LOAD_ALIAS_MAP_DEFINITION = 9;
  var RUNSTATE_LOAD_INITIALIZATION_COMPLETE = 10;
  var RUNSTATE_LOAD_CONSUMER_EVENTS = 11;
  var RUNSTATE_LOAD_PRODUCER_EVENTS = 12;
  var RUNSTATE_LOGIN_COMPLETE = 13;
  var RUNSTATE_RUN = 14;
  var RESERVED_TOP_BIT = 268435456;
  var CAN_OPENLCB_MSG = 134217728;
  var MASK_CAN_FRAME_SEQUENCE_NUMBER = 117440512;
  var MASK_CAN_FRAME_TYPE = MASK_CAN_FRAME_SEQUENCE_NUMBER;
  var MASK_CAN_VARIABLE_FIELD = 16773120;
  var OPENLCB_MESSAGE_STANDARD_FRAME_TYPE = 16777216;
  var CAN_FRAME_TYPE_DATAGRAM_ONLY = 33554432;
  var CAN_FRAME_TYPE_DATAGRAM_FIRST = 50331648;
  var CAN_FRAME_TYPE_DATAGRAM_MIDDLE = 67108864;
  var CAN_FRAME_TYPE_DATAGRAM_FINAL = 83886080;
  var CAN_FRAME_TYPE_RESERVED = 100663296;
  var CAN_FRAME_TYPE_STREAM = 117440512;
  var MTI_INITIALIZATION_COMPLETE = 256;
  var MTI_INITIALIZATION_COMPLETE_SIMPLE = 257;
  var MTI_VERIFY_NODE_ID_ADDRESSED = 1160;
  var MTI_VERIFY_NODE_ID_GLOBAL = 1168;
  var MTI_VERIFIED_NODE_ID = 368;
  var MTI_VERIFIED_NODE_ID_SIMPLE = 369;
  var MTI_OPTIONAL_INTERACTION_REJECTED = 104;
  var MTI_TERMINATE_DUE_TO_ERROR = 168;
  var MTI_PROTOCOL_SUPPORT_INQUIRY = 2088;
  var MTI_PROTOCOL_SUPPORT_REPLY = 1640;
  var MTI_CONSUMER_IDENTIFY = 2292;
  var MTI_CONSUMER_RANGE_IDENTIFIED = 1188;
  var MTI_CONSUMER_IDENTIFIED_UNKNOWN = 1223;
  var MTI_CONSUMER_IDENTIFIED_SET = 1220;
  var MTI_CONSUMER_IDENTIFIED_CLEAR = 1221;
  var MTI_CONSUMER_IDENTIFIED_RESERVED = 1222;
  var MTI_PRODUCER_IDENTIFY = 2324;
  var MTI_PRODUCER_RANGE_IDENTIFIED = 1316;
  var MTI_PRODUCER_IDENTIFIED_UNKNOWN = 1351;
  var MTI_PRODUCER_IDENTIFIED_SET = 1348;
  var MTI_PRODUCER_IDENTIFIED_CLEAR = 1349;
  var MTI_PRODUCER_IDENTIFIED_RESERVED = 1350;
  var MTI_EVENTS_IDENTIFY_DEST = 2408;
  var MTI_EVENTS_IDENTIFY = 2416;
  var MTI_EVENT_LEARN = 1428;
  var MTI_PC_EVENT_REPORT = 1460;
  var MTI_PC_EVENT_REPORT_WITH_PAYLOAD = 3860;
  var MTI_SIMPLE_NODE_INFO_REQUEST = 3560;
  var MTI_SIMPLE_NODE_INFO_REPLY = 2568;
  var MTI_TRAIN_PROTOCOL = 1515;
  var MTI_TRAIN_REPLY = 489;
  var MTI_SIMPLE_TRAIN_INFO_REQUEST = 3496;
  var MTI_SIMPLE_TRAIN_INFO_REPLY = 2504;
  var MTI_STREAM_INIT_REQUEST = 3272;
  var MTI_STREAM_INIT_REPLY = 2152;
  var MTI_FRAME_TYPE_CAN_STREAM_SEND = 61440;
  var MTI_STREAM_SEND = 8072;
  var MTI_STREAM_PROCEED = 2184;
  var MTI_STREAM_COMPLETE = 2216;
  var STREAM_REPLY_ACCEPT = 32768;
  var STREAM_ID_RESERVED = 255;
  var MTI_DATAGRAM = 7240;
  var MTI_DATAGRAM_OK_REPLY = 2600;
  var MTI_DATAGRAM_REJECTED_REPLY = 2632;
  var MASK_MULTIFRAME_BITS = 48;
  var MULTIFRAME_ONLY = 0;
  var MULTIFRAME_FIRST = 16;
  var MULTIFRAME_MIDDLE = 48;
  var MULTIFRAME_FINAL = 32;
  var MASK_STREAM_OR_DATAGRAM = 4096;
  var MASK_PRIORITY = 3072;
  var MASK_SIMPLE_PROTOCOL = 16;
  var MASK_DEST_ADDRESS_PRESENT = 8;
  var MASK_EVENT_PRESENT = 4;
  var MASK_PRIORITY_MODIFIER = 3;
  var CAN_CONTROL_FRAME_CID7 = 117440512;
  var CAN_CONTROL_FRAME_CID6 = 100663296;
  var CAN_CONTROL_FRAME_CID5 = 83886080;
  var CAN_CONTROL_FRAME_CID4 = 67108864;
  var CAN_CONTROL_FRAME_CID3 = 50331648;
  var CAN_CONTROL_FRAME_CID2 = 33554432;
  var CAN_CONTROL_FRAME_CID1 = 16777216;
  var CAN_CONTROL_FRAME_RID = 7340032;
  var CAN_CONTROL_FRAME_AMD = 7344128;
  var CAN_CONTROL_FRAME_AME = 7348224;
  var CAN_CONTROL_FRAME_AMR = 7352320;
  var CAN_CONTROL_FRAME_ERROR_INFO_REPORT_0 = 7405568;
  var CAN_CONTROL_FRAME_ERROR_INFO_REPORT_1 = 7409664;
  var CAN_CONTROL_FRAME_ERROR_INFO_REPORT_2 = 7413760;
  var CAN_CONTROL_FRAME_ERROR_INFO_REPORT_3 = 7417856;
  var MASK_CAN_STREAM_OR_DATAGRAM = 16777216;
  var MASK_CAN_PRIORITY = 12582912;
  var MASK_CAN_SIMPLE_PROTOCOL = 65536;
  var MASK_CAN_DEST_ADDRESS_PRESENT = 32768;
  var MASK_CAN_EVENT_PRESENT = 16384;
  var MASK_CAN_PRIORITY_MODIFIER = 12288;
  var MASK_CAN_SOURCE_ALIAS = 4095;
  var PSI_SIMPLE = 8388608;
  var PSI_DATAGRAM = 4194304;
  var PSI_STREAM = 2097152;
  var PSI_MEMORY_CONFIGURATION = 1048576;
  var PSI_RESERVATION = 524288;
  var PSI_EVENT_EXCHANGE = 262144;
  var PSI_IDENTIFICATION = 131072;
  var PSI_TEACHING_LEARNING = 65536;
  var PSI_REMOTE_BUTTON = 32768;
  var PSI_ABBREVIATED_DEFAULT_CDI = 16384;
  var PSI_DISPLAY = 8192;
  var PSI_SIMPLE_NODE_INFORMATION = 4096;
  var PSI_CONFIGURATION_DESCRIPTION_INFO = 2048;
  var PSI_TRAIN_CONTROL = 1024;
  var PSI_FUNCTION_DESCRIPTION = 512;
  var PSI_RESERVED_0 = 256;
  var PSI_RESERVED_1 = 128;
  var PSI_FUNCTION_CONFIGURATION = 64;
  var PSI_FIRMWARE_UPGRADE = 32;
  var PSI_FIRMWARE_UPGRADE_ACTIVE = 16;
  var EVENT_ID_EMERGENCY_OFF = 0x010000000000FFFFn;
  var EVENT_ID_CLEAR_EMERGENCY_OFF = 0x010000000000FFFEn;
  var EVENT_ID_EMERGENCY_STOP = 0x010000000000FFFDn;
  var EVENT_ID_CLEAR_EMERGENCY_STOP = 0x010000000000FFFCn;
  var EVENT_ID_NODE_RECORDED_NEW_LOG = 0x010000000000FFF8n;
  var EVENT_ID_POWER_SUPPLY_BROWN_OUT_NODE = 0x010000000000FFF1n;
  var EVENT_ID_POWER_SUPPLY_BROWN_OUT_STANDARD = 0x010000000000FFF0n;
  var EVENT_ID_IDENT_BUTTON_COMBO_PRESSED = 0x010000000000FF00n;
  var EVENT_ID_LINK_ERROR_CODE_1 = 0x010000000000FF01n;
  var EVENT_ID_LINK_ERROR_CODE_2 = 0x010000000000FF02n;
  var EVENT_ID_LINK_ERROR_CODE_3 = 0x010000000000FF03n;
  var EVENT_ID_LINK_ERROR_CODE_4 = 0x010000000000FF04n;
  var EVENT_ID_DUPLICATE_NODE_DETECTED = 0x0101000000000201n;
  var EVENT_ID_TRAIN = 0x0101000000000303n;
  var EVENT_ID_TRAIN_PROXY = 0x0101000000000304n;
  var EVENT_ID_FIRMWARE_CORRUPTED = 0x0101000000000601n;
  var EVENT_ID_FIRMWARE_UPGRADE_BY_HARDWARE_SWITCH = 0x0101000000000602n;
  var EVENT_ID_CBUS_OFF_SPACE = 0x0101010000000000n;
  var EVENT_ID_CBUS_ON_SPACE = 0x0101010100000000n;
  var EVENT_ID_DCC_ACCESSORY_ACTIVATE = 0x0101020000FF0000n;
  var EVENT_ID_DCC_ACCESSORY_DEACTIVATE = 0x0101020000FE0000n;
  var EVENT_ID_DCC_TURNOUT_FEEDBACK_HIGH = 0x0101020000FD0000n;
  var EVENT_ID_DCC_TURNOUT_FEEDBACK_LOW = 0x0101020000FC0000n;
  var EVENT_ID_DCC_SENSOR_FEEDBACK_HIGH = 0x0101020000FB0000n;
  var EVENT_ID_DCC_SENSOR_FEEDBACK_LO = 0x0101020000FA0000n;
  var EVENT_ID_DCC_EXTENDED_ACCESSORY_CMD_SPACE = 0x01010200010000FFn;
  var EVENT_TRAIN_SEARCH_SPACE = 0x090099FF00000000n;
  var TRAIN_SEARCH_MASK = 0xFFFFFFFF00000000n;
  var TRAIN_SEARCH_FLAG_ALLOCATE = 128;
  var TRAIN_SEARCH_FLAG_EXACT = 64;
  var TRAIN_SEARCH_FLAG_ADDRESS_ONLY = 32;
  var TRAIN_SEARCH_FLAG_DCC = 8;
  var TRAIN_SEARCH_FLAG_LONG_ADDR = 4;
  var TRAIN_SEARCH_SPEED_STEP_MASK = 3;
  var TRAIN_MAX_DCC_SHORT_ADDRESS = 128;
  var S_OK = 0;
  var ERROR_PERMANENT = 4096;
  var ERROR_PERMANENT_STREAMS_NOT_SUPPORTED = 4112;
  var ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN = 4225;
  var ERROR_PERMANENT_CONFIG_MEM_OUT_OF_BOUNDS_INVALID_ADDRESS = 4226;
  var ERROR_PERMANENT_CONFIG_MEM_ADDRESS_WRITE_TO_READ_ONLY = 4227;
  var ERROR_PERMANENT_SOURCE_NOT_PERMITTED = 4128;
  var ERROR_PERMANENT_NOT_IMPLEMENTED = 4160;
  var ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN = 4161;
  var ERROR_PERMANENT_NOT_IMPLEMENTED_COMMAND_UNKNOWN = 4162;
  var ERROR_PERMANENT_NOT_IMPLEMENTED_UNKNOWN_MTI_OR_TRANPORT_PROTOCOL = 4163;
  var ERROR_CODE_PERMANENT_COUNT_OUT_OF_RANGE = 4164;
  var ERROR_PERMANENT_INVALID_ARGUMENTS = 4224;
  var ERROR_TEMPORARY = 8192;
  var ERROR_TEMPORARY_BUFFER_UNAVAILABLE = 8224;
  var ERROR_TEMPORARY_NOT_EXPECTED_OUT_OF_ORDER = 8256;
  var ERROR_TEMPORARY_TRANSFER_ERROR = 8320;
  var ERROR_TEMPORARY_TIME_OUT = 8209;
  var ERROR_TEMPORARY_OUT_OF_ORDER_MIDDLE_END_WITH_NO_START = 8257;
  var ERROR_TEMPORARY_OUT_OF_ORDER_START_BEFORE_LAST_END = 8258;
  var DATAGRAM_OK_REPLY_PENDING = 128;
  var CONFIG_MEM_CONFIGURATION = 32;
  var CONFIG_MEM_READ_SPACE_IN_BYTE_6 = 64;
  var CONFIG_MEM_READ_SPACE_FD = 65;
  var CONFIG_MEM_READ_SPACE_FE = 66;
  var CONFIG_MEM_READ_SPACE_FF = 67;
  var CONFIG_MEM_READ_REPLY_OK_SPACE_IN_BYTE_6 = 80;
  var CONFIG_MEM_READ_REPLY_OK_SPACE_FD = 81;
  var CONFIG_MEM_READ_REPLY_OK_SPACE_FE = 82;
  var CONFIG_MEM_READ_REPLY_OK_SPACE_FF = 83;
  var CONFIG_MEM_READ_REPLY_FAIL_SPACE_IN_BYTE_6 = 88;
  var CONFIG_MEM_READ_REPLY_FAIL_SPACE_FD = 89;
  var CONFIG_MEM_READ_REPLY_FAIL_SPACE_FE = 90;
  var CONFIG_MEM_READ_REPLY_FAIL_SPACE_FF = 91;
  var CONFIG_MEM_READ_STREAM_SPACE_IN_BYTE_6 = 96;
  var CONFIG_MEM_READ_STREAM_SPACE_FD = 97;
  var CONFIG_MEM_READ_STREAM_SPACE_FE = 98;
  var CONFIG_MEM_READ_STREAM_SPACE_FF = 99;
  var CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_IN_BYTE_6 = 112;
  var CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FD = 113;
  var CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FE = 114;
  var CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FF = 115;
  var CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_IN_BYTE_6 = 120;
  var CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FD = 121;
  var CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FE = 122;
  var CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FF = 123;
  var CONFIG_MEM_WRITE_SPACE_IN_BYTE_6 = 0;
  var CONFIG_MEM_WRITE_SPACE_FD = 1;
  var CONFIG_MEM_WRITE_SPACE_FE = 2;
  var CONFIG_MEM_WRITE_SPACE_FF = 3;
  var CONFIG_MEM_WRITE_REPLY_OK_SPACE_IN_BYTE_6 = 16;
  var CONFIG_MEM_WRITE_REPLY_OK_SPACE_FD = 17;
  var CONFIG_MEM_WRITE_REPLY_OK_SPACE_FE = 18;
  var CONFIG_MEM_WRITE_REPLY_OK_SPACE_FF = 19;
  var CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_IN_BYTE_6 = 24;
  var CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FD = 25;
  var CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FE = 26;
  var CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FF = 27;
  var CONFIG_MEM_WRITE_UNDER_MASK_SPACE_IN_BYTE_6 = 8;
  var CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FD = 9;
  var CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FE = 10;
  var CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FF = 11;
  var CONFIG_MEM_WRITE_STREAM_SPACE_IN_BYTE_6 = 32;
  var CONFIG_MEM_WRITE_STREAM_SPACE_FD = 33;
  var CONFIG_MEM_WRITE_STREAM_SPACE_FE = 34;
  var CONFIG_MEM_WRITE_STREAM_SPACE_FF = 35;
  var CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_IN_BYTE_6 = 48;
  var CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FD = 49;
  var CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FE = 50;
  var CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FF = 51;
  var CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_IN_BYTE_6 = 56;
  var CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FD = 57;
  var CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FE = 58;
  var CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FF = 59;
  var CONFIG_MEM_OPTIONS_CMD = 128;
  var CONFIG_MEM_OPTIONS_REPLY = 130;
  var CONFIG_MEM_GET_ADDRESS_SPACE_INFO_CMD = 132;
  var CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_NOT_PRESENT = 134;
  var CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_PRESENT = 135;
  var CONFIG_MEM_RESERVE_LOCK = 136;
  var CONFIG_MEM_RESERVE_LOCK_REPLY = 138;
  var CONFIG_MEM_GET_UNIQUE_ID = 140;
  var CONFIG_MEM_GET_UNIQUE_ID_REPLY = 141;
  var CONFIG_MEM_UNFREEZE = 160;
  var CONFIG_MEM_FREEZE = 161;
  var CONFIG_MEM_UPDATE_COMPLETE = 168;
  var CONFIG_MEM_RESET_REBOOT = 169;
  var CONFIG_MEM_FACTORY_RESET = 170;
  var CONFIG_MEM_SPACE_CONFIGURATION_DEFINITION_INFO = 255;
  var CONFIG_MEM_SPACE_ALL = 254;
  var CONFIG_MEM_SPACE_CONFIGURATION_MEMORY = 253;
  var CONFIG_MEM_SPACE_ACDI_MANUFACTURER_ACCESS = 252;
  var CONFIG_MEM_SPACE_ACDI_USER_ACCESS = 251;
  var CONFIG_MEM_SPACE_TRAIN_FUNCTION_DEFINITION_INFO = 250;
  var CONFIG_MEM_SPACE_TRAIN_FUNCTION_CONFIGURATION_MEMORY = 249;
  var CONFIG_MEM_SPACE_FIRMWARE = 239;
  var CONFIG_MEM_ACDI_MANUFACTURER_VERSION_ADDRESS = 0;
  var CONFIG_MEM_ACDI_MANUFACTURER_ADDRESS = 1;
  var CONFIG_MEM_ACDI_MODEL_ADDRESS = 42;
  var CONFIG_MEM_ACDI_HARDWARE_VERSION_ADDRESS = 83;
  var CONFIG_MEM_ACDI_SOFTWARE_VERSION_ADDRESS = 104;
  var CONFIG_MEM_ACDI_VERSION_LEN = 1;
  var CONFIG_MEM_ACDI_MANUFACTURER_LEN = 41;
  var CONFIG_MEM_ACDI_MODEL_LEN = 41;
  var CONFIG_MEM_ACDI_HARDWARE_VERSION_LEN = 21;
  var CONFIG_MEM_ACDI_SOFTWARE_VERSION_LEN = 21;
  var CONFIG_MEM_USER_MODEL_ADDRESS = 0;
  var CONFIG_MEM_USER_DESCRIPTION_ADDRESS = 63;
  var CONFIG_MEM_ACDI_USER_VERSION_ADDRESS = 0;
  var CONFIG_MEM_ACDI_USER_NAME_ADDRESS = 1;
  var CONFIG_MEM_ACDI_USER_DESCRIPTION_ADDRESS = 64;
  var CONFIG_MEM_ACDI_USER_VERSION_LEN = 1;
  var CONFIG_MEM_ACDI_USER_NAME_LEN = 63;
  var CONFIG_MEM_ACDI_USER_DESCRIPTION_LEN = 64;
  var CONFIG_MEM_CONFIG_USER_NAME_OFFSET = 0;
  var CONFIG_MEM_CONFIG_USER_DESCRIPTION_OFFSET = 63;
  var CONFIG_MEM_REPLY_OK_OFFSET = 16;
  var CONFIG_MEM_REPLY_FAIL_OFFSET = 24;
  var CONFIG_OPTIONS_COMMANDS_WRITE_UNDER_MASK = 32768;
  var CONFIG_OPTIONS_COMMANDS_UNALIGNED_READS = 16384;
  var CONFIG_OPTIONS_COMMANDS_UNALIGNED_WRITES = 8192;
  var CONFIG_OPTIONS_COMMANDS_ACDI_MANUFACTURER_READ = 2048;
  var CONFIG_OPTIONS_COMMANDS_ACDI_USER_READ = 1024;
  var CONFIG_OPTIONS_COMMANDS_ACDI_USER_WRITE = 512;
  var CONFIG_OPTIONS_WRITE_LENGTH_RESERVED = 128 | 64 | 32 | 2;
  var CONFIG_OPTIONS_WRITE_LENGTH_STREAM_READ_WRITE = 1;
  var CONFIG_OPTIONS_SPACE_INFO_FLAG_READ_ONLY = 1;
  var CONFIG_OPTIONS_SPACE_INFO_FLAG_USE_LOW_ADDRESS = 2;
  var MAX_INTERNAL_ENUM_KEYS_VALUES = 6;
  var MAX_USER_ENUM_KEYS_VALUES = 4;
  var USER_ENUM_KEYS_VALUES_1 = 0;
  var USER_ENUM_KEYS_VALUES_2 = 1;
  var USER_ENUM_KEYS_VALUES_3 = 2;
  var USER_ENUM_KEYS_VALUES_4 = 3;
  var MAX_NODE_ENUM_KEY_VALUES = MAX_USER_ENUM_KEYS_VALUES + MAX_INTERNAL_ENUM_KEYS_VALUES;
  var OPENLCB_MAIN_STATMACHINE_NODE_ENUMERATOR_INDEX = MAX_USER_ENUM_KEYS_VALUES;
  var OPENLCB_LOGIN_STATMACHINE_NODE_ENUMERATOR_INDEX = MAX_USER_ENUM_KEYS_VALUES + 1;
  var CAN_STATEMACHINE_NODE_ENUMRATOR_KEY = MAX_USER_ENUM_KEYS_VALUES + 2;
  var DATAGRAM_TIMEOUT_ENUM_KEY = MAX_USER_ENUM_KEYS_VALUES + 3;
  var OPENLCB_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX = MAX_USER_ENUM_KEYS_VALUES + 4;
  var OPENLCB_LOGIN_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX = MAX_USER_ENUM_KEYS_VALUES + 5;
  var BROADCAST_TIME_ID_DEFAULT_FAST_CLOCK = 0x0101000001000000n;
  var BROADCAST_TIME_ID_DEFAULT_REALTIME_CLOCK = 0x0101000001010000n;
  var BROADCAST_TIME_ID_ALTERNATE_CLOCK_1 = 0x0101000001020000n;
  var BROADCAST_TIME_ID_ALTERNATE_CLOCK_2 = 0x0101000001030000n;
  var BROADCAST_TIME_MASK_CLOCK_ID = 0xFFFFFFFFFFFF0000n;
  var BROADCAST_TIME_MASK_COMMAND_DATA = 0x000000000000FFFFn;
  var BROADCAST_TIME_REPORT_TIME_BASE = 0;
  var BROADCAST_TIME_REPORT_DATE_BASE = 8448;
  var BROADCAST_TIME_REPORT_YEAR_BASE = 12288;
  var BROADCAST_TIME_REPORT_RATE_BASE = 16384;
  var BROADCAST_TIME_SET_TIME_BASE = 32768;
  var BROADCAST_TIME_SET_DATE_BASE = 41216;
  var BROADCAST_TIME_SET_YEAR_BASE = 45056;
  var BROADCAST_TIME_SET_RATE_BASE = 49152;
  var BROADCAST_TIME_QUERY = 61440;
  var BROADCAST_TIME_STOP = 61441;
  var BROADCAST_TIME_START = 61442;
  var BROADCAST_TIME_DATE_ROLLOVER = 61443;
  var BROADCAST_TIME_SET_COMMAND_OFFSET = 32768;
  var TRAIN_INSTRUCTION_P_BIT = 128;
  var TRAIN_SET_SPEED_DIRECTION = 0;
  var TRAIN_SET_FUNCTION = 1;
  var TRAIN_EMERGENCY_STOP = 2;
  var TRAIN_QUERY_SPEEDS = 16;
  var TRAIN_QUERY_FUNCTION = 17;
  var TRAIN_CONTROLLER_CONFIG = 32;
  var TRAIN_LISTENER_CONFIG = 48;
  var TRAIN_MANAGEMENT = 64;
  var TRAIN_CONTROLLER_ASSIGN = 1;
  var TRAIN_CONTROLLER_RELEASE = 2;
  var TRAIN_CONTROLLER_QUERY = 3;
  var TRAIN_CONTROLLER_CHANGED = 4;
  var TRAIN_LISTENER_ATTACH = 1;
  var TRAIN_LISTENER_DETACH = 2;
  var TRAIN_LISTENER_QUERY = 3;
  var TRAIN_MGMT_RESERVE = 1;
  var TRAIN_MGMT_RELEASE = 2;
  var TRAIN_MGMT_NOOP = 3;
  var TRAIN_LISTENER_FLAG_REVERSE = 2;
  var TRAIN_LISTENER_FLAG_LINK_F0 = 4;
  var TRAIN_LISTENER_FLAG_LINK_FN = 8;
  var TRAIN_LISTENER_FLAG_HIDE = 128;
  var NULL_NODE_ID = 0x000000000000n;
  var NULL_EVENT_ID = 0x0000000000000000n;
  var LEN_MESSAGE_BYTES_BASIC = 16;
  var LEN_MESSAGE_BYTES_DATAGRAM = 72;
  var LEN_MESSAGE_BYTES_SNIP = 256;
  var LEN_MESSAGE_BYTES_STREAM = 256;
  var LEN_MESSAGE_BYTES_WORKER = (
    // max(SNIP, STREAM)
    LEN_MESSAGE_BYTES_STREAM < LEN_MESSAGE_BYTES_SNIP ? LEN_MESSAGE_BYTES_SNIP : LEN_MESSAGE_BYTES_STREAM
  );
  var LEN_EVENT_ID = 8;
  var LEN_DATAGRAM_MAX_PAYLOAD = 64;
  var LEN_EVENT_PAYLOAD = LEN_MESSAGE_BYTES_SNIP;
  var LEN_SNIP_NAME_BUFFER = 41;
  var LEN_SNIP_MODEL_BUFFER = 41;
  var LEN_SNIP_HARDWARE_VERSION_BUFFER = 21;
  var LEN_SNIP_SOFTWARE_VERSION_BUFFER = 21;
  var LEN_SNIP_USER_NAME_BUFFER = 63;
  var LEN_SNIP_USER_DESCRIPTION_BUFFER = 64;
  var LEN_SNIP_USER_DATA = LEN_SNIP_USER_NAME_BUFFER + LEN_SNIP_USER_DESCRIPTION_BUFFER;
  var LEN_SNIP_VERSION = 1;
  var LEN_SNIP_USER_VERSION = 1;
  var LEN_SNIP_STRUCTURE = 264;
  var LEN_CONFIG_MEM_OPTIONS_DESCRIPTION = 64 - 1;
  var LEN_CONFIG_MEM_ADDRESS_SPACE_DESCRIPTION = 60 - 1;

  // src/openlcb/types.js
  var PAYLOAD_TYPE = Object.freeze({
    BASIC: "BASIC",
    DATAGRAM: "DATAGRAM",
    SNIP: "SNIP",
    STREAM: "STREAM",
    WORKER: "WORKER"
  });
  var PAYLOAD_TYPE_LEN = Object.freeze({
    BASIC: LEN_MESSAGE_BYTES_BASIC,
    DATAGRAM: LEN_MESSAGE_BYTES_DATAGRAM,
    SNIP: LEN_MESSAGE_BYTES_SNIP,
    STREAM: LEN_MESSAGE_BYTES_STREAM,
    WORKER: LEN_MESSAGE_BYTES_WORKER
  });
  var EVENT_STATUS = Object.freeze({
    UNKNOWN: 0,
    SET: 1,
    CLEAR: 2
  });
  var EVENT_RANGE_COUNT = Object.freeze({
    C_1: 0,
    C_2: 2,
    C_4: 4,
    C_8: 8,
    C_16: 16,
    C_32: 32,
    C_64: 64,
    C_128: 128,
    C_256: 256,
    C_512: 512,
    C_1024: 1024,
    C_2048: 2048,
    C_4096: 4096,
    C_8192: 8192,
    C_16384: 16384,
    C_32768: 32768
  });
  var ADDRESS_SPACE_ENCODING = Object.freeze({
    IN_BYTE_1: 0,
    IN_BYTE_6: 1
  });
  var BROADCAST_TIME_EVENT_TYPE = Object.freeze({
    REPORT_TIME: 0,
    REPORT_DATE: 1,
    REPORT_YEAR: 2,
    REPORT_RATE: 3,
    SET_TIME: 4,
    SET_DATE: 5,
    SET_YEAR: 6,
    SET_RATE: 7,
    QUERY: 8,
    STOP: 9,
    START: 10,
    DATE_ROLLOVER: 11,
    UNKNOWN: 255
  });
  var TRAIN_EMERGENCY_TYPE = Object.freeze({
    ESTOP: 0,
    GLOBAL_STOP: 1,
    GLOBAL_OFF: 2
  });
  function createMessage({
    payloadType = PAYLOAD_TYPE.BASIC,
    mti = 0,
    sourceAlias = 0,
    sourceId = NULL_NODE_ID,
    destAlias = 0,
    destId = NULL_NODE_ID
  } = {}) {
    const len = PAYLOAD_TYPE_LEN[payloadType];
    if (len === void 0) {
      throw new Error(`createMessage: unknown payloadType "${payloadType}"`);
    }
    return {
      state: {
        allocated: false,
        inprocess: false,
        invalid: false,
        loopback: false
      },
      mti,
      sourceAlias,
      destAlias,
      sourceId,
      destId,
      payloadType,
      payloadCount: 0,
      payload: new Uint8Array(len),
      timer: { assemblyTicks: 0, tickSnapshot: 0, retryCount: 0 },
      referenceCount: 0
    };
  }
  function createEvent(event = NULL_EVENT_ID, status = EVENT_STATUS.UNKNOWN) {
    return { event, status };
  }
  function createEventRange(startBase = NULL_EVENT_ID, eventCount = EVENT_RANGE_COUNT.C_1) {
    return { startBase, eventCount };
  }
  function createNode(id = NULL_NODE_ID, parameters = null) {
    return {
      state: {
        runState: 0,
        allocated: false,
        permitted: false,
        initialized: false,
        duplicateIdDetected: false,
        openlcbDatagramAckSent: false,
        resendDatagram: false,
        firmwareUpgradeActive: false
      },
      id,
      alias: 0,
      seed: id,
      consumers: {
        count: 0,
        list: [],
        rangeCount: 0,
        rangeList: [],
        enumerator: { running: false, enumIndex: 0, rangeEnumIndex: 0 }
      },
      producers: {
        count: 0,
        list: [],
        rangeCount: 0,
        rangeList: [],
        enumerator: { running: false, enumIndex: 0, rangeEnumIndex: 0 }
      },
      parameters,
      timerticks: 0,
      ownerNode: NULL_NODE_ID,
      lastReceivedDatagram: null,
      index: 0,
      trainState: null
    };
  }

  // src/openlcb/node.js
  var EVENT_ID_OFFSET_BITS = 16n;
  var NodePool = class {
    /**
     * @param {Object} [opts]
     * @param {() => void} [opts.on100msTimerTick] optional application-level tick hook
     */
    constructor({ on100msTimerTick = null } = {}) {
      this._nodes = [];
      this._enumIndex = new Array(MAX_NODE_ENUM_KEY_VALUES).fill(0);
      this._on100msTimerTick = on100msTimerTick;
      this._lastTick = 0;
    }
    initialize() {
      this._nodes.length = 0;
      this._enumIndex.fill(0);
      this._lastTick = 0;
    }
    /**
     * Allocate a new node. Auto-creates consumer/producer events per
     * `parameters.consumerCountAutocreate` / `producerCountAutocreate`,
     * starting from `nodeId << 16`. Returns the node.
     *
     * @param {bigint} nodeId
     * @param {Object} nodeParameters
     * @returns {Object} node
     */
    allocate(nodeId, nodeParameters) {
      const node = createNode(nodeId, nodeParameters);
      node.index = this._nodes.length;
      node.state.allocated = true;
      node.state.runState = RUNSTATE_INIT;
      node.seed = nodeId;
      this._generateEventIds(node);
      this._nodes.push(node);
      return node;
    }
    _generateEventIds(node) {
      const base = BigInt.asUintN(64, node.id) << EVENT_ID_OFFSET_BITS;
      const params = node.parameters ?? {};
      const consumerCount = params.consumerCountAutocreate ?? 0;
      const producerCount = params.producerCountAutocreate ?? 0;
      node.consumers.list = [];
      for (let i = 0; i < consumerCount; i++) {
        node.consumers.list.push({ event: base + BigInt(i), status: 0 });
      }
      node.consumers.count = consumerCount;
      node.consumers.rangeList = node.consumers.rangeList ?? [];
      node.consumers.rangeCount = node.consumers.rangeList.length;
      node.consumers.enumerator = { running: false, enumIndex: 0, rangeEnumIndex: 0 };
      node.producers.list = [];
      for (let i = 0; i < producerCount; i++) {
        node.producers.list.push({ event: base + BigInt(i), status: 0 });
      }
      node.producers.count = producerCount;
      node.producers.rangeList = node.producers.rangeList ?? [];
      node.producers.rangeCount = node.producers.rangeList.length;
      node.producers.enumerator = { running: false, enumIndex: 0, rangeEnumIndex: 0 };
    }
    // -------------------------------------------------------------------------
    // Enumeration — independent cursors keyed by 0..MAX_NODE_ENUM_KEY_VALUES-1
    // -------------------------------------------------------------------------
    getFirst(key) {
      if (key >= MAX_NODE_ENUM_KEY_VALUES) return null;
      this._enumIndex[key] = 0;
      return this._nodes[0] ?? null;
    }
    getNext(key) {
      if (key >= MAX_NODE_ENUM_KEY_VALUES) return null;
      const idx = ++this._enumIndex[key];
      return idx < this._nodes.length ? this._nodes[idx] : null;
    }
    isLast(key) {
      if (key >= MAX_NODE_ENUM_KEY_VALUES) return false;
      if (this._nodes.length === 0) return false;
      return this._enumIndex[key] >= this._nodes.length - 1;
    }
    // -------------------------------------------------------------------------
    // Lookup
    // -------------------------------------------------------------------------
    findByAlias(alias) {
      for (const n of this._nodes) {
        if (n.alias === alias) return n;
      }
      return null;
    }
    findByNodeId(nodeId) {
      for (const n of this._nodes) {
        if (n.id === nodeId) return n;
      }
      return null;
    }
    getCount() {
      return this._nodes.length;
    }
    /** Underlying array — kept for callers that need to iterate. Do not mutate. */
    getAllNodes() {
      return this._nodes;
    }
    // -------------------------------------------------------------------------
    // Lifecycle
    // -------------------------------------------------------------------------
    /** Reset every node to pre-login state without deallocating. */
    resetState() {
      for (const n of this._nodes) {
        n.state.runState = RUNSTATE_INIT;
        n.state.permitted = false;
        n.state.initialized = false;
      }
    }
    /**
     * Fires the optional application callback at most once per unique tick
     * value. The per-node `timerticks` field is no longer incremented — the
     * main loop uses elapsed-time subtraction against the global tick.
     */
    timerTick(currentTick) {
      if ((currentTick - this._lastTick & 255) === 0) return;
      this._lastTick = currentTick & 255;
      if (this._on100msTimerTick) this._on100msTimerTick();
    }
  };

  // src/openlcb/message-fifo.js
  var MessageFifo = class {
    constructor() {
      this._queue = [];
    }
    /** Reset the queue to empty. */
    initialize() {
      this._queue.length = 0;
    }
    /**
     * Append a message to the tail. Returns the queued message (never null —
     * the JS port has no fixed capacity).
     * @param {Object} msg
     */
    push(msg) {
      this._queue.push(msg);
      return msg;
    }
    /**
     * Remove and return the oldest message, or null if the queue is empty.
     * @returns {Object|null}
     */
    pop() {
      if (this._queue.length === 0) return null;
      return this._queue.shift();
    }
    isEmpty() {
      return this._queue.length === 0;
    }
    getAllocatedCount() {
      return this._queue.length;
    }
    /**
     * Mark every queued message whose sourceAlias matches `alias` as invalid.
     * Called when an AMR retires an alias so stale incoming messages don't
     * generate replies to a gone-away node. Matches the C semantics: the
     * messages stay in the FIFO; the pop-phase / TX guard discards them.
     *
     * @param {number} alias 12-bit CAN alias. If 0, does nothing.
     */
    checkAndInvalidateMessagesBySourceAlias(alias) {
      if (alias === 0) return;
      for (const msg of this._queue) {
        if (msg.sourceAlias === alias) {
          msg.state.invalid = true;
        }
      }
    }
  };

  // src/drivers/can/types.js
  var LEN_CAN_BYTE_ARRAY = 8;
  var OFFSET_CAN_WITHOUT_DEST_ADDRESS = 0;
  var OFFSET_CAN_WITH_DEST_ADDRESS = 2;
  var CAN_MTI_PCER_WITH_PAYLOAD_FIRST = 3862;
  var CAN_MTI_PCER_WITH_PAYLOAD_MIDDLE = 3861;
  var CAN_MTI_PCER_WITH_PAYLOAD_LAST = 3860;
  function createCanMsg({ identifier = 0, payloadCount = 0, payload = null } = {}) {
    const bytes = new Uint8Array(LEN_CAN_BYTE_ARRAY);
    if (payload) {
      const n = Math.min(payload.length, LEN_CAN_BYTE_ARRAY);
      for (let i = 0; i < n; i++) bytes[i] = payload[i] & 255;
      if (payloadCount === 0) payloadCount = n;
    }
    return {
      state: { allocated: false },
      identifier: identifier >>> 0,
      payloadCount,
      payload: bytes
    };
  }
  function createAliasMapping(nodeId = 0n, alias = 0) {
    return {
      nodeId,
      alias,
      isDuplicate: false,
      isPermitted: false
    };
  }
  function createListenerAliasEntry() {
    return {
      nodeId: 0n,
      alias: 0,
      verifyTicks: 0,
      verifyPending: false
    };
  }

  // src/drivers/can/alias-mappings.js
  var MAX_NODE_ID = 0xFFFFFFFFFFFFn;
  var AliasMappings = class {
    constructor(capacity = 16) {
      this._capacity = capacity;
      this._list = [];
      for (let i = 0; i < capacity; i++) {
        this._list.push(createAliasMapping());
      }
      this.hasDuplicateAlias = false;
    }
    initialize() {
      this.flush();
    }
    flush() {
      for (const e of this._list) {
        e.nodeId = 0n;
        e.alias = 0;
        e.isDuplicate = false;
        e.isPermitted = false;
      }
      this.hasDuplicateAlias = false;
    }
    /** Pointer-equivalent: caller may read/write flags directly. */
    getList() {
      return this._list;
    }
    /** Kept for C API parity; prefer `getList()` + `hasDuplicateAlias`. */
    getAliasMappingInfo() {
      return { list: this._list, hasDuplicateAlias: this.hasDuplicateAlias };
    }
    setHasDuplicateAliasFlag() {
      this.hasDuplicateAlias = true;
    }
    clearHasDuplicateAliasFlag() {
      this.hasDuplicateAlias = false;
    }
    /**
     * Register or refresh an (alias, nodeId) pair. Reuses the existing slot if
     * nodeId is already present (correct conflict-recovery behaviour), else
     * takes the first empty slot. Returns the entry, or null on invalid input
     * / table full.
     */
    register(alias, nodeId) {
      if (alias === 0 || alias > 4095) return null;
      if (nodeId === 0n || nodeId > MAX_NODE_ID) return null;
      for (const e of this._list) {
        if (e.alias === 0 || e.nodeId === nodeId) {
          e.alias = alias;
          e.nodeId = nodeId;
          return e;
        }
      }
      return null;
    }
    /** Remove the entry with the given alias. Safe to call on non-existent aliases. */
    unregister(alias) {
      for (const e of this._list) {
        if (e.alias === alias) {
          e.alias = 0;
          e.nodeId = 0n;
          e.isDuplicate = false;
          e.isPermitted = false;
          return;
        }
      }
    }
    findMappingByAlias(alias) {
      if (alias === 0 || alias > 4095) return null;
      for (const e of this._list) {
        if (e.alias === alias) return e;
      }
      return null;
    }
    findMappingByNodeId(nodeId) {
      if (nodeId === 0n || nodeId > MAX_NODE_ID) return null;
      for (const e of this._list) {
        if (e.nodeId === nodeId) return e;
      }
      return null;
    }
  };

  // src/drivers/can/alias-mapping-listener.js
  var AliasMappingListener = class {
    /**
     * @param {Object} opts
     * @param {number} [opts.capacity=16] table depth
     * @param {number} [opts.probeTickInterval=10]   rate-limit (100ms ticks) between prober calls
     * @param {number} [opts.probeIntervalTicks=600] age (prober ticks) before a resolved alias is re-probed
     * @param {number} [opts.verifyTimeoutTicks=30]  AME reply window before the alias is declared stale
     */
    constructor({
      capacity = 16,
      probeTickInterval = 10,
      probeIntervalTicks = 600,
      verifyTimeoutTicks = 30
    } = {}) {
      this._capacity = capacity;
      this._probeTickInterval = probeTickInterval;
      this._probeIntervalTicks = probeIntervalTicks;
      this._verifyTimeoutTicks = verifyTimeoutTicks;
      this._table = [];
      for (let i = 0; i < capacity; i++) {
        this._table.push(createListenerAliasEntry());
      }
      this._verifyCursor = 0;
      this._verifyLastTick = 0;
      this._verifyCounter = 0;
    }
    initialize() {
      for (const e of this._table) {
        e.nodeId = 0n;
        e.alias = 0;
        e.verifyTicks = 0;
        e.verifyPending = false;
      }
      this._verifyCursor = 0;
      this._verifyLastTick = 0;
      this._verifyCounter = 0;
    }
    /**
     * Register a listener Node ID with alias = 0. If nodeId already exists,
     * returns the existing entry unchanged. Returns null if table full or
     * nodeId is 0.
     */
    register(nodeId) {
      if (nodeId === 0n) return null;
      for (const e of this._table) {
        if (e.nodeId === nodeId) return e;
      }
      for (const e of this._table) {
        if (e.nodeId === 0n) {
          e.nodeId = nodeId;
          e.alias = 0;
          e.verifyTicks = 0;
          e.verifyPending = false;
          return e;
        }
      }
      return null;
    }
    unregister(nodeId) {
      if (nodeId === 0n) return;
      for (const e of this._table) {
        if (e.nodeId === nodeId) {
          e.nodeId = 0n;
          e.alias = 0;
          e.verifyTicks = 0;
          e.verifyPending = false;
          return;
        }
      }
    }
    /**
     * Store a resolved alias for a registered Node ID. No-op if the nodeId
     * isn't in the table (i.e. this AMD isn't from one of our listeners) or
     * if the alias is out of range.
     */
    setAlias(nodeId, alias) {
      if (alias === 0 || alias > 4095) return;
      if (nodeId === 0n) return;
      for (const e of this._table) {
        if (e.nodeId === nodeId) {
          e.alias = alias;
          e.verifyTicks = this._verifyCounter;
          e.verifyPending = false;
          return;
        }
      }
    }
    findByNodeId(nodeId) {
      if (nodeId === 0n) return null;
      for (const e of this._table) {
        if (e.nodeId === nodeId) return e;
      }
      return null;
    }
    /** Clear every alias but keep nodeIds (response to a global AME). */
    flushAliases() {
      for (const e of this._table) {
        e.alias = 0;
        e.verifyPending = false;
      }
    }
    /** Clear a single entry by its current alias (response to AMR). */
    clearAliasByAlias(alias) {
      if (alias === 0) return;
      for (const e of this._table) {
        if (e.alias === alias) {
          e.alias = 0;
          e.verifyPending = false;
          return;
        }
      }
    }
    /**
     * Round-robin probe one listener entry. Returns the Node ID whose alias
     * should be verified (caller sends a targeted AME), or 0n if nothing to
     * do this tick. Rate-limited to once per probeTickInterval; at most one
     * entry per call.
     */
    checkOneVerification(currentTick) {
      const elapsed = currentTick - this._verifyLastTick & 255;
      if (elapsed < this._probeTickInterval) return 0n;
      this._verifyLastTick = currentTick & 255;
      this._verifyCounter = this._verifyCounter + 1 & 65535;
      for (let scanned = 0; scanned < this._capacity; scanned++) {
        this._verifyCursor = (this._verifyCursor + 1) % this._capacity;
        const entry = this._table[this._verifyCursor];
        if (entry.nodeId === 0n) continue;
        if (entry.alias === 0 && !entry.verifyPending) continue;
        if (entry.verifyPending) {
          const age2 = this._verifyCounter - entry.verifyTicks & 65535;
          if (age2 >= this._verifyTimeoutTicks) {
            entry.alias = 0;
            entry.verifyPending = false;
          }
          continue;
        }
        const age = this._verifyCounter - entry.verifyTicks & 65535;
        if (age >= this._probeIntervalTicks) {
          entry.verifyPending = true;
          entry.verifyTicks = this._verifyCounter;
          return entry.nodeId;
        }
      }
      return 0n;
    }
  };

  // src/drivers/can/buffer-fifo.js
  var CanBufferFifo = class {
    constructor() {
      this._queue = [];
    }
    initialize() {
      this._queue.length = 0;
    }
    /** Push to the tail. Always succeeds (no capacity in the JS port). Returns true. */
    push(canMsg) {
      this._queue.push(canMsg);
      return true;
    }
    /** Pop from the head. Returns the oldest frame, or null if empty. */
    pop() {
      if (this._queue.length === 0) return null;
      return this._queue.shift();
    }
    isEmpty() {
      return this._queue.length === 0;
    }
    getAllocatedCount() {
      return this._queue.length;
    }
  };

  // src/drivers/can/utilities.js
  var utilities_exports2 = {};
  __export(utilities_exports2, {
    appendCanPayloadToOpenlcbPayload: () => appendCanPayloadToOpenlcbPayload,
    clearCanMessage: () => clearCanMessage,
    convertCanMtiToOpenlcbMti: () => convertCanMtiToOpenlcbMti,
    copy64BitToCanMessage: () => copy64BitToCanMessage,
    copyCanMessage: () => copyCanMessage,
    copyNodeIdToCanPayload: () => copyNodeIdToCanPayload,
    copyOpenlcbPayloadToCanPayload: () => copyOpenlcbPayloadToCanPayload,
    countNullsInPayloads: () => countNullsInPayloads,
    extractCanPayloadAsNodeId: () => extractCanPayloadAsNodeId,
    extractDestAliasFromCanMessage: () => extractDestAliasFromCanMessage,
    extractSourceAliasFromCanIdentifier: () => extractSourceAliasFromCanIdentifier,
    isOpenlcbMessage: () => isOpenlcbMessage,
    loadCanMessage: () => loadCanMessage
  });

  // src/openlcb/utilities.js
  var utilities_exports = {};
  __export(utilities_exports, {
    calculateMemoryOffsetIntoNodeSpace: () => calculateMemoryOffsetIntoNodeSpace,
    clearOpenlcbMessage: () => clearOpenlcbMessage,
    clearOpenlcbMessagePayload: () => clearOpenlcbMessagePayload,
    copyByteArrayToPayload: () => copyByteArrayToPayload,
    copyByteToPayload: () => copyByteToPayload,
    copyDwordToPayload: () => copyDwordToPayload,
    copyEventIdToConfigMemBuffer: () => copyEventIdToConfigMemBuffer,
    copyEventIdToPayload: () => copyEventIdToPayload,
    copyNodeIdToConfigMemBuffer: () => copyNodeIdToConfigMemBuffer,
    copyNodeIdToPayload: () => copyNodeIdToPayload,
    copyStringToPayload: () => copyStringToPayload,
    copyWordToPayload: () => copyWordToPayload,
    countNullsInPayload: () => countNullsInPayload,
    extractByteFromPayload: () => extractByteFromPayload,
    extractDwordFromPayload: () => extractDwordFromPayload,
    extractEventIdFromConfigMemBuffer: () => extractEventIdFromConfigMemBuffer,
    extractEventIdFromPayload: () => extractEventIdFromPayload,
    extractNodeIdFromConfigMemBuffer: () => extractNodeIdFromConfigMemBuffer,
    extractNodeIdFromPayload: () => extractNodeIdFromPayload,
    extractWordFromConfigMemBuffer: () => extractWordFromConfigMemBuffer,
    extractWordFromPayload: () => extractWordFromPayload,
    findConsumerEventIndex: () => findConsumerEventIndex,
    findProducerEventIndex: () => findProducerEventIndex,
    generateEventRangeId: () => generateEventRangeId,
    isAddressedMessageForNode: () => isAddressedMessageForNode,
    isAddressedOpenlcbMessage: () => isAddressedOpenlcbMessage,
    isEventIdInConsumerRanges: () => isEventIdInConsumerRanges,
    isEventIdInProducerRanges: () => isEventIdInProducerRanges,
    loadConfigMemReplyReadFailHeader: () => loadConfigMemReplyReadFailHeader,
    loadConfigMemReplyReadOkHeader: () => loadConfigMemReplyReadOkHeader,
    loadConfigMemReplyWriteFailHeader: () => loadConfigMemReplyWriteFailHeader,
    loadConfigMemReplyWriteOkHeader: () => loadConfigMemReplyWriteOkHeader,
    loadOpenlcbMessage: () => loadOpenlcbMessage,
    payloadTypeToLen: () => payloadTypeToLen,
    setMultiFrameFlag: () => setMultiFrameFlag
  });
  function payloadTypeToLen(payloadType) {
    switch (payloadType) {
      case PAYLOAD_TYPE.BASIC:
        return LEN_MESSAGE_BYTES_BASIC;
      case PAYLOAD_TYPE.DATAGRAM:
        return LEN_MESSAGE_BYTES_DATAGRAM;
      case PAYLOAD_TYPE.SNIP:
        return LEN_MESSAGE_BYTES_SNIP;
      case PAYLOAD_TYPE.STREAM:
        return LEN_MESSAGE_BYTES_STREAM;
      case PAYLOAD_TYPE.WORKER:
        return LEN_MESSAGE_BYTES_WORKER;
      default:
        return 0;
    }
  }
  function loadOpenlcbMessage(msg, sourceAlias, sourceId, destAlias, destId, mti) {
    msg.destAlias = destAlias;
    msg.destId = destId;
    msg.sourceAlias = sourceAlias;
    msg.sourceId = sourceId;
    msg.mti = mti;
    msg.payloadCount = 0;
    msg.timer.assemblyTicks = 0;
    msg.payload.fill(0);
  }
  function clearOpenlcbMessagePayload(msg) {
    msg.payload.fill(0);
    msg.payloadCount = 0;
  }
  function clearOpenlcbMessage(msg) {
    msg.destAlias = 0;
    msg.destId = 0n;
    msg.sourceAlias = 0;
    msg.sourceId = 0n;
    msg.mti = 0;
    msg.payloadCount = 0;
    msg.timer.assemblyTicks = 0;
    msg.referenceCount = 0;
    msg.state.allocated = false;
    msg.state.inprocess = false;
    msg.state.invalid = false;
  }
  function copyEventIdToPayload(msg, eventId) {
    let v = BigInt.asUintN(64, eventId);
    for (let i = 7; i >= 0; i--) {
      msg.payload[i] = Number(v & 0xFFn);
      v >>= 8n;
    }
    msg.payloadCount = 8;
  }
  function copyNodeIdToPayload(msg, nodeId, offset) {
    let v = BigInt.asUintN(48, nodeId);
    for (let i = 5; i >= 0; i--) {
      msg.payload[offset + i] = Number(v & 0xFFn);
      v >>= 8n;
      msg.payloadCount++;
    }
  }
  function copyByteToPayload(msg, byte, offset) {
    msg.payload[offset] = byte & 255;
    msg.payloadCount++;
  }
  function copyWordToPayload(msg, word, offset) {
    msg.payload[offset] = word >>> 8 & 255;
    msg.payload[offset + 1] = word & 255;
    msg.payloadCount += 2;
  }
  function copyDwordToPayload(msg, doubleword, offset) {
    msg.payload[offset] = doubleword >>> 24 & 255;
    msg.payload[offset + 1] = doubleword >>> 16 & 255;
    msg.payload[offset + 2] = doubleword >>> 8 & 255;
    msg.payload[offset + 3] = doubleword & 255;
    msg.payloadCount += 4;
  }
  function copyStringToPayload(msg, string, offset) {
    const payloadLen = payloadTypeToLen(msg.payloadType);
    let counter = 0;
    while (counter < string.length) {
      if (counter + offset < payloadLen - 1) {
        msg.payload[counter + offset] = string.charCodeAt(counter) & 255;
        msg.payloadCount++;
        counter++;
      } else {
        break;
      }
    }
    msg.payload[counter + offset] = 0;
    msg.payloadCount++;
    counter++;
    return counter;
  }
  function copyByteArrayToPayload(msg, byteArray, offset, requestedBytes) {
    const payloadLen = payloadTypeToLen(msg.payloadType);
    let counter = 0;
    for (let i = 0; i < requestedBytes; i++) {
      if (i + offset < payloadLen) {
        msg.payload[i + offset] = byteArray[i] & 255;
        msg.payloadCount++;
        counter++;
      } else {
        break;
      }
    }
    return counter;
  }
  function extractNodeIdFromPayload(msg, offset) {
    const p = msg.payload;
    return BigInt(p[offset]) << 40n | BigInt(p[offset + 1]) << 32n | BigInt(p[offset + 2]) << 24n | BigInt(p[offset + 3]) << 16n | BigInt(p[offset + 4]) << 8n | BigInt(p[offset + 5]);
  }
  function extractEventIdFromPayload(msg) {
    const p = msg.payload;
    return BigInt(p[0]) << 56n | BigInt(p[1]) << 48n | BigInt(p[2]) << 40n | BigInt(p[3]) << 32n | BigInt(p[4]) << 24n | BigInt(p[5]) << 16n | BigInt(p[6]) << 8n | BigInt(p[7]);
  }
  function extractByteFromPayload(msg, offset) {
    return msg.payload[offset];
  }
  function extractWordFromPayload(msg, offset) {
    return (msg.payload[offset] << 8 | msg.payload[offset + 1]) & 65535;
  }
  function extractDwordFromPayload(msg, offset) {
    return (msg.payload[offset] << 24 | msg.payload[offset + 1] << 16 | msg.payload[offset + 2] << 8 | msg.payload[offset + 3]) >>> 0;
  }
  function setMultiFrameFlag(payload, offset, flag) {
    payload[offset] = payload[offset] & 15 | flag & 240;
  }
  function isAddressedOpenlcbMessage(msg) {
    return (msg.mti & MASK_DEST_ADDRESS_PRESENT) === MASK_DEST_ADDRESS_PRESENT;
  }
  function isAddressedMessageForNode(node, msg) {
    return node.alias === msg.destAlias || node.id === msg.destId;
  }
  function countNullsInPayload(msg) {
    let count = 0;
    for (let i = 0; i < msg.payloadCount; i++) {
      if (msg.payload[i] === 0) count++;
    }
    return count;
  }
  function findProducerEventIndex(node, eventId) {
    const list = node.producers.list;
    for (let i = 0; i < node.producers.count; i++) {
      if (list[i].event === eventId) return i;
    }
    return -1;
  }
  function findConsumerEventIndex(node, eventId) {
    const list = node.consumers.list;
    for (let i = 0; i < node.consumers.count; i++) {
      if (list[i].event === eventId) return i;
    }
    return -1;
  }
  function extractNodeIdFromConfigMemBuffer(buffer, index) {
    return BigInt(buffer[index]) << 40n | BigInt(buffer[index + 1]) << 32n | BigInt(buffer[index + 2]) << 24n | BigInt(buffer[index + 3]) << 16n | BigInt(buffer[index + 4]) << 8n | BigInt(buffer[index + 5]);
  }
  function extractWordFromConfigMemBuffer(buffer, index) {
    return (buffer[index] << 8 | buffer[index + 1]) & 65535;
  }
  function copyNodeIdToConfigMemBuffer(buffer, nodeId, index) {
    let v = BigInt.asUintN(48, nodeId);
    for (let i = 5; i >= 0; i--) {
      buffer[index + i] = Number(v & 0xFFn);
      v >>= 8n;
    }
  }
  function copyEventIdToConfigMemBuffer(buffer, eventId, index) {
    let v = BigInt.asUintN(64, eventId);
    for (let i = 7; i >= 0; i--) {
      buffer[index + i] = Number(v & 0xFFn);
      v >>= 8n;
    }
  }
  function extractEventIdFromConfigMemBuffer(buffer, index) {
    return BigInt(buffer[index]) << 56n | BigInt(buffer[index + 1]) << 48n | BigInt(buffer[index + 2]) << 40n | BigInt(buffer[index + 3]) << 32n | BigInt(buffer[index + 4]) << 24n | BigInt(buffer[index + 5]) << 16n | BigInt(buffer[index + 6]) << 8n | BigInt(buffer[index + 7]);
  }
  function calculateMemoryOffsetIntoNodeSpace(node) {
    const cfg = node.parameters.addressSpaceConfigMemory;
    let offsetPerNode = cfg.highestAddress;
    if (cfg.lowAddressValid) {
      offsetPerNode = cfg.highestAddress - cfg.lowAddress;
    }
    return offsetPerNode * node.index;
  }
  function isEventIdInConsumerRanges(node, eventId) {
    for (let i = 0; i < node.consumers.rangeCount; i++) {
      const r = node.consumers.rangeList[i];
      const startEvent = r.startBase;
      const endEvent = r.startBase + BigInt(r.eventCount);
      if (eventId >= startEvent && eventId <= endEvent) return true;
    }
    return false;
  }
  function isEventIdInProducerRanges(node, eventId) {
    for (let i = 0; i < node.producers.rangeCount; i++) {
      const r = node.producers.rangeList[i];
      const startEvent = r.startBase;
      const endEvent = r.startBase + BigInt(r.eventCount);
      if (eventId >= startEvent && eventId <= endEvent) return true;
    }
    return false;
  }
  function generateEventRangeId(baseEventId, count) {
    let bitsNeeded = 0n;
    let temp = BigInt(count) - 1n;
    while (temp > 0n) {
      bitsNeeded++;
      temp >>= 1n;
    }
    const mask = (1n << bitsNeeded) - 1n;
    return baseEventId & ~mask | mask;
  }
  function loadConfigMemReplyWriteOkHeader(sm, req) {
    sm.outgoing.msg.payloadCount = 0;
    loadOpenlcbMessage(
      sm.outgoing.msg,
      sm.node.alias,
      sm.node.id,
      sm.incoming.msg.sourceAlias,
      sm.incoming.msg.sourceId,
      MTI_DATAGRAM
    );
    copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_CONFIGURATION, 0);
    copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[1] + CONFIG_MEM_REPLY_OK_OFFSET, 1);
    copyDwordToPayload(sm.outgoing.msg, req.address, 2);
    if (req.encoding === ADDRESS_SPACE_ENCODING.IN_BYTE_6) {
      copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[6], 6);
    }
    sm.outgoing.valid = false;
  }
  function loadConfigMemReplyWriteFailHeader(sm, req, errorCode) {
    sm.outgoing.msg.payloadCount = 0;
    loadOpenlcbMessage(
      sm.outgoing.msg,
      sm.node.alias,
      sm.node.id,
      sm.incoming.msg.sourceAlias,
      sm.incoming.msg.sourceId,
      MTI_DATAGRAM
    );
    copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_CONFIGURATION, 0);
    copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[1] + CONFIG_MEM_REPLY_FAIL_OFFSET, 1);
    copyDwordToPayload(sm.outgoing.msg, req.address, 2);
    if (req.encoding === ADDRESS_SPACE_ENCODING.IN_BYTE_6) {
      copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[6], 6);
      copyWordToPayload(sm.outgoing.msg, errorCode, 7);
    } else {
      copyWordToPayload(sm.outgoing.msg, errorCode, 6);
    }
    sm.outgoing.valid = false;
  }
  function loadConfigMemReplyReadOkHeader(sm, req) {
    sm.outgoing.msg.payloadCount = 0;
    loadOpenlcbMessage(
      sm.outgoing.msg,
      sm.node.alias,
      sm.node.id,
      sm.incoming.msg.sourceAlias,
      sm.incoming.msg.sourceId,
      MTI_DATAGRAM
    );
    copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_CONFIGURATION, 0);
    copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[1] + CONFIG_MEM_REPLY_OK_OFFSET, 1);
    copyDwordToPayload(sm.outgoing.msg, req.address, 2);
    if (req.encoding === ADDRESS_SPACE_ENCODING.IN_BYTE_6) {
      copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[6], 6);
    }
    sm.outgoing.valid = false;
  }
  function loadConfigMemReplyReadFailHeader(sm, req, errorCode) {
    sm.outgoing.msg.payloadCount = 0;
    loadOpenlcbMessage(
      sm.outgoing.msg,
      sm.node.alias,
      sm.node.id,
      sm.incoming.msg.sourceAlias,
      sm.incoming.msg.sourceId,
      MTI_DATAGRAM
    );
    copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_CONFIGURATION, 0);
    copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[1] + CONFIG_MEM_REPLY_FAIL_OFFSET, 1);
    copyDwordToPayload(sm.outgoing.msg, req.address, 2);
    if (req.encoding === ADDRESS_SPACE_ENCODING.IN_BYTE_6) {
      copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[6], 6);
    }
    copyWordToPayload(sm.outgoing.msg, errorCode, req.dataStart);
  }

  // src/drivers/can/utilities.js
  function clearCanMessage(canMsg) {
    canMsg.identifier = 0;
    canMsg.payloadCount = 0;
    canMsg.payload.fill(0);
  }
  function loadCanMessage(canMsg, identifier, payloadSize, bytes = []) {
    canMsg.identifier = identifier >>> 0;
    canMsg.payloadCount = payloadSize;
    for (let i = 0; i < LEN_CAN_BYTE_ARRAY; i++) {
      canMsg.payload[i] = (i < bytes.length ? bytes[i] : 0) & 255;
    }
  }
  function copyNodeIdToCanPayload(canMsg, nodeId, startOffset) {
    if (startOffset > 2) return 0;
    canMsg.payloadCount = 6 + startOffset;
    let v = BigInt.asUintN(48, nodeId);
    for (let i = startOffset + 5; i >= startOffset; i--) {
      canMsg.payload[i] = Number(v & 0xFFn);
      v >>= 8n;
    }
    return canMsg.payloadCount;
  }
  function copyOpenlcbPayloadToCanPayload(openlcbMsg, canMsg, openlcbStartIndex, canStartIndex) {
    canMsg.payloadCount = 0;
    let count = 0;
    if (openlcbMsg.payloadCount === 0) return 0;
    let src = openlcbStartIndex;
    for (let i = canStartIndex; i < LEN_CAN_BYTE_ARRAY; i++) {
      canMsg.payload[i] = openlcbMsg.payload[src++];
      count++;
      if (src >= openlcbMsg.payloadCount) break;
    }
    canMsg.payloadCount = canStartIndex + count;
    return count;
  }
  function appendCanPayloadToOpenlcbPayload(openlcbMsg, canMsg, canStartIndex) {
    let result = 0;
    const bufferLen = payloadTypeToLen(openlcbMsg.payloadType);
    for (let i = canStartIndex; i < canMsg.payloadCount; i++) {
      if (openlcbMsg.payloadCount < bufferLen) {
        openlcbMsg.payload[openlcbMsg.payloadCount++] = canMsg.payload[i];
        result++;
      } else {
        break;
      }
    }
    return result;
  }
  function copy64BitToCanMessage(canMsg, data) {
    let v = BigInt.asUintN(64, data);
    for (let i = 7; i >= 0; i--) {
      canMsg.payload[i] = Number(v & 0xFFn);
      v >>= 8n;
    }
    canMsg.payloadCount = 8;
    return 8;
  }
  function copyCanMessage(source, target) {
    target.identifier = source.identifier;
    for (let i = 0; i < source.payloadCount; i++) {
      target.payload[i] = source.payload[i];
    }
    target.payloadCount = source.payloadCount;
    return source.payloadCount;
  }
  function extractCanPayloadAsNodeId(canMsg) {
    const p = canMsg.payload;
    return BigInt(p[0]) << 40n | BigInt(p[1]) << 32n | BigInt(p[2]) << 24n | BigInt(p[3]) << 16n | BigInt(p[4]) << 8n | BigInt(p[5]);
  }
  function extractSourceAliasFromCanIdentifier(canMsg) {
    return canMsg.identifier & 4095;
  }
  function extractDestAliasFromCanMessage(canMsg) {
    switch (canMsg.identifier & MASK_CAN_FRAME_TYPE) {
      case OPENLCB_MESSAGE_STANDARD_FRAME_TYPE:
        if (canMsg.identifier & MASK_CAN_DEST_ADDRESS_PRESENT) {
          return (canMsg.payload[0] & 15) << 8 | canMsg.payload[1];
        }
        return 0;
      case CAN_FRAME_TYPE_STREAM:
      case CAN_FRAME_TYPE_DATAGRAM_ONLY:
      case CAN_FRAME_TYPE_DATAGRAM_FIRST:
      case CAN_FRAME_TYPE_DATAGRAM_MIDDLE:
      case CAN_FRAME_TYPE_DATAGRAM_FINAL:
        return canMsg.identifier >>> 12 & 4095;
      default:
        return 0;
    }
  }
  function convertCanMtiToOpenlcbMti(canMsg) {
    switch (canMsg.identifier & MASK_CAN_FRAME_TYPE) {
      case OPENLCB_MESSAGE_STANDARD_FRAME_TYPE: {
        let mti = canMsg.identifier >>> 12 & 4095;
        if (mti === CAN_MTI_PCER_WITH_PAYLOAD_FIRST || mti === CAN_MTI_PCER_WITH_PAYLOAD_MIDDLE || mti === CAN_MTI_PCER_WITH_PAYLOAD_LAST) {
          mti = MTI_PC_EVENT_REPORT_WITH_PAYLOAD;
        }
        return mti;
      }
      case CAN_FRAME_TYPE_STREAM:
        return MTI_STREAM_SEND;
      case CAN_FRAME_TYPE_DATAGRAM_ONLY:
      case CAN_FRAME_TYPE_DATAGRAM_FIRST:
      case CAN_FRAME_TYPE_DATAGRAM_MIDDLE:
      case CAN_FRAME_TYPE_DATAGRAM_FINAL:
        return MTI_DATAGRAM;
      default:
        return 0;
    }
  }
  function isOpenlcbMessage(canMsg) {
    return (canMsg.identifier & CAN_OPENLCB_MSG) === CAN_OPENLCB_MSG;
  }
  function countNullsInCanPayload(canMsg) {
    let count = 0;
    for (let i = 0; i < canMsg.payloadCount; i++) {
      if (canMsg.payload[i] === 0) count++;
    }
    return count;
  }
  function countNullsInOpenlcbPayload(openlcbMsg) {
    let count = 0;
    for (let i = 0; i < openlcbMsg.payloadCount; i++) {
      if (openlcbMsg.payload[i] === 0) count++;
    }
    return count;
  }
  function countNullsInPayloads(openlcbMsg, canMsg) {
    return countNullsInCanPayload(canMsg) + countNullsInOpenlcbPayload(openlcbMsg);
  }

  // src/drivers/can/login-message-handler.js
  function generateSeed(startSeed) {
    const MASK24 = 0xFFFFFFn;
    const MASK8 = 0xFFn;
    let lfsr2 = startSeed & MASK24;
    let lfsr1 = startSeed >> 24n & MASK24;
    const temp1 = (lfsr1 << 9n | lfsr2 >> 15n & 0x1FFn) & MASK24;
    const temp2 = lfsr2 << 9n & MASK24;
    lfsr1 = lfsr1 + temp1 + 0x1B0CA3n;
    lfsr2 = lfsr2 + temp2 + 0x7A4BA9n;
    lfsr1 = (lfsr1 & MASK24) + ((lfsr2 & MASK8 << 24n) >> 24n);
    lfsr2 = lfsr2 & MASK24;
    return lfsr1 << 24n | lfsr2;
  }
  function generateAlias(seed) {
    const lfsr2 = Number(seed & 0xFFFFFFn);
    const lfsr1 = Number(seed >> 24n & 0xFFFFFFn);
    return (lfsr1 ^ lfsr2 ^ lfsr1 >>> 12 ^ lfsr2 >>> 12) & 4095;
  }
  var CanLoginMessageHandler = class {
    /**
     * @param {Object} deps
     * @param {AliasMappings} deps.aliasMappings required
     * @param {(alias: number, nodeId: bigint) => void} [deps.onAliasChange] optional
     */
    constructor(deps) {
      this._aliasMappings = deps.aliasMappings;
      this._onAliasChange = deps.onAliasChange ?? null;
    }
    /** State 1: seed ← Node ID, skip straight to GENERATE_ALIAS. */
    stateInit(stateInfo) {
      const node = stateInfo.node;
      node.seed = node.id;
      node.state.runState = RUNSTATE_GENERATE_ALIAS;
    }
    /** State 2: advance LFSR (entered on alias conflict retry only). */
    stateGenerateSeed(stateInfo) {
      const node = stateInfo.node;
      node.seed = generateSeed(node.seed);
      node.state.runState = RUNSTATE_GENERATE_ALIAS;
    }
    /**
     * State 3: derive 12-bit alias, reject 0 and collisions with siblings,
     * register mapping, notify, advance.
     */
    stateGenerateAlias(stateInfo) {
      const node = stateInfo.node;
      node.alias = generateAlias(node.seed);
      while (node.alias === 0 || this._aliasMappings.findMappingByAlias(node.alias) !== null) {
        node.seed = generateSeed(node.seed);
        node.alias = generateAlias(node.seed);
      }
      this._aliasMappings.register(node.alias, node.id);
      if (this._onAliasChange) this._onAliasChange(node.alias, node.id);
      node.state.runState = RUNSTATE_LOAD_CHECK_ID_07;
    }
    /** State 4: CID7 frame carrying Node ID bits 47-36. */
    stateLoadCid07(stateInfo) {
      const node = stateInfo.node;
      const idHigh = Number(node.id >> 24n & 0xFFFFFFn);
      stateInfo.loginOutgoingCanMsg.payloadCount = 0;
      stateInfo.loginOutgoingCanMsg.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_CID7 | idHigh & 16773120 | node.alias) >>> 0;
      stateInfo.loginOutgoingValid = true;
      node.state.runState = RUNSTATE_LOAD_CHECK_ID_06;
    }
    /** State 5: CID6 frame carrying Node ID bits 35-24. */
    stateLoadCid06(stateInfo) {
      const node = stateInfo.node;
      const idMid = Number(node.id >> 12n & 0xFFFFFFn);
      stateInfo.loginOutgoingCanMsg.payloadCount = 0;
      stateInfo.loginOutgoingCanMsg.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_CID6 | idMid & 16773120 | node.alias) >>> 0;
      stateInfo.loginOutgoingValid = true;
      node.state.runState = RUNSTATE_LOAD_CHECK_ID_05;
    }
    /** State 6: CID5 frame carrying Node ID bits 23-12. */
    stateLoadCid05(stateInfo) {
      const node = stateInfo.node;
      const idLow = Number(node.id & 0xFFFFFFn);
      stateInfo.loginOutgoingCanMsg.payloadCount = 0;
      stateInfo.loginOutgoingCanMsg.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_CID5 | idLow & 16773120 | node.alias) >>> 0;
      stateInfo.loginOutgoingValid = true;
      node.state.runState = RUNSTATE_LOAD_CHECK_ID_04;
    }
    /** State 7: CID4 frame carrying Node ID bits 11-0. Snapshot tick for wait. */
    stateLoadCid04(stateInfo) {
      const node = stateInfo.node;
      const shifted = Number(node.id << 12n & 0xFFFFFFn);
      stateInfo.loginOutgoingCanMsg.payloadCount = 0;
      stateInfo.loginOutgoingCanMsg.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_CID4 | shifted & 16773120 | node.alias) >>> 0;
      node.timerticks = stateInfo.currentTick & 255;
      stateInfo.loginOutgoingValid = true;
      node.state.runState = RUNSTATE_WAIT_200ms;
    }
    /** State 8: wait until >200ms elapsed (C uses >2 ticks = ≥300ms real-world). */
    stateWait200ms(stateInfo) {
      const node = stateInfo.node;
      const elapsed = stateInfo.currentTick - node.timerticks & 255;
      if (elapsed > 2) {
        node.state.runState = RUNSTATE_LOAD_RESERVE_ID;
      }
    }
    /** State 9: RID frame. */
    stateLoadRid(stateInfo) {
      const node = stateInfo.node;
      stateInfo.loginOutgoingCanMsg.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_RID | node.alias) >>> 0;
      stateInfo.loginOutgoingCanMsg.payloadCount = 0;
      stateInfo.loginOutgoingValid = true;
      node.state.runState = RUNSTATE_LOAD_ALIAS_MAP_DEFINITION;
    }
    /** State 10: AMD frame + mark node permitted. Final state of CAN login. */
    stateLoadAmd(stateInfo) {
      const node = stateInfo.node;
      stateInfo.loginOutgoingCanMsg.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_AMD | node.alias) >>> 0;
      copyNodeIdToCanPayload(stateInfo.loginOutgoingCanMsg, node.id, 0);
      stateInfo.loginOutgoingValid = true;
      node.state.permitted = true;
      const mapping = this._aliasMappings.findMappingByAlias(node.alias);
      if (mapping) mapping.isPermitted = true;
      node.state.runState = RUNSTATE_LOAD_INITIALIZATION_COMPLETE;
    }
  };

  // src/drivers/can/login-statemachine.js
  var CanLoginStatemachine = class {
    /**
     * @param {CanLoginMessageHandler} handler required
     */
    constructor(handler) {
      this._handler = handler;
    }
    /** Run exactly one state for the node in `stateInfo`. Non-blocking. */
    run(stateInfo) {
      switch (stateInfo.node.state.runState) {
        case RUNSTATE_INIT:
          return this._handler.stateInit(stateInfo);
        case RUNSTATE_GENERATE_SEED:
          return this._handler.stateGenerateSeed(stateInfo);
        case RUNSTATE_GENERATE_ALIAS:
          return this._handler.stateGenerateAlias(stateInfo);
        case RUNSTATE_LOAD_CHECK_ID_07:
          return this._handler.stateLoadCid07(stateInfo);
        case RUNSTATE_LOAD_CHECK_ID_06:
          return this._handler.stateLoadCid06(stateInfo);
        case RUNSTATE_LOAD_CHECK_ID_05:
          return this._handler.stateLoadCid05(stateInfo);
        case RUNSTATE_LOAD_CHECK_ID_04:
          return this._handler.stateLoadCid04(stateInfo);
        case RUNSTATE_WAIT_200ms:
          return this._handler.stateWait200ms(stateInfo);
        case RUNSTATE_LOAD_RESERVE_ID:
          return this._handler.stateLoadRid(stateInfo);
        case RUNSTATE_LOAD_ALIAS_MAP_DEFINITION:
          return this._handler.stateLoadAmd(stateInfo);
        default:
          return;
      }
    }
  };

  // src/drivers/can/rx-message-handler.js
  var CAN_RX_INPROCESS_TIMEOUT_TICKS = 30;
  function assemblyKey(sourceAlias, destAlias, mti) {
    return sourceAlias * 268435456 + destAlias * 65536 + mti;
  }
  var CanRxMessageHandler = class {
    /**
     * @param {Object} deps
     * @param {AliasMappings}   deps.aliasMappings        required
     * @param {CanBufferFifo}   deps.canTxFifo            required — where AMR/AMD/RID control replies go
     * @param {MessageFifo}     deps.openlcbFifo          required — where completed OpenLCB messages go
     * @param {() => number}    deps.getCurrentTick       required — current 100ms tick (0-255)
     * @param {AliasMappingListener} [deps.listener]      optional — listener alias table
     */
    constructor(deps) {
      this._aliasMappings = deps.aliasMappings;
      this._canTxFifo = deps.canTxFifo;
      this._openlcbFifo = deps.openlcbFifo;
      this._getCurrentTick = deps.getCurrentTick;
      this._listener = deps.listener ?? null;
      this._inProcess = /* @__PURE__ */ new Map();
    }
    // -------------------------------------------------------------------------
    // Internal helpers
    // -------------------------------------------------------------------------
    _pushReject(sourceAlias, destAlias, originalMti, errorCode) {
      const reply = createMessage({ payloadType: PAYLOAD_TYPE.BASIC });
      const mti = originalMti === MTI_DATAGRAM ? MTI_DATAGRAM_REJECTED_REPLY : MTI_OPTIONAL_INTERACTION_REJECTED;
      loadOpenlcbMessage(reply, sourceAlias, 0n, destAlias, 0n, mti);
      copyWordToPayload(reply, destAlias, 0);
      copyWordToPayload(reply, errorCode, 2);
      this._openlcbFifo.push(reply);
    }
    /** Returns true if we saw the remote source alias duplicate one of ours. */
    _checkForDuplicateAlias(canMsg) {
      const sourceAlias = extractSourceAliasFromCanIdentifier(canMsg);
      const mapping = this._aliasMappings.findMappingByAlias(sourceAlias);
      if (!mapping) return false;
      mapping.isDuplicate = true;
      this._aliasMappings.setHasDuplicateAliasFlag();
      if (mapping.isPermitted) {
        const out = createCanMsg();
        out.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_AMR | sourceAlias) >>> 0;
        copyNodeIdToCanPayload(out, mapping.nodeId, 0);
        this._canTxFifo.push(out);
      }
      return true;
    }
    // -------------------------------------------------------------------------
    // Multi-frame OpenLCB messages
    // -------------------------------------------------------------------------
    firstFrame(canMsg, offset, payloadType) {
      const destAlias = extractDestAliasFromCanMessage(canMsg);
      const sourceAlias = extractSourceAliasFromCanIdentifier(canMsg);
      const mti = convertCanMtiToOpenlcbMti(canMsg);
      const key = assemblyKey(sourceAlias, destAlias, mti);
      if (this._inProcess.has(key)) {
        this._pushReject(destAlias, sourceAlias, mti, ERROR_TEMPORARY_OUT_OF_ORDER_START_BEFORE_LAST_END);
        return;
      }
      const msg = createMessage({ payloadType });
      if (!msg) {
        this._pushReject(destAlias, sourceAlias, mti, ERROR_TEMPORARY_BUFFER_UNAVAILABLE);
        return;
      }
      loadOpenlcbMessage(msg, sourceAlias, 0n, destAlias, 0n, mti);
      msg.timer.assemblyTicks = this._getCurrentTick();
      msg.state.inprocess = true;
      appendCanPayloadToOpenlcbPayload(msg, canMsg, offset);
      this._inProcess.set(key, msg);
    }
    middleFrame(canMsg, offset) {
      const destAlias = extractDestAliasFromCanMessage(canMsg);
      const sourceAlias = extractSourceAliasFromCanIdentifier(canMsg);
      const mti = convertCanMtiToOpenlcbMti(canMsg);
      const key = assemblyKey(sourceAlias, destAlias, mti);
      const msg = this._inProcess.get(key);
      if (!msg) {
        this._pushReject(destAlias, sourceAlias, mti, ERROR_TEMPORARY_OUT_OF_ORDER_MIDDLE_END_WITH_NO_START);
        return;
      }
      const elapsed = this._getCurrentTick() - msg.timer.assemblyTicks & 255;
      if (elapsed >= CAN_RX_INPROCESS_TIMEOUT_TICKS) {
        this._inProcess.delete(key);
        this._pushReject(destAlias, sourceAlias, mti, ERROR_TEMPORARY_OUT_OF_ORDER_MIDDLE_END_WITH_NO_START);
        return;
      }
      appendCanPayloadToOpenlcbPayload(msg, canMsg, offset);
    }
    lastFrame(canMsg, offset) {
      const destAlias = extractDestAliasFromCanMessage(canMsg);
      const sourceAlias = extractSourceAliasFromCanIdentifier(canMsg);
      const mti = convertCanMtiToOpenlcbMti(canMsg);
      const key = assemblyKey(sourceAlias, destAlias, mti);
      const msg = this._inProcess.get(key);
      if (!msg) {
        this._pushReject(destAlias, sourceAlias, mti, ERROR_TEMPORARY_OUT_OF_ORDER_MIDDLE_END_WITH_NO_START);
        return;
      }
      appendCanPayloadToOpenlcbPayload(msg, canMsg, offset);
      msg.state.inprocess = false;
      this._inProcess.delete(key);
      this._openlcbFifo.push(msg);
    }
    singleFrame(canMsg, offset, payloadType) {
      const destAlias = extractDestAliasFromCanMessage(canMsg);
      const sourceAlias = extractSourceAliasFromCanIdentifier(canMsg);
      const mti = convertCanMtiToOpenlcbMti(canMsg);
      const msg = createMessage({ payloadType });
      loadOpenlcbMessage(msg, sourceAlias, 0n, destAlias, 0n, mti);
      appendCanPayloadToOpenlcbPayload(msg, canMsg, offset);
      this._openlcbFifo.push(msg);
    }
    streamFrame(canMsg, offset, payloadType) {
      const destAlias = extractDestAliasFromCanMessage(canMsg);
      const sourceAlias = extractSourceAliasFromCanIdentifier(canMsg);
      const mti = convertCanMtiToOpenlcbMti(canMsg);
      const msg = createMessage({ payloadType });
      loadOpenlcbMessage(msg, sourceAlias, 0n, destAlias, 0n, mti);
      appendCanPayloadToOpenlcbPayload(msg, canMsg, 0);
      this._openlcbFifo.push(msg);
    }
    /**
     * Legacy SNIP reassembly — messages arrive without multi-frame framing
     * bits, so completion is detected by counting NULL terminators.
     */
    canLegacySnip(canMsg, offset, payloadType) {
      const destAlias = extractDestAliasFromCanMessage(canMsg);
      const sourceAlias = extractSourceAliasFromCanIdentifier(canMsg);
      const mti = convertCanMtiToOpenlcbMti(canMsg);
      const key = assemblyKey(sourceAlias, destAlias, mti);
      const msg = this._inProcess.get(key);
      if (!msg) {
        this.firstFrame(canMsg, offset, payloadType);
        return;
      }
      if (countNullsInPayloads(msg, canMsg) < 6) {
        this.middleFrame(canMsg, offset);
      } else {
        this.lastFrame(canMsg, offset);
      }
    }
    // -------------------------------------------------------------------------
    // CAN control frames
    // -------------------------------------------------------------------------
    cidFrame(canMsg) {
      if (!canMsg) return;
      const sourceAlias = extractSourceAliasFromCanIdentifier(canMsg);
      const mapping = this._aliasMappings.findMappingByAlias(sourceAlias);
      if (!mapping) return;
      const reply = createCanMsg();
      reply.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_RID | sourceAlias) >>> 0;
      reply.payloadCount = 0;
      this._canTxFifo.push(reply);
    }
    ridFrame(canMsg) {
      this._checkForDuplicateAlias(canMsg);
    }
    /**
     * AMD: another node is mapping Node ID → alias. Two effects:
     *   1. Duplicate-alias detection against our own table.
     *   2. If the listener table is linked in, store the resolved alias and
     *      release any Train Listener Attach messages that were held waiting
     *      for this Node ID's alias.
     */
    amdFrame(canMsg) {
      this._checkForDuplicateAlias(canMsg);
      if (!this._listener) return;
      const nodeId = extractCanPayloadAsNodeId(canMsg);
      const alias = extractSourceAliasFromCanIdentifier(canMsg);
      this._listener.setAlias(nodeId, alias);
      this._releaseHeldMessagesForListener(nodeId);
    }
    _releaseHeldMessagesForListener(listenerId) {
      for (const [key, msg] of this._inProcess) {
        if (!msg.state.inprocess) continue;
        if (msg.mti !== MTI_TRAIN_PROTOCOL) continue;
        const instruction = extractByteFromPayload(msg, 0);
        const subCommand = extractByteFromPayload(msg, 1);
        if (instruction !== TRAIN_LISTENER_CONFIG) continue;
        if (subCommand !== TRAIN_LISTENER_ATTACH) continue;
        const heldListenerId = extractNodeIdFromPayload(msg, 3);
        if (heldListenerId !== listenerId) continue;
        msg.state.inprocess = false;
        this._inProcess.delete(key);
        this._openlcbFifo.push(msg);
      }
    }
    /**
     * AME: query for aliases. Non-empty payload = targeted query for a
     * specific Node ID; empty payload = global query.
     */
    ameFrame(canMsg) {
      if (this._checkForDuplicateAlias(canMsg)) return;
      if (canMsg.payloadCount > 0) {
        const mapping = this._aliasMappings.findMappingByNodeId(extractCanPayloadAsNodeId(canMsg));
        if (mapping && mapping.isPermitted) {
          const out = createCanMsg();
          out.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_AMD | mapping.alias) >>> 0;
          copyNodeIdToCanPayload(out, mapping.nodeId, 0);
          this._canTxFifo.push(out);
        }
        return;
      }
      if (this._listener) this._listener.flushAliases();
      for (const entry of this._aliasMappings.getList()) {
        if (entry.alias !== 0 && entry.isPermitted) {
          if (this._listener) this._listener.setAlias(entry.nodeId, entry.alias);
          const out = createCanMsg();
          out.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_AMD | entry.alias) >>> 0;
          copyNodeIdToCanPayload(out, entry.nodeId, 0);
          this._canTxFifo.push(out);
        }
      }
    }
    /**
     * AMR: remote node releases its alias. Drop all in-progress assemblies
     * from that source, mark queued OpenLCB messages invalid so they're
     * discarded on pop, and clear the listener entry.
     */
    amrFrame(canMsg) {
      this._checkForDuplicateAlias(canMsg);
      const alias = extractSourceAliasFromCanIdentifier(canMsg);
      for (const [key, msg] of this._inProcess) {
        if (msg.sourceAlias === alias) this._inProcess.delete(key);
      }
      if (typeof this._openlcbFifo.checkAndInvalidateMessagesBySourceAlias === "function") {
        this._openlcbFifo.checkAndInvalidateMessagesBySourceAlias(alias);
      }
      if (this._listener) this._listener.clearAliasByAlias(alias);
    }
    errorInfoReportFrame(canMsg) {
      this._checkForDuplicateAlias(canMsg);
    }
  };

  // src/drivers/can/tx-message-handler.js
  var DATAGRAM_ONLY = (RESERVED_TOP_BIT | CAN_OPENLCB_MSG | CAN_FRAME_TYPE_DATAGRAM_ONLY) >>> 0;
  var DATAGRAM_FIRST = (RESERVED_TOP_BIT | CAN_OPENLCB_MSG | CAN_FRAME_TYPE_DATAGRAM_FIRST) >>> 0;
  var DATAGRAM_MIDDLE = (RESERVED_TOP_BIT | CAN_OPENLCB_MSG | CAN_FRAME_TYPE_DATAGRAM_MIDDLE) >>> 0;
  var DATAGRAM_LAST = (RESERVED_TOP_BIT | CAN_OPENLCB_MSG | CAN_FRAME_TYPE_DATAGRAM_FINAL) >>> 0;
  var STANDARD_FRAME = (RESERVED_TOP_BIT | CAN_OPENLCB_MSG | OPENLCB_MESSAGE_STANDARD_FRAME_TYPE) >>> 0;
  var STREAM_FRAME_ID = (RESERVED_TOP_BIT | CAN_OPENLCB_MSG | CAN_FRAME_TYPE_STREAM) >>> 0;
  function buildDatagramIdentifier(base, msg) {
    return (base | (msg.destAlias & 4095) << 12 | msg.sourceAlias & 4095) >>> 0;
  }
  function buildStandardIdentifier(msg) {
    return (STANDARD_FRAME | (msg.mti & 4095) << 12 | msg.sourceAlias & 4095) >>> 0;
  }
  var CanTxMessageHandler = class {
    /**
     * @param {Object} deps
     * @param {(canMsg: Object) => boolean} deps.transmit     required — hardware TX
     * @param {(canMsg: Object) => void}   [deps.onTransmit]  optional — post-TX hook
     */
    constructor(deps) {
      this._transmit = deps.transmit;
      this._onTransmit = deps.onTransmit ?? null;
    }
    _send(canMsg) {
      const ok = this._transmit(canMsg);
      if (ok && this._onTransmit) this._onTransmit(canMsg);
      return ok;
    }
    // -------------------------------------------------------------------------
    // Datagram — up to 72 bytes, automatic ONLY/FIRST/MIDDLE/LAST selection
    // -------------------------------------------------------------------------
    datagramFrame(openlcbMsg, canMsgWorker, indexRef) {
      const startIndex = indexRef.value;
      const len = copyOpenlcbPayloadToCanPayload(
        openlcbMsg,
        canMsgWorker,
        startIndex,
        OFFSET_CAN_WITHOUT_DEST_ADDRESS
      );
      let baseId;
      if (openlcbMsg.payloadCount <= LEN_CAN_BYTE_ARRAY) {
        baseId = DATAGRAM_ONLY;
      } else if (startIndex < LEN_CAN_BYTE_ARRAY) {
        baseId = DATAGRAM_FIRST;
      } else if (startIndex + len < openlcbMsg.payloadCount) {
        baseId = DATAGRAM_MIDDLE;
      } else {
        baseId = DATAGRAM_LAST;
      }
      canMsgWorker.identifier = buildDatagramIdentifier(baseId, openlcbMsg);
      const result = this._send(canMsgWorker);
      if (result) indexRef.value = startIndex + len;
      return result;
    }
    // -------------------------------------------------------------------------
    // Global (unaddressed) OpenLCB message — single frame only
    // -------------------------------------------------------------------------
    unaddressedMsgFrame(openlcbMsg, canMsgWorker, indexRef) {
      if (openlcbMsg.payloadCount > LEN_CAN_BYTE_ARRAY) {
        throw new Error("unaddressedMsgFrame: payload > 8 bytes (use PCER-with-payload path)");
      }
      const startIndex = indexRef.value;
      const len = copyOpenlcbPayloadToCanPayload(
        openlcbMsg,
        canMsgWorker,
        startIndex,
        OFFSET_CAN_WITHOUT_DEST_ADDRESS
      );
      canMsgWorker.identifier = buildStandardIdentifier(openlcbMsg);
      const result = this._send(canMsgWorker);
      if (result) indexRef.value = startIndex + len;
      return result;
    }
    // -------------------------------------------------------------------------
    // Addressed OpenLCB message — dest alias in payload[0..1], ONLY/FIRST/...
    // -------------------------------------------------------------------------
    addressedMsgFrame(openlcbMsg, canMsgWorker, indexRef) {
      canMsgWorker.payload[0] = openlcbMsg.destAlias >>> 8 & 255;
      canMsgWorker.payload[1] = openlcbMsg.destAlias & 255;
      canMsgWorker.identifier = buildStandardIdentifier(openlcbMsg);
      const startIndex = indexRef.value;
      const len = copyOpenlcbPayloadToCanPayload(
        openlcbMsg,
        canMsgWorker,
        startIndex,
        OFFSET_CAN_WITH_DEST_ADDRESS
      );
      let flag;
      if (openlcbMsg.payloadCount <= 6) {
        flag = MULTIFRAME_ONLY;
      } else if (startIndex < 6) {
        flag = MULTIFRAME_FIRST;
      } else if (startIndex + len < openlcbMsg.payloadCount) {
        flag = MULTIFRAME_MIDDLE;
      } else {
        flag = MULTIFRAME_FINAL;
      }
      setMultiFrameFlag(canMsgWorker.payload, 0, flag);
      const result = this._send(canMsgWorker);
      if (result) indexRef.value = startIndex + len;
      return result;
    }
    // -------------------------------------------------------------------------
    // Stream Data Send — DID (payload[0]) in CAN byte 0, data in bytes 1-7
    // -------------------------------------------------------------------------
    streamFrame(openlcbMsg, canMsgWorker, indexRef) {
      canMsgWorker.identifier = (STREAM_FRAME_ID | (openlcbMsg.destAlias & 4095) << 12 | openlcbMsg.sourceAlias & 4095) >>> 0;
      canMsgWorker.payload[0] = openlcbMsg.payload[0];
      const startIndex = indexRef.value;
      let src = startIndex === 0 ? 1 : startIndex;
      let count = 0;
      for (let i = 1; i < LEN_CAN_BYTE_ARRAY; i++) {
        if (src >= openlcbMsg.payloadCount) break;
        canMsgWorker.payload[i] = openlcbMsg.payload[src++];
        count++;
      }
      canMsgWorker.payloadCount = 1 + count;
      const result = this._send(canMsgWorker);
      if (result) {
        indexRef.value = startIndex === 0 ? 1 + count : startIndex + count;
      }
      return result;
    }
    // -------------------------------------------------------------------------
    // Raw CAN frame (CID/RID/AMD during login, already fully constructed)
    // -------------------------------------------------------------------------
    canFrame(canMsg) {
      return this._send(canMsg);
    }
  };

  // src/drivers/can/main-statemachine.js
  var CanMainStatemachine = class {
    /**
     * @param {Object} deps
     * @param {NodePool}             deps.nodePool            required
     * @param {AliasMappings}        deps.aliasMappings       required
     * @param {CanBufferFifo}        deps.canTxFifo           required — outgoing CAN frames
     * @param {CanLoginStatemachine} deps.loginStatemachine   required — per-node login driver
     * @param {(canMsg) => boolean}  deps.sendCanMessage      required — hardware TX (wraps TX handler)
     * @param {() => number}         deps.getCurrentTick      required — global 100ms tick
     * @param {AliasMappingListener} [deps.listener]          optional — listener alias table
     */
    constructor(deps) {
      this._nodePool = deps.nodePool;
      this._aliasMappings = deps.aliasMappings;
      this._canTxFifo = deps.canTxFifo;
      this._loginStatemachine = deps.loginStatemachine;
      this._sendCanMessage = deps.sendCanMessage;
      this._getCurrentTick = deps.getCurrentTick;
      this._listener = deps.listener ?? null;
      this._loginCanMsg = createCanMsg();
      this._stateInfo = {
        node: null,
        loginOutgoingCanMsg: this._loginCanMsg,
        loginOutgoingValid: false,
        outgoingCanMsg: null,
        // retained on TX failure for retry
        currentTick: 0
      };
    }
    /** Access the internal state info (testing/debug). Do not mutate. */
    getStateInfo() {
      return this._stateInfo;
    }
    // -------------------------------------------------------------------------
    // Public: run one cooperative iteration
    // -------------------------------------------------------------------------
    run() {
      this._handleListenerVerification();
      if (this._handleDuplicateAliases()) return;
      if (this._handleOutgoingCanMessage()) return;
      if (this._handleLoginOutgoingCanMessage()) return;
      if (this._handleTryEnumerateFirstNode()) return;
      if (this._handleTryEnumerateNextNode()) return;
    }
    // -------------------------------------------------------------------------
    // Handlers (exposed for unit tests)
    // -------------------------------------------------------------------------
    /** Scan the alias mapping table; reset any node that flagged a duplicate. */
    handleDuplicateAliases() {
      return this._handleDuplicateAliases();
    }
    _handleDuplicateAliases() {
      if (!this._aliasMappings.hasDuplicateAlias) return false;
      let found = false;
      for (const entry of this._aliasMappings.getList()) {
        if (entry.alias > 0 && entry.isDuplicate) {
          const alias = entry.alias;
          this._aliasMappings.unregister(alias);
          this._resetNode(this._nodePool.findByAlias(alias));
          found = true;
        }
      }
      this._aliasMappings.clearHasDuplicateAliasFlag();
      return found;
    }
    handleOutgoingCanMessage() {
      return this._handleOutgoingCanMessage();
    }
    _handleOutgoingCanMessage() {
      if (!this._stateInfo.outgoingCanMsg) {
        this._stateInfo.outgoingCanMsg = this._canTxFifo.pop();
      }
      if (this._stateInfo.outgoingCanMsg) {
        if (this._sendCanMessage(this._stateInfo.outgoingCanMsg)) {
          this._stateInfo.outgoingCanMsg = null;
        }
        return true;
      }
      return false;
    }
    handleLoginOutgoingCanMessage() {
      return this._handleLoginOutgoingCanMessage();
    }
    _handleLoginOutgoingCanMessage() {
      if (this._stateInfo.loginOutgoingValid) {
        if (this._sendCanMessage(this._stateInfo.loginOutgoingCanMsg)) {
          this._stateInfo.loginOutgoingValid = false;
        }
        return true;
      }
      return false;
    }
    handleTryEnumerateFirstNode() {
      return this._handleTryEnumerateFirstNode();
    }
    _handleTryEnumerateFirstNode() {
      if (!this._stateInfo.node) {
        this._stateInfo.node = this._nodePool.getFirst(CAN_STATEMACHINE_NODE_ENUMRATOR_KEY);
        if (!this._stateInfo.node) return true;
        if (this._stateInfo.node.state.runState < RUNSTATE_LOAD_INITIALIZATION_COMPLETE) {
          this._stateInfo.currentTick = this._getCurrentTick();
          this._loginStatemachine.run(this._stateInfo);
        }
        return true;
      }
      return false;
    }
    handleTryEnumerateNextNode() {
      return this._handleTryEnumerateNextNode();
    }
    _handleTryEnumerateNextNode() {
      this._stateInfo.node = this._nodePool.getNext(CAN_STATEMACHINE_NODE_ENUMRATOR_KEY);
      if (!this._stateInfo.node) return true;
      if (this._stateInfo.node.state.runState < RUNSTATE_LOAD_INITIALIZATION_COMPLETE) {
        this._stateInfo.currentTick = this._getCurrentTick();
        this._loginStatemachine.run(this._stateInfo);
      }
      return false;
    }
    handleListenerVerification() {
      return this._handleListenerVerification();
    }
    _handleListenerVerification() {
      if (!this._listener) return false;
      const probeId = this._listener.checkOneVerification(this._getCurrentTick());
      if (probeId === 0n) return false;
      const sourceNode = this._nodePool.getFirst(CAN_STATEMACHINE_NODE_ENUMRATOR_KEY);
      if (!sourceNode || sourceNode.alias === 0) return false;
      const ame = createCanMsg();
      ame.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_AME | sourceNode.alias) >>> 0;
      copyNodeIdToCanPayload(ame, probeId, 0);
      this._canTxFifo.push(ame);
      return true;
    }
    // -------------------------------------------------------------------------
    // Self-originated global AME (§6.2.3)
    // -------------------------------------------------------------------------
    /**
     * Queue a global AME, flushing the listener cache and re-emitting AMDs
     * for local virtual nodes so external nodes learn about them without
     * waiting for self-echoed frames (which the CAN hardware never produces).
     */
    sendGlobalAliasEnquiry() {
      if (this._listener) this._listener.flushAliases();
      for (const entry of this._aliasMappings.getList()) {
        if (entry.alias !== 0 && entry.isPermitted) {
          if (this._listener) this._listener.setAlias(entry.nodeId, entry.alias);
          const amd = createCanMsg();
          amd.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_AMD | entry.alias) >>> 0;
          copyNodeIdToCanPayload(amd, entry.nodeId, 0);
          this._canTxFifo.push(amd);
        }
      }
      const ame = createCanMsg();
      ame.identifier = (RESERVED_TOP_BIT | CAN_CONTROL_FRAME_AME) >>> 0;
      ame.payloadCount = 0;
      this._canTxFifo.push(ame);
    }
    // -------------------------------------------------------------------------
    // Node reset helper (duplicate alias handling)
    // -------------------------------------------------------------------------
    _resetNode(node) {
      if (!node) return;
      node.alias = 0;
      node.state.permitted = false;
      node.state.initialized = false;
      node.state.duplicateIdDetected = false;
      node.state.firmwareUpgradeActive = false;
      node.state.resendDatagram = false;
      node.state.openlcbDatagramAckSent = false;
      node.lastReceivedDatagram = null;
      node.state.runState = RUNSTATE_GENERATE_SEED;
    }
  };

  // src/drivers/can/rx-statemachine.js
  var OFFSET_DEST_ID_IN_PAYLOAD = 2;
  var OFFSET_DEST_ID_IN_IDENTIFIER = 0;
  var OFFSET_NO_DEST_ID = 0;
  function extractCanMti(canMsg) {
    return canMsg.identifier >>> 12 & 4095;
  }
  var CanRxStatemachine = class {
    /**
     * @param {Object} deps
     * @param {CanRxMessageHandler} deps.rxHandler required — handlers for single/first/middle/last/stream/legacy-snip and control frames
     * @param {AliasMappings} deps.aliasMappings   required — used to drop frames addressed to nodes we don't own
     * @param {(canMsg) => void} [deps.onReceive]  optional tap for monitoring/logging
     */
    constructor(deps) {
      this._rx = deps.rxHandler;
      this._aliasMappings = deps.aliasMappings;
      this._onReceive = deps.onReceive ?? null;
    }
    /** Entry point — call with a freshly decoded incoming CAN frame. */
    handleFrame(canMsg) {
      if (this._onReceive) this._onReceive(canMsg);
      if (isOpenlcbMessage(canMsg)) {
        this._handleOpenlcbFrame(canMsg);
      } else {
        this._handleControlFrame(canMsg);
      }
    }
    // -------------------------------------------------------------------------
    // OpenLCB frame dispatch (CAN_OPENLCB_MSG bit set)
    // -------------------------------------------------------------------------
    _handleOpenlcbFrame(canMsg) {
      switch (canMsg.identifier & MASK_CAN_FRAME_TYPE) {
        case OPENLCB_MESSAGE_STANDARD_FRAME_TYPE: {
          if (canMsg.identifier & MASK_CAN_DEST_ADDRESS_PRESENT) {
            const destAlias = extractDestAliasFromCanMessage(canMsg);
            if (!this._aliasMappings.findMappingByAlias(destAlias)) return;
            this._handleStandardAddressed(canMsg, extractCanMti(canMsg));
          } else {
            this._handleStandardGlobal(canMsg, extractCanMti(canMsg));
          }
          return;
        }
        case CAN_FRAME_TYPE_DATAGRAM_ONLY:
          if (!this._addressedToUs(canMsg)) return;
          this._rx.singleFrame(canMsg, OFFSET_DEST_ID_IN_IDENTIFIER, PAYLOAD_TYPE.BASIC);
          return;
        case CAN_FRAME_TYPE_DATAGRAM_FIRST:
          if (!this._addressedToUs(canMsg)) return;
          this._rx.firstFrame(canMsg, OFFSET_DEST_ID_IN_IDENTIFIER, PAYLOAD_TYPE.DATAGRAM);
          return;
        case CAN_FRAME_TYPE_DATAGRAM_MIDDLE:
          if (!this._addressedToUs(canMsg)) return;
          this._rx.middleFrame(canMsg, OFFSET_DEST_ID_IN_IDENTIFIER);
          return;
        case CAN_FRAME_TYPE_DATAGRAM_FINAL:
          if (!this._addressedToUs(canMsg)) return;
          this._rx.lastFrame(canMsg, OFFSET_DEST_ID_IN_IDENTIFIER);
          return;
        case CAN_FRAME_TYPE_STREAM:
          if (!this._addressedToUs(canMsg)) return;
          this._rx.streamFrame(canMsg, OFFSET_DEST_ID_IN_IDENTIFIER, PAYLOAD_TYPE.STREAM);
          return;
        default:
          return;
      }
    }
    _addressedToUs(canMsg) {
      const destAlias = extractDestAliasFromCanMessage(canMsg);
      return this._aliasMappings.findMappingByAlias(destAlias) !== null;
    }
    _handleStandardAddressed(canMsg, canMti) {
      switch (canMsg.payload[0] & MASK_MULTIFRAME_BITS) {
        case MULTIFRAME_ONLY:
          if (canMti === MTI_SIMPLE_NODE_INFO_REPLY) {
            this._rx.canLegacySnip(canMsg, OFFSET_DEST_ID_IN_PAYLOAD, PAYLOAD_TYPE.SNIP);
          } else {
            this._rx.singleFrame(canMsg, OFFSET_DEST_ID_IN_PAYLOAD, PAYLOAD_TYPE.BASIC);
          }
          return;
        case MULTIFRAME_FIRST:
          this._rx.firstFrame(
            canMsg,
            OFFSET_DEST_ID_IN_PAYLOAD,
            canMti === MTI_SIMPLE_NODE_INFO_REPLY ? PAYLOAD_TYPE.SNIP : PAYLOAD_TYPE.BASIC
          );
          return;
        case MULTIFRAME_MIDDLE:
          this._rx.middleFrame(canMsg, OFFSET_DEST_ID_IN_PAYLOAD);
          return;
        case MULTIFRAME_FINAL:
          this._rx.lastFrame(canMsg, OFFSET_DEST_ID_IN_PAYLOAD);
          return;
        default:
          return;
      }
    }
    _handleStandardGlobal(canMsg, canMti) {
      switch (canMti) {
        case CAN_MTI_PCER_WITH_PAYLOAD_FIRST:
          this._rx.firstFrame(canMsg, OFFSET_NO_DEST_ID, PAYLOAD_TYPE.SNIP);
          return;
        case CAN_MTI_PCER_WITH_PAYLOAD_MIDDLE:
          this._rx.middleFrame(canMsg, OFFSET_NO_DEST_ID);
          return;
        case CAN_MTI_PCER_WITH_PAYLOAD_LAST:
          this._rx.lastFrame(canMsg, OFFSET_NO_DEST_ID);
          return;
        default:
          this._rx.singleFrame(canMsg, OFFSET_NO_DEST_ID, PAYLOAD_TYPE.BASIC);
          return;
      }
    }
    // -------------------------------------------------------------------------
    // CAN control frames (CAN_OPENLCB_MSG bit clear)
    // -------------------------------------------------------------------------
    _handleControlFrame(canMsg) {
      if ((canMsg.identifier & MASK_CAN_FRAME_SEQUENCE_NUMBER) === 0) {
        this._handleControlVariableField(canMsg);
      } else {
        this._handleControlCid(canMsg);
      }
    }
    _handleControlVariableField(canMsg) {
      switch (canMsg.identifier & MASK_CAN_VARIABLE_FIELD) {
        case CAN_CONTROL_FRAME_RID:
          this._rx.ridFrame(canMsg);
          return;
        case CAN_CONTROL_FRAME_AMD:
          this._rx.amdFrame(canMsg);
          return;
        case CAN_CONTROL_FRAME_AME:
          this._rx.ameFrame(canMsg);
          return;
        case CAN_CONTROL_FRAME_AMR:
          this._rx.amrFrame(canMsg);
          return;
        case CAN_CONTROL_FRAME_ERROR_INFO_REPORT_0:
        case CAN_CONTROL_FRAME_ERROR_INFO_REPORT_1:
        case CAN_CONTROL_FRAME_ERROR_INFO_REPORT_2:
        case CAN_CONTROL_FRAME_ERROR_INFO_REPORT_3:
          this._rx.errorInfoReportFrame(canMsg);
          return;
        default:
          return;
      }
    }
    _handleControlCid(canMsg) {
      switch (canMsg.identifier & MASK_CAN_FRAME_SEQUENCE_NUMBER) {
        case CAN_CONTROL_FRAME_CID7:
        case CAN_CONTROL_FRAME_CID6:
        case CAN_CONTROL_FRAME_CID5:
        case CAN_CONTROL_FRAME_CID4:
        case CAN_CONTROL_FRAME_CID3:
        case CAN_CONTROL_FRAME_CID2:
        case CAN_CONTROL_FRAME_CID1:
          this._rx.cidFrame(canMsg);
          return;
        default:
          return;
      }
    }
  };

  // src/openlcb/login-statemachine.js
  function loadInitializationComplete(stateInfo) {
    const node = stateInfo.node;
    const mti = node.parameters && node.parameters.protocolSupport & PSI_SIMPLE ? MTI_INITIALIZATION_COMPLETE_SIMPLE : MTI_INITIALIZATION_COMPLETE;
    loadOpenlcbMessage(stateInfo.outgoing.msg, node.alias, node.id, 0, 0n, mti);
    copyNodeIdToPayload(stateInfo.outgoing.msg, node.id, 0);
    stateInfo.outgoing.msg.payloadCount = 6;
    node.state.initialized = true;
    node.producers.enumerator.running = true;
    node.producers.enumerator.enumIndex = 0;
    node.producers.enumerator.rangeEnumIndex = 0;
    node.consumers.enumerator.running = false;
    node.consumers.enumerator.enumIndex = 0;
    node.consumers.enumerator.rangeEnumIndex = 0;
    stateInfo.outgoing.valid = true;
    node.state.runState = RUNSTATE_LOAD_PRODUCER_EVENTS;
  }
  function loadProducerEvent(stateInfo, extractEventMti) {
    const node = stateInfo.node;
    if (node.producers.count === 0 && node.producers.rangeCount === 0) {
      node.producers.enumerator.running = false;
      node.state.runState = RUNSTATE_LOAD_CONSUMER_EVENTS;
      stateInfo.outgoing.valid = false;
      return;
    }
    if (node.producers.enumerator.rangeEnumIndex < node.producers.rangeCount) {
      loadOpenlcbMessage(stateInfo.outgoing.msg, node.alias, node.id, 0, 0n, MTI_PRODUCER_RANGE_IDENTIFIED);
      const range = node.producers.rangeList[node.producers.enumerator.rangeEnumIndex];
      const eventId = generateEventRangeId(range.startBase, range.eventCount);
      copyEventIdToPayload(stateInfo.outgoing.msg, eventId);
      node.producers.enumerator.rangeEnumIndex++;
      stateInfo.outgoing.enumerate = true;
      stateInfo.outgoing.valid = true;
      return;
    }
    if (node.producers.enumerator.enumIndex < node.producers.count) {
      const idx = node.producers.enumerator.enumIndex;
      const eventMti = extractEventMti ? extractEventMti(node, idx) : MTI_PRODUCER_IDENTIFIED_UNKNOWN;
      const eventId = node.producers.list[idx].event;
      loadOpenlcbMessage(stateInfo.outgoing.msg, node.alias, node.id, 0, 0n, eventMti);
      copyEventIdToPayload(stateInfo.outgoing.msg, eventId);
      node.producers.enumerator.enumIndex++;
      stateInfo.outgoing.enumerate = true;
      stateInfo.outgoing.valid = true;
      return;
    }
    node.producers.enumerator.enumIndex = 0;
    node.producers.enumerator.rangeEnumIndex = 0;
    node.producers.enumerator.running = false;
    node.consumers.enumerator.enumIndex = 0;
    node.consumers.enumerator.rangeEnumIndex = 0;
    node.consumers.enumerator.running = true;
    stateInfo.outgoing.enumerate = false;
    stateInfo.outgoing.valid = false;
    node.state.runState = RUNSTATE_LOAD_CONSUMER_EVENTS;
  }
  function loadConsumerEvent(stateInfo, extractEventMti) {
    const node = stateInfo.node;
    if (node.consumers.count === 0 && node.consumers.rangeCount === 0) {
      node.consumers.enumerator.running = false;
      node.state.runState = RUNSTATE_LOGIN_COMPLETE;
      stateInfo.outgoing.valid = false;
      return;
    }
    if (node.consumers.enumerator.rangeEnumIndex < node.consumers.rangeCount) {
      loadOpenlcbMessage(stateInfo.outgoing.msg, node.alias, node.id, 0, 0n, MTI_CONSUMER_RANGE_IDENTIFIED);
      const range = node.consumers.rangeList[node.consumers.enumerator.rangeEnumIndex];
      const eventId = generateEventRangeId(range.startBase, range.eventCount);
      copyEventIdToPayload(stateInfo.outgoing.msg, eventId);
      node.consumers.enumerator.rangeEnumIndex++;
      stateInfo.outgoing.enumerate = true;
      stateInfo.outgoing.valid = true;
      return;
    }
    if (node.consumers.enumerator.enumIndex < node.consumers.count) {
      const idx = node.consumers.enumerator.enumIndex;
      const eventMti = extractEventMti ? extractEventMti(node, idx) : MTI_CONSUMER_IDENTIFIED_UNKNOWN;
      const eventId = node.consumers.list[idx].event;
      loadOpenlcbMessage(stateInfo.outgoing.msg, node.alias, node.id, 0, 0n, eventMti);
      copyEventIdToPayload(stateInfo.outgoing.msg, eventId);
      node.consumers.enumerator.enumIndex++;
      stateInfo.outgoing.enumerate = true;
      stateInfo.outgoing.valid = true;
      return;
    }
    node.producers.enumerator.enumIndex = 0;
    node.producers.enumerator.rangeEnumIndex = 0;
    node.producers.enumerator.running = false;
    node.consumers.enumerator.enumIndex = 0;
    node.consumers.enumerator.rangeEnumIndex = 0;
    node.consumers.enumerator.running = false;
    stateInfo.outgoing.enumerate = false;
    stateInfo.outgoing.valid = false;
    node.state.runState = RUNSTATE_LOGIN_COMPLETE;
  }
  var OpenLcbLoginStatemachine = class {
    /**
     * @param {Object} deps
     * @param {NodePool}   deps.nodePool          required
     * @param {(msg) => boolean} deps.sendOpenlcbMsg required — TX callback
     * @param {(node) => boolean} [deps.onLoginComplete] optional gate before RUN
     * @param {(node, index) => number} [deps.extractProducerEventStateMti]
     *        optional — default PRODUCER_IDENTIFIED_UNKNOWN
     * @param {(node, index) => number} [deps.extractConsumerEventStateMti]
     *        optional — default CONSUMER_IDENTIFIED_UNKNOWN
     * @param {(statemachineInfo) => void} [deps.processMainStatemachine]
     *        optional — the main MTI dispatcher (Phase 4). Wired up for
     *        sibling dispatch of login messages to local virtual nodes.
     */
    constructor(deps) {
      this._nodePool = deps.nodePool;
      this._sendOpenlcbMsg = deps.sendOpenlcbMsg;
      this._onLoginComplete = deps.onLoginComplete ?? null;
      this._extractProducerMti = deps.extractProducerEventStateMti ?? null;
      this._extractConsumerMti = deps.extractConsumerEventStateMti ?? null;
      this._processMainStatemachine = deps.processMainStatemachine ?? null;
      this._stateInfo = {
        node: null,
        outgoing: {
          msg: createMessage({ payloadType: PAYLOAD_TYPE.BASIC }),
          valid: false,
          enumerate: false
        }
      };
      this._stateInfo.outgoing.msg.state.allocated = true;
      this._sibling = {
        node: null,
        incoming: { msg: null, enumerate: false },
        outgoing: {
          msg: createMessage({ payloadType: PAYLOAD_TYPE.WORKER }),
          valid: false,
          enumerate: false
        },
        active: false,
        currentTick: 0
      };
      this._sibling.outgoing.msg.state.allocated = true;
    }
    /** Access internal state (tests/debug). Do not mutate. */
    getStateInfo() {
      return this._stateInfo;
    }
    // -------------------------------------------------------------------------
    // State dispatch
    // -------------------------------------------------------------------------
    /** Dispatch to the handler matching node.state.runState. */
    process(stateInfo) {
      switch (stateInfo.node.state.runState) {
        case RUNSTATE_LOAD_INITIALIZATION_COMPLETE:
          return loadInitializationComplete(stateInfo);
        case RUNSTATE_LOAD_CONSUMER_EVENTS:
          return loadConsumerEvent(stateInfo, this._extractConsumerMti);
        case RUNSTATE_LOAD_PRODUCER_EVENTS:
          return loadProducerEvent(stateInfo, this._extractProducerMti);
        case RUNSTATE_LOGIN_COMPLETE:
          if (this._onLoginComplete && !this._onLoginComplete(stateInfo.node)) return;
          stateInfo.node.state.runState = RUNSTATE_RUN;
          return;
        default:
          return;
      }
    }
    // -------------------------------------------------------------------------
    // Main-loop iteration
    // -------------------------------------------------------------------------
    run() {
      if (!this._sibling.active) {
        if (this._handleOutgoing()) return;
      }
      if (this._sibling.active) {
        if (this._siblingHandleOutgoing()) return;
        if (this._siblingHandleReenumerate()) return;
        if (this._siblingDispatchCurrent()) {
          this._siblingAdvance();
          if (!this._sibling.active) {
            this._stateInfo.outgoing.msg.state.loopback = false;
            this._stateInfo.outgoing.valid = false;
          }
          return;
        }
      }
      if (this._handleTryReenumerate()) return;
      if (this._handleTryEnumerateFirstNode()) return;
      if (this._handleTryEnumerateNextNode()) return;
    }
    // -------------------------------------------------------------------------
    // Main-context helpers (public for unit tests)
    // -------------------------------------------------------------------------
    handleOutgoingOpenlcbMessage() {
      return this._handleOutgoing();
    }
    _handleOutgoing() {
      if (!this._stateInfo.outgoing.valid) return false;
      if (this._sendOpenlcbMsg(this._stateInfo.outgoing.msg)) {
        if (!this._siblingDispatchBegin()) {
          this._stateInfo.outgoing.valid = false;
        }
      }
      return true;
    }
    handleTryReenumerate() {
      return this._handleTryReenumerate();
    }
    _handleTryReenumerate() {
      if (this._stateInfo.outgoing.enumerate) {
        this.process(this._stateInfo);
        return true;
      }
      return false;
    }
    handleTryEnumerateFirstNode() {
      return this._handleTryEnumerateFirstNode();
    }
    _handleTryEnumerateFirstNode() {
      if (this._stateInfo.node) return false;
      this._stateInfo.node = this._nodePool.getFirst(OPENLCB_LOGIN_STATMACHINE_NODE_ENUMERATOR_INDEX);
      if (!this._stateInfo.node) return true;
      if (this._stateInfo.node.state.runState < RUNSTATE_RUN) {
        this.process(this._stateInfo);
      }
      return true;
    }
    handleTryEnumerateNextNode() {
      return this._handleTryEnumerateNextNode();
    }
    _handleTryEnumerateNextNode() {
      if (!this._stateInfo.node) return false;
      this._stateInfo.node = this._nodePool.getNext(OPENLCB_LOGIN_STATMACHINE_NODE_ENUMERATOR_INDEX);
      if (!this._stateInfo.node) return true;
      if (this._stateInfo.node.state.runState < RUNSTATE_RUN) {
        this.process(this._stateInfo);
      }
      return true;
    }
    // -------------------------------------------------------------------------
    // Sibling dispatch (optional — only meaningful when multiple virtual nodes
    // exist and the main MTI dispatcher has been wired in)
    // -------------------------------------------------------------------------
    _siblingDispatchBegin() {
      if (this._nodePool.getCount() <= 1) return false;
      if (!this._processMainStatemachine) return false;
      this._sibling.incoming.msg = this._stateInfo.outgoing.msg;
      this._sibling.incoming.enumerate = false;
      this._sibling.incoming.msg.state.loopback = true;
      this._sibling.node = this._nodePool.getFirst(OPENLCB_LOGIN_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX);
      this._sibling.active = true;
      return true;
    }
    _siblingHandleOutgoing() {
      if (this._sibling.outgoing.valid) {
        if (this._sendOpenlcbMsg(this._sibling.outgoing.msg)) {
          this._sibling.outgoing.valid = false;
        }
        return true;
      }
      return false;
    }
    _siblingHandleReenumerate() {
      if (this._sibling.incoming.enumerate) {
        this._processMainStatemachine(this._sibling);
        return true;
      }
      return false;
    }
    _siblingDispatchCurrent() {
      if (!this._sibling.node) {
        this._sibling.active = false;
        return false;
      }
      if (this._sibling.node.state.runState === RUNSTATE_RUN) {
        this._processMainStatemachine(this._sibling);
      }
      return true;
    }
    _siblingAdvance() {
      if (!this._sibling.node) return false;
      this._sibling.node = this._nodePool.getNext(OPENLCB_LOGIN_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX);
      if (!this._sibling.node) {
        this._sibling.active = false;
        this._sibling.incoming.msg = null;
      }
      return true;
    }
  };

  // src/openlcb/main-statemachine.js
  var DISPATCH = /* @__PURE__ */ new Map([
    [MTI_SIMPLE_NODE_INFO_REQUEST, { handler: "snipSimpleNodeInfoRequest", onMissing: "reject" }],
    [MTI_SIMPLE_NODE_INFO_REPLY, { handler: "snipSimpleNodeInfoReply", onMissing: "silent" }],
    [MTI_INITIALIZATION_COMPLETE, { handler: "messageNetworkInitializationComplete", onMissing: "silent" }],
    [MTI_INITIALIZATION_COMPLETE_SIMPLE, { handler: "messageNetworkInitializationCompleteSimple", onMissing: "silent" }],
    [MTI_PROTOCOL_SUPPORT_INQUIRY, { handler: "messageNetworkProtocolSupportInquiry", onMissing: "silent" }],
    [MTI_PROTOCOL_SUPPORT_REPLY, { handler: "messageNetworkProtocolSupportReply", onMissing: "silent" }],
    [MTI_VERIFY_NODE_ID_ADDRESSED, { handler: "messageNetworkVerifyNodeIdAddressed", onMissing: "silent" }],
    [MTI_VERIFY_NODE_ID_GLOBAL, { handler: "messageNetworkVerifyNodeIdGlobal", onMissing: "silent" }],
    [MTI_VERIFIED_NODE_ID, { handler: "messageNetworkVerifiedNodeId", onMissing: "silent" }],
    [MTI_VERIFIED_NODE_ID_SIMPLE, { handler: "messageNetworkVerifiedNodeId", onMissing: "silent" }],
    [MTI_OPTIONAL_INTERACTION_REJECTED, { handler: "messageNetworkOptionalInteractionRejected", onMissing: "silent" }],
    [MTI_TERMINATE_DUE_TO_ERROR, { handler: "messageNetworkTerminateDueToError", onMissing: "silent" }],
    [MTI_CONSUMER_IDENTIFY, { handler: "eventTransportConsumerIdentify", onMissing: "silent" }],
    [MTI_CONSUMER_RANGE_IDENTIFIED, { handler: "eventTransportConsumerRangeIdentified", onMissing: "silent" }],
    [MTI_CONSUMER_IDENTIFIED_UNKNOWN, { handler: "eventTransportConsumerIdentifiedUnknown", onMissing: "silent" }],
    [MTI_CONSUMER_IDENTIFIED_SET, { handler: "eventTransportConsumerIdentifiedSet", onMissing: "silent" }],
    [MTI_CONSUMER_IDENTIFIED_CLEAR, { handler: "eventTransportConsumerIdentifiedClear", onMissing: "silent" }],
    [MTI_CONSUMER_IDENTIFIED_RESERVED, { handler: "eventTransportConsumerIdentifiedReserved", onMissing: "silent" }],
    [MTI_PRODUCER_IDENTIFY, { handler: "eventTransportProducerIdentify", onMissing: "silent" }],
    [MTI_PRODUCER_RANGE_IDENTIFIED, { handler: "eventTransportProducerRangeIdentified", onMissing: "silent" }],
    [MTI_PRODUCER_IDENTIFIED_UNKNOWN, { handler: "eventTransportProducerIdentifiedUnknown", onMissing: "silent" }],
    [MTI_PRODUCER_IDENTIFIED_SET, { handler: "eventTransportProducerIdentifiedSet", onMissing: "silent" }],
    [MTI_PRODUCER_IDENTIFIED_CLEAR, { handler: "eventTransportProducerIdentifiedClear", onMissing: "silent" }],
    [MTI_PRODUCER_IDENTIFIED_RESERVED, { handler: "eventTransportProducerIdentifiedReserved", onMissing: "silent" }],
    [MTI_EVENTS_IDENTIFY_DEST, { handler: "eventTransportIdentifyDest", onMissing: "silent" }],
    [MTI_EVENTS_IDENTIFY, { handler: "eventTransportIdentify", onMissing: "silent" }],
    [MTI_EVENT_LEARN, { handler: "eventTransportLearn", onMissing: "silent" }],
    [MTI_PC_EVENT_REPORT, { handler: "eventTransportPcReport", onMissing: "silent" }],
    [MTI_PC_EVENT_REPORT_WITH_PAYLOAD, { handler: "eventTransportPcReportWithPayload", onMissing: "silent" }],
    [MTI_TRAIN_PROTOCOL, { handler: "trainControlCommand", onMissing: "reject" }],
    [MTI_TRAIN_REPLY, { handler: "trainControlReply", onMissing: "silent" }],
    [MTI_SIMPLE_TRAIN_INFO_REQUEST, { handler: "simpleTrainNodeIdentInfoRequest", onMissing: "reject" }],
    [MTI_SIMPLE_TRAIN_INFO_REPLY, { handler: "simpleTrainNodeIdentInfoReply", onMissing: "silent" }],
    [MTI_DATAGRAM, { handler: "datagram", onMissing: "datagramReject" }],
    [MTI_DATAGRAM_OK_REPLY, { handler: "datagramOkReply", onMissing: "silent" }],
    [MTI_DATAGRAM_REJECTED_REPLY, { handler: "datagramRejectedReply", onMissing: "silent" }],
    [MTI_STREAM_INIT_REQUEST, { handler: "streamInitiateRequest", onMissing: "reject" }],
    [MTI_STREAM_INIT_REPLY, { handler: "streamInitiateReply", onMissing: "silent" }],
    [MTI_STREAM_SEND, { handler: "streamSendData", onMissing: "reject" }],
    [MTI_STREAM_PROCEED, { handler: "streamDataProceed", onMissing: "silent" }],
    [MTI_STREAM_COMPLETE, { handler: "streamDataComplete", onMissing: "reject" }]
  ]);
  var OpenLcbMainStatemachine = class {
    /**
     * @param {Object} deps
     * @param {NodePool}   deps.nodePool        required
     * @param {MessageFifo} deps.openlcbRxFifo  required — incoming OpenLCB FIFO
     * @param {(msg) => boolean} deps.sendOpenlcbMsg required — wire TX callback
     * @param {() => number}     deps.getCurrentTick required — 100ms tick
     * @param {Object}           deps.handlers       required — MTI callbacks (see DISPATCH)
     * @param {(sm) => void}     [deps.loadInteractionRejected]
     *        Optional custom reject builder (falls back to the internal one
     *        which writes UNKNOWN_MTI into the standard OIR format).
     * @param {(sm, errorCode) => void} [deps.loadDatagramRejected]
     *        Required only if any datagram handler is wired up.
     */
    constructor(deps) {
      this._nodePool = deps.nodePool;
      this._openlcbRxFifo = deps.openlcbRxFifo;
      this._sendOpenlcbMsg = deps.sendOpenlcbMsg;
      this._getCurrentTick = deps.getCurrentTick;
      this._handlers = deps.handlers;
      this._customReject = deps.loadInteractionRejected ?? null;
      this._loadDatagramRejected = deps.loadDatagramRejected ?? null;
      this._sm = {
        node: null,
        incoming: { msg: null, enumerate: false },
        outgoing: {
          msg: createMessage({ payloadType: PAYLOAD_TYPE.WORKER }),
          valid: false,
          enumerate: false
        },
        currentTick: 0,
        enumeratorKey: OPENLCB_MAIN_STATMACHINE_NODE_ENUMERATOR_INDEX,
        trainSearchMatchFound: false
      };
      this._sm.outgoing.msg.state.allocated = true;
      this._sib = {
        node: null,
        incoming: { msg: null, enumerate: false },
        outgoing: {
          msg: createMessage({ payloadType: PAYLOAD_TYPE.WORKER }),
          valid: false,
          enumerate: false
        },
        currentTick: 0,
        enumeratorKey: OPENLCB_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX,
        trainSearchMatchFound: false
      };
      this._sib.outgoing.msg.state.allocated = true;
      this._siblingActive = false;
      this._siblingResponseQueue = [];
    }
    /** Access internal state for tests/debug. */
    getStateInfo() {
      return this._sm;
    }
    getSiblingStateInfo() {
      return this._sib;
    }
    // -------------------------------------------------------------------------
    // Helpers
    // -------------------------------------------------------------------------
    _defaultLoadInteractionRejected(sm) {
      if (!sm || !sm.node || !sm.incoming.msg || !sm.outgoing.msg) return;
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        sm.incoming.msg.sourceAlias,
        sm.incoming.msg.sourceId,
        MTI_OPTIONAL_INTERACTION_REJECTED
      );
      copyWordToPayload(sm.outgoing.msg, ERROR_PERMANENT_NOT_IMPLEMENTED_UNKNOWN_MTI_OR_TRANPORT_PROTOCOL, 0);
      copyWordToPayload(sm.outgoing.msg, sm.incoming.msg.mti, 2);
      sm.outgoing.valid = true;
    }
    _loadInteractionRejected(sm) {
      (this._customReject ?? this._defaultLoadInteractionRejected.bind(this))(sm);
    }
    /**
     * Return true when the node should process the incoming message.
     * Global messages pass through; addressed messages need an alias or ID
     * match. Verify Node ID Global is always accepted. Nodes must be
     * initialized to see wire traffic. Loopback copies skip their source.
     */
    doesNodeProcessMsg(sm) {
      if (!sm.node || !sm.incoming.msg) return false;
      if (sm.incoming.msg.state.loopback && sm.node.id === sm.incoming.msg.sourceId) {
        return false;
      }
      if (!sm.node.state.initialized) return false;
      const isAddressed = (sm.incoming.msg.mti & MASK_DEST_ADDRESS_PRESENT) === MASK_DEST_ADDRESS_PRESENT;
      if (!isAddressed) return true;
      if (sm.node.alias === sm.incoming.msg.destAlias || sm.node.id === sm.incoming.msg.destId) {
        return true;
      }
      return sm.incoming.msg.mti === MTI_VERIFY_NODE_ID_GLOBAL;
    }
    // -------------------------------------------------------------------------
    // MTI dispatch — the core switch
    // -------------------------------------------------------------------------
    process(sm) {
      if (!sm || !this.doesNodeProcessMsg(sm)) return;
      const entry = DISPATCH.get(sm.incoming.msg.mti);
      if (!entry) {
        if (isAddressedMessageForNode(sm.node, sm.incoming.msg)) {
          this._loadInteractionRejected(sm);
        }
        return;
      }
      const fn = this._handlers[entry.handler];
      if (fn) {
        fn(sm);
        return;
      }
      switch (entry.onMissing) {
        case "reject":
          this._loadInteractionRejected(sm);
          return;
        case "datagramReject":
          if (this._loadDatagramRejected) {
            this._loadDatagramRejected(sm, ERROR_PERMANENT_NOT_IMPLEMENTED);
          }
          return;
        case "silent":
        default:
          return;
      }
    }
    // -------------------------------------------------------------------------
    // Run-loop handlers
    // -------------------------------------------------------------------------
    _handleOutgoing() {
      if (!this._sm.outgoing.valid) return false;
      if (this._sendOpenlcbMsg(this._sm.outgoing.msg)) {
        if (!this._siblingDispatchBegin()) {
          this._sm.outgoing.valid = false;
        }
      }
      return true;
    }
    _handleTryReenumerate() {
      if (this._sm.incoming.enumerate) {
        this.process(this._sm);
        return true;
      }
      return false;
    }
    _handleTryPopNextIncoming() {
      if (this._sm.incoming.msg) return false;
      const next = this._openlcbRxFifo.pop();
      this._sm.incoming.msg = next;
      if (next && next.state.invalid) {
        this._sm.incoming.msg = null;
        return true;
      }
      this._sm.currentTick = this._getCurrentTick();
      return !next;
    }
    _freeIncomingMessage(sm) {
      sm.incoming.msg = null;
      sm.incoming.enumerate = false;
      sm.node = null;
      sm.trainSearchMatchFound = false;
    }
    _handleTryEnumerateFirstNode() {
      if (this._sm.node) return false;
      this._sm.node = this._nodePool.getFirst(OPENLCB_MAIN_STATMACHINE_NODE_ENUMERATOR_INDEX);
      if (!this._sm.node) {
        this._freeIncomingMessage(this._sm);
        return true;
      }
      if (this._sm.node.state.runState === RUNSTATE_RUN) {
        this.process(this._sm);
      }
      return true;
    }
    _handleTryEnumerateNextNode() {
      if (!this._sm.node) return false;
      this._sm.node = this._nodePool.getNext(OPENLCB_MAIN_STATMACHINE_NODE_ENUMERATOR_INDEX);
      if (!this._sm.node) {
        this._freeIncomingMessage(this._sm);
        return true;
      }
      if (this._sm.node.state.runState === RUNSTATE_RUN) {
        this.process(this._sm);
      }
      return true;
    }
    // -------------------------------------------------------------------------
    // Sibling dispatch
    // -------------------------------------------------------------------------
    _siblingDispatchBegin() {
      if (this._nodePool.getCount() <= 1) return false;
      this._sib.incoming.msg = this._sm.outgoing.msg;
      this._sib.incoming.enumerate = false;
      this._sib.incoming.msg.state.loopback = true;
      this._sib.trainSearchMatchFound = false;
      this._sib.node = this._nodePool.getFirst(OPENLCB_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX);
      this._siblingActive = true;
      return true;
    }
    _siblingHandleOutgoing() {
      if (this._sib.outgoing.valid) {
        if (this._sendOpenlcbMsg(this._sib.outgoing.msg)) {
          this._sib.outgoing.valid = false;
        }
        return true;
      }
      return false;
    }
    _siblingHandleReenumerate() {
      if (this._sib.incoming.enumerate) {
        this.process(this._sib);
        return true;
      }
      return false;
    }
    _siblingDispatchCurrent() {
      if (!this._sib.node) {
        this._siblingActive = false;
        return false;
      }
      if (this._sib.node.state.runState === RUNSTATE_RUN) {
        this.process(this._sib);
      }
      return true;
    }
    _siblingAdvance() {
      if (!this._sib.node) return false;
      this._sib.node = this._nodePool.getNext(OPENLCB_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX);
      if (!this._sib.node) {
        this._siblingActive = false;
        this._sib.incoming.msg = null;
      }
      return true;
    }
    // -------------------------------------------------------------------------
    // Send with sibling dispatch — wrapper for login-statemachine / apps
    // -------------------------------------------------------------------------
    /**
     * Send `msg` to the wire and, if we have multiple virtual nodes, queue it
     * into the sibling dispatch path so other virtual nodes see it too.
     */
    sendWithSiblingDispatch(msg) {
      if (!this._sendOpenlcbMsg(msg)) return false;
      if (this._nodePool.getCount() <= 1) return true;
      const copy = createMessage({ payloadType: PAYLOAD_TYPE.WORKER });
      copy.mti = msg.mti;
      copy.sourceAlias = msg.sourceAlias;
      copy.sourceId = msg.sourceId;
      copy.destAlias = msg.destAlias;
      copy.destId = msg.destId;
      copy.payloadCount = msg.payloadCount;
      copy.state.loopback = true;
      for (let i = 0; i < msg.payloadCount; i++) copy.payload[i] = msg.payload[i];
      this._siblingResponseQueue.push(copy);
      return true;
    }
    // -------------------------------------------------------------------------
    // Main-loop iteration
    // -------------------------------------------------------------------------
    run() {
      if (!this._siblingActive) {
        if (this._handleOutgoing()) return;
      }
      if (this._siblingActive) {
        if (this._siblingHandleOutgoing()) return;
        if (this._siblingHandleReenumerate()) return;
        if (this._siblingDispatchCurrent()) {
          this._siblingAdvance();
          if (!this._siblingActive) {
            this._sm.outgoing.msg.state.loopback = false;
            this._sm.outgoing.valid = false;
          }
          return;
        }
      }
      if (!this._siblingActive && this._siblingResponseQueue.length > 0) {
        const queued = this._siblingResponseQueue.shift();
        this._sib.incoming.msg = queued;
        this._sib.incoming.enumerate = false;
        this._sib.trainSearchMatchFound = false;
        this._sib.node = this._nodePool.getFirst(OPENLCB_SIBLING_DISPATCH_NODE_ENUMERATOR_INDEX);
        this._siblingActive = true;
        return;
      }
      if (this._handleTryReenumerate()) return;
      if (this._handleTryPopNextIncoming()) return;
      if (this._handleTryEnumerateFirstNode()) return;
      if (this._handleTryEnumerateNextNode()) return;
    }
  };

  // src/protocol/message-network.js
  var ProtocolMessageNetwork = class {
    /**
     * @param {Object} [deps]
     * @param {(node, sourceId, errorCode, rejectedMti) => void} [deps.onOptionalInteractionRejected]
     * @param {(node, sourceId, errorCode, rejectedMti) => void} [deps.onTerminateDueToError]
     */
    constructor(deps = {}) {
      this._onOptionalInteractionRejected = deps.onOptionalInteractionRejected ?? null;
      this._onTerminateDueToError = deps.onTerminateDueToError ?? null;
    }
    // -------------------------------------------------------------------------
    // Internal builders
    // -------------------------------------------------------------------------
    /** Fire duplicate-node-detected PCER once per boot. */
    _loadDuplicateNodeId(sm) {
      if (sm.node.state.duplicateIdDetected) return;
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        sm.incoming.msg.sourceAlias,
        sm.incoming.msg.sourceId,
        MTI_PC_EVENT_REPORT
      );
      copyEventIdToPayload(sm.outgoing.msg, EVENT_ID_DUPLICATE_NODE_DETECTED);
      sm.node.state.duplicateIdDetected = true;
      sm.outgoing.valid = true;
    }
    /**
     * Build a Verified Node ID reply. Per MessageNetworkS §3.4.2, the reply
     * is always unaddressed even when the triggering Verify was addressed.
     */
    _loadVerifiedNodeId(sm) {
      const params = sm.node.parameters;
      const mti = params && params.protocolSupport & PSI_SIMPLE ? MTI_VERIFIED_NODE_ID_SIMPLE : MTI_VERIFIED_NODE_ID;
      loadOpenlcbMessage(sm.outgoing.msg, sm.node.alias, sm.node.id, 0, 0n, mti);
      copyNodeIdToPayload(sm.outgoing.msg, sm.node.id, 0);
      sm.outgoing.valid = true;
    }
    // -------------------------------------------------------------------------
    // Public handlers (one per MTI)
    // -------------------------------------------------------------------------
    handleInitializationComplete(sm) {
      if (extractNodeIdFromPayload(sm.incoming.msg, 0) === sm.node.id) {
        this._loadDuplicateNodeId(sm);
        return;
      }
      sm.outgoing.valid = false;
    }
    // Same logic as full Initialization Complete — duplicate check only.
    handleInitializationCompleteSimple(sm) {
      this.handleInitializationComplete(sm);
    }
    handleProtocolSupportInquiry(sm) {
      let flags = sm.node.parameters.protocolSupport;
      if (sm.node.state.firmwareUpgradeActive) {
        flags = flags & ~PSI_FIRMWARE_UPGRADE | PSI_FIRMWARE_UPGRADE_ACTIVE;
      }
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        sm.incoming.msg.sourceAlias,
        sm.incoming.msg.sourceId,
        MTI_PROTOCOL_SUPPORT_REPLY
      );
      copyByteToPayload(sm.outgoing.msg, flags >>> 16 & 255, 0);
      copyByteToPayload(sm.outgoing.msg, flags >>> 8 & 255, 1);
      copyByteToPayload(sm.outgoing.msg, flags & 255, 2);
      copyByteToPayload(sm.outgoing.msg, 0, 3);
      copyByteToPayload(sm.outgoing.msg, 0, 4);
      copyByteToPayload(sm.outgoing.msg, 0, 5);
      sm.outgoing.valid = true;
    }
    handleProtocolSupportReply(sm) {
      sm.outgoing.valid = false;
    }
    /** Reply if payload is empty (unaddressed probe) or contains our Node ID. */
    handleVerifyNodeIdGlobal(sm) {
      if (sm.incoming.msg.payloadCount > 0) {
        if (extractNodeIdFromPayload(sm.incoming.msg, 0) === sm.node.id) {
          this._loadVerifiedNodeId(sm);
          return;
        }
        sm.outgoing.valid = false;
        return;
      }
      this._loadVerifiedNodeId(sm);
    }
    /** Addressed variant: always reply with our Node ID. */
    handleVerifyNodeIdAddressed(sm) {
      this._loadVerifiedNodeId(sm);
    }
    /** Received a Verified Node ID — check for a duplicate of our ID. */
    handleVerifiedNodeId(sm) {
      if (extractNodeIdFromPayload(sm.incoming.msg, 0) === sm.node.id) {
        this._loadDuplicateNodeId(sm);
        return;
      }
      sm.outgoing.valid = false;
    }
    handleOptionalInteractionRejected(sm) {
      sm.outgoing.valid = false;
      if (!this._onOptionalInteractionRejected) return;
      let errorCode = 0;
      let rejectedMti = 0;
      const msg = sm.incoming.msg;
      if (msg.payloadCount >= 2) errorCode = extractWordFromPayload(msg, 0);
      if (msg.payloadCount >= 4) rejectedMti = extractWordFromPayload(msg, 2);
      this._onOptionalInteractionRejected(sm.node, msg.sourceId, errorCode, rejectedMti);
    }
    handleTerminateDueToError(sm) {
      sm.outgoing.valid = false;
      if (!this._onTerminateDueToError) return;
      let errorCode = 0;
      let rejectedMti = 0;
      const msg = sm.incoming.msg;
      if (msg.payloadCount >= 2) errorCode = extractWordFromPayload(msg, 0);
      if (msg.payloadCount >= 4) rejectedMti = extractWordFromPayload(msg, 2);
      this._onTerminateDueToError(sm.node, msg.sourceId, errorCode, rejectedMti);
    }
  };

  // src/protocol/snip.js
  function processString(outgoingMsg, offset, str, maxStrLen, byteCount) {
    const strLen = str.length;
    const maxFitting = Math.min(strLen, maxStrLen - 1);
    const copyLen = byteCount > 0 && maxFitting > byteCount - 1 ? byteCount - 1 : maxFitting;
    for (let i = 0; i < copyLen; i++) {
      outgoingMsg.payload[offset + i] = str.charCodeAt(i) & 255;
    }
    outgoingMsg.payload[offset + copyLen] = 0;
    outgoingMsg.payloadCount += copyLen + 1;
    return offset + copyLen + 1;
  }
  function processVersion(outgoingMsg, offset, version) {
    outgoingMsg.payload[offset] = version & 255;
    outgoingMsg.payloadCount++;
    return offset + 1;
  }
  var ProtocolSnip = class {
    /**
     * @param {Object} [deps]
     * @param {(node, address, count, buffer) => void} [deps.configMemoryRead]
     *        Callback to read `count` bytes from a node's Configuration Memory
     *        space (0xFD) into the provided Uint8Array.
     */
    constructor(deps = {}) {
      this._configMemoryRead = deps.configMemoryRead ?? null;
    }
    // -------------------------------------------------------------------------
    // Field loaders — each returns the offset just past the field written
    // -------------------------------------------------------------------------
    loadManufacturerVersionId(node, outgoingMsg, offset, requestedBytes) {
      if (requestedBytes > 0) {
        return processVersion(outgoingMsg, offset, node.parameters.snip.mfgVersion);
      }
      return offset;
    }
    loadName(node, outgoingMsg, offset, requestedBytes) {
      return processString(outgoingMsg, offset, node.parameters.snip.name, LEN_SNIP_NAME_BUFFER, requestedBytes);
    }
    loadModel(node, outgoingMsg, offset, requestedBytes) {
      return processString(outgoingMsg, offset, node.parameters.snip.model, LEN_SNIP_MODEL_BUFFER, requestedBytes);
    }
    loadHardwareVersion(node, outgoingMsg, offset, requestedBytes) {
      return processString(outgoingMsg, offset, node.parameters.snip.hardwareVersion, LEN_SNIP_HARDWARE_VERSION_BUFFER, requestedBytes);
    }
    loadSoftwareVersion(node, outgoingMsg, offset, requestedBytes) {
      return processString(outgoingMsg, offset, node.parameters.snip.softwareVersion, LEN_SNIP_SOFTWARE_VERSION_BUFFER, requestedBytes);
    }
    loadUserVersionId(node, outgoingMsg, offset, requestedBytes) {
      if (requestedBytes > 0) {
        return processVersion(outgoingMsg, offset, node.parameters.snip.userVersion);
      }
      return offset;
    }
    loadUserName(node, outgoingMsg, offset, requestedBytes) {
      return this._loadConfigMemString(
        node,
        outgoingMsg,
        offset,
        requestedBytes,
        CONFIG_MEM_CONFIG_USER_NAME_OFFSET,
        LEN_SNIP_USER_NAME_BUFFER
      );
    }
    loadUserDescription(node, outgoingMsg, offset, requestedBytes) {
      return this._loadConfigMemString(
        node,
        outgoingMsg,
        offset,
        requestedBytes,
        CONFIG_MEM_CONFIG_USER_DESCRIPTION_OFFSET,
        LEN_SNIP_USER_DESCRIPTION_BUFFER
      );
    }
    _loadConfigMemString(node, outgoingMsg, offset, requestedBytes, baseOffset, fieldMaxLen) {
      let address = baseOffset;
      const cfg = node.parameters.addressSpaceConfigMemory;
      if (cfg && cfg.lowAddressValid) address += cfg.lowAddress;
      if (!this._configMemoryRead) {
        return processString(outgoingMsg, offset, "", fieldMaxLen, requestedBytes);
      }
      const buffer = new Uint8Array(requestedBytes);
      this._configMemoryRead(node, address, requestedBytes, buffer);
      let strLen = 0;
      while (strLen < buffer.length && buffer[strLen] !== 0) strLen++;
      let str = "";
      for (let i = 0; i < strLen; i++) str += String.fromCharCode(buffer[i]);
      return processString(outgoingMsg, offset, str, fieldMaxLen, requestedBytes);
    }
    // -------------------------------------------------------------------------
    // Public MTI handlers
    // -------------------------------------------------------------------------
    /** Build and return a SNIP reply. 8 fields appended in order. */
    handleSimpleNodeInfoRequest(sm) {
      let off = 0;
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        sm.incoming.msg.sourceAlias,
        sm.incoming.msg.sourceId,
        MTI_SIMPLE_NODE_INFO_REPLY
      );
      off = this.loadManufacturerVersionId(sm.node, sm.outgoing.msg, off, 1);
      off = this.loadName(sm.node, sm.outgoing.msg, off, LEN_SNIP_NAME_BUFFER - 1);
      off = this.loadModel(sm.node, sm.outgoing.msg, off, LEN_SNIP_MODEL_BUFFER - 1);
      off = this.loadHardwareVersion(sm.node, sm.outgoing.msg, off, LEN_SNIP_HARDWARE_VERSION_BUFFER - 1);
      off = this.loadSoftwareVersion(sm.node, sm.outgoing.msg, off, LEN_SNIP_SOFTWARE_VERSION_BUFFER - 1);
      off = this.loadUserVersionId(sm.node, sm.outgoing.msg, off, 1);
      off = this.loadUserName(sm.node, sm.outgoing.msg, off, LEN_SNIP_USER_NAME_BUFFER - 1);
      off = this.loadUserDescription(sm.node, sm.outgoing.msg, off, LEN_SNIP_USER_DESCRIPTION_BUFFER - 1);
      sm.outgoing.valid = true;
    }
    handleSimpleNodeInfoReply(sm) {
      sm.outgoing.valid = false;
    }
    /**
     * Validate a SNIP reply: correct MTI, size, and exactly 6 NULL separators.
     * @param {Object} snipReplyMsg
     * @returns {boolean}
     */
    static validateSnipReply(snipReplyMsg) {
      if (snipReplyMsg.payloadCount > LEN_MESSAGE_BYTES_SNIP) return false;
      if (snipReplyMsg.mti !== MTI_SIMPLE_NODE_INFO_REPLY) return false;
      return countNullsInPayload(snipReplyMsg) === 6;
    }
  };

  // src/protocol/event-transport.js
  var LEN_EVENT_ID2 = 8;
  var ProtocolEventTransport = class _ProtocolEventTransport {
    /**
     * All callbacks are optional. See C interface_openlcb_protocol_event_transport_t
     * for the full list. Accepted signatures (node, eventId[, status, payload]).
     */
    constructor(deps = {}) {
      this._cb = deps;
    }
    _fire(name, ...args) {
      const fn = this._cb[name];
      if (fn) fn(...args);
    }
    // -------------------------------------------------------------------------
    // Status → MTI helpers
    // -------------------------------------------------------------------------
    static extractConsumerEventStatusMti(node, index) {
      switch (node.consumers.list[index].status) {
        case EVENT_STATUS.SET:
          return MTI_CONSUMER_IDENTIFIED_SET;
        case EVENT_STATUS.CLEAR:
          return MTI_CONSUMER_IDENTIFIED_CLEAR;
        default:
          return MTI_CONSUMER_IDENTIFIED_UNKNOWN;
      }
    }
    static extractProducerEventStatusMti(node, index) {
      switch (node.producers.list[index].status) {
        case EVENT_STATUS.SET:
          return MTI_PRODUCER_IDENTIFIED_SET;
        case EVENT_STATUS.CLEAR:
          return MTI_PRODUCER_IDENTIFIED_CLEAR;
        default:
          return MTI_PRODUCER_IDENTIFIED_UNKNOWN;
      }
    }
    // -------------------------------------------------------------------------
    // Internal: consumed-event cross-dispatch
    // -------------------------------------------------------------------------
    _testForConsumedEvent(sm, status, payload) {
      const cb = this._cb.onConsumedEventIdentified;
      if (!cb) return;
      const targetEventId = extractEventIdFromPayload(sm.incoming.msg);
      if (isEventIdInConsumerRanges(sm.node, targetEventId)) {
        cb(sm.node, -1, targetEventId, status, payload);
        return;
      }
      const idx = findConsumerEventIndex(sm.node, targetEventId);
      if (idx >= 0) cb(sm.node, idx, targetEventId, status, payload);
    }
    _testForConsumedEventPcer(sm, payload) {
      const cb = this._cb.onConsumedEventPcer;
      if (!cb) return;
      const targetEventId = extractEventIdFromPayload(sm.incoming.msg);
      if (isEventIdInConsumerRanges(sm.node, targetEventId)) {
        cb(sm.node, -1, targetEventId, payload);
        return;
      }
      const idx = findConsumerEventIndex(sm.node, targetEventId);
      if (idx >= 0) cb(sm.node, idx, targetEventId, payload);
    }
    // -------------------------------------------------------------------------
    // Enumerators — produce one message per call, set `enumerate` until done
    // -------------------------------------------------------------------------
    /** @returns {boolean} true while more producer-enum work remains */
    _identifyProducers(sm) {
      const node = sm.node;
      if (node.consumers.enumerator.running) return false;
      if (!node.producers.enumerator.running) {
        sm.incoming.enumerate = true;
        node.producers.enumerator.running = true;
        node.producers.enumerator.enumIndex = 0;
        node.producers.enumerator.rangeEnumIndex = 0;
      }
      if (node.producers.enumerator.rangeEnumIndex < node.producers.rangeCount) {
        const r = node.producers.rangeList[node.producers.enumerator.rangeEnumIndex];
        loadOpenlcbMessage(
          sm.outgoing.msg,
          node.alias,
          node.id,
          sm.incoming.msg.sourceAlias,
          sm.incoming.msg.sourceId,
          MTI_PRODUCER_RANGE_IDENTIFIED
        );
        copyEventIdToPayload(sm.outgoing.msg, generateEventRangeId(r.startBase, r.eventCount));
        node.producers.enumerator.rangeEnumIndex++;
        sm.outgoing.enumerate = true;
        sm.outgoing.valid = true;
        return true;
      }
      if (node.producers.enumerator.enumIndex < node.producers.count) {
        const idx = node.producers.enumerator.enumIndex;
        loadOpenlcbMessage(
          sm.outgoing.msg,
          node.alias,
          node.id,
          sm.incoming.msg.sourceAlias,
          sm.incoming.msg.sourceId,
          _ProtocolEventTransport.extractProducerEventStatusMti(node, idx)
        );
        copyEventIdToPayload(sm.outgoing.msg, node.producers.list[idx].event);
        node.producers.enumerator.enumIndex++;
        sm.outgoing.enumerate = true;
        sm.outgoing.valid = true;
        return true;
      }
      node.consumers.enumerator.enumIndex = 0;
      node.consumers.enumerator.rangeEnumIndex = 0;
      node.consumers.enumerator.running = false;
      sm.outgoing.enumerate = true;
      sm.outgoing.valid = false;
      return false;
    }
    /** @returns {boolean} true while more consumer-enum work remains */
    _identifyConsumers(sm) {
      const node = sm.node;
      if (!node.consumers.enumerator.running) {
        sm.incoming.enumerate = true;
        node.consumers.enumerator.running = true;
        node.consumers.enumerator.enumIndex = 0;
        node.consumers.enumerator.rangeEnumIndex = 0;
      }
      if (node.consumers.enumerator.rangeEnumIndex < node.consumers.rangeCount) {
        const r = node.consumers.rangeList[node.consumers.enumerator.rangeEnumIndex];
        loadOpenlcbMessage(
          sm.outgoing.msg,
          node.alias,
          node.id,
          sm.incoming.msg.sourceAlias,
          sm.incoming.msg.sourceId,
          MTI_CONSUMER_RANGE_IDENTIFIED
        );
        copyEventIdToPayload(sm.outgoing.msg, generateEventRangeId(r.startBase, r.eventCount));
        node.consumers.enumerator.rangeEnumIndex++;
        sm.outgoing.enumerate = true;
        sm.outgoing.valid = true;
        return true;
      }
      if (node.consumers.enumerator.enumIndex < node.consumers.count) {
        const idx = node.consumers.enumerator.enumIndex;
        loadOpenlcbMessage(
          sm.outgoing.msg,
          node.alias,
          node.id,
          sm.incoming.msg.sourceAlias,
          sm.incoming.msg.sourceId,
          _ProtocolEventTransport.extractConsumerEventStatusMti(node, idx)
        );
        copyEventIdToPayload(sm.outgoing.msg, node.consumers.list[idx].event);
        node.consumers.enumerator.enumIndex++;
        sm.outgoing.enumerate = true;
        sm.outgoing.valid = true;
        return true;
      }
      node.producers.enumerator.enumIndex = 0;
      node.producers.enumerator.rangeEnumIndex = 0;
      node.producers.enumerator.running = false;
      node.consumers.enumerator.enumIndex = 0;
      node.consumers.enumerator.rangeEnumIndex = 0;
      node.consumers.enumerator.running = false;
      sm.incoming.enumerate = false;
      sm.outgoing.valid = false;
      return false;
    }
    // -------------------------------------------------------------------------
    // Identify single producer/consumer — targeted request
    // -------------------------------------------------------------------------
    handleConsumerIdentify(sm) {
      const targetEventId = extractEventIdFromPayload(sm.incoming.msg);
      const idx = findConsumerEventIndex(sm.node, targetEventId);
      if (idx >= 0) {
        loadOpenlcbMessage(
          sm.outgoing.msg,
          sm.node.alias,
          sm.node.id,
          sm.incoming.msg.sourceAlias,
          sm.incoming.msg.sourceId,
          _ProtocolEventTransport.extractConsumerEventStatusMti(sm.node, idx)
        );
        copyEventIdToPayload(sm.outgoing.msg, sm.node.consumers.list[idx].event);
        sm.outgoing.valid = true;
      } else if (isEventIdInConsumerRanges(sm.node, targetEventId)) {
        loadOpenlcbMessage(
          sm.outgoing.msg,
          sm.node.alias,
          sm.node.id,
          sm.incoming.msg.sourceAlias,
          sm.incoming.msg.sourceId,
          MTI_CONSUMER_IDENTIFIED_UNKNOWN
        );
        copyEventIdToPayload(sm.outgoing.msg, targetEventId);
        sm.outgoing.valid = true;
      } else {
        sm.outgoing.valid = false;
      }
    }
    handleProducerIdentify(sm) {
      const targetEventId = extractEventIdFromPayload(sm.incoming.msg);
      const idx = findProducerEventIndex(sm.node, targetEventId);
      if (idx >= 0) {
        loadOpenlcbMessage(
          sm.outgoing.msg,
          sm.node.alias,
          sm.node.id,
          sm.incoming.msg.sourceAlias,
          sm.incoming.msg.sourceId,
          _ProtocolEventTransport.extractProducerEventStatusMti(sm.node, idx)
        );
        copyEventIdToPayload(sm.outgoing.msg, sm.node.producers.list[idx].event);
        sm.outgoing.valid = true;
      } else if (isEventIdInProducerRanges(sm.node, targetEventId)) {
        loadOpenlcbMessage(
          sm.outgoing.msg,
          sm.node.alias,
          sm.node.id,
          sm.incoming.msg.sourceAlias,
          sm.incoming.msg.sourceId,
          MTI_PRODUCER_IDENTIFIED_UNKNOWN
        );
        copyEventIdToPayload(sm.outgoing.msg, targetEventId);
        sm.outgoing.valid = true;
      } else {
        sm.outgoing.valid = false;
      }
    }
    // -------------------------------------------------------------------------
    // Range Identified — callback only, no automatic response
    // -------------------------------------------------------------------------
    handleConsumerRangeIdentified(sm) {
      this._fire("onConsumerRangeIdentified", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    handleProducerRangeIdentified(sm) {
      this._fire("onProducerRangeIdentified", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    // -------------------------------------------------------------------------
    // Consumer Identified variants
    // -------------------------------------------------------------------------
    handleConsumerIdentifiedUnknown(sm) {
      this._fire("onConsumerIdentifiedUnknown", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    handleConsumerIdentifiedSet(sm) {
      this._fire("onConsumerIdentifiedSet", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    handleConsumerIdentifiedClear(sm) {
      this._fire("onConsumerIdentifiedClear", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    handleConsumerIdentifiedReserved(sm) {
      this._fire("onConsumerIdentifiedReserved", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    // -------------------------------------------------------------------------
    // Producer Identified variants — also cross-dispatch as consumed events
    // -------------------------------------------------------------------------
    handleProducerIdentifiedUnknown(sm) {
      this._testForConsumedEvent(sm, EVENT_STATUS.UNKNOWN, null);
      this._fire("onProducerIdentifiedUnknown", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    handleProducerIdentifiedSet(sm) {
      this._testForConsumedEvent(sm, EVENT_STATUS.SET, null);
      this._fire("onProducerIdentifiedSet", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    handleProducerIdentifiedClear(sm) {
      this._testForConsumedEvent(sm, EVENT_STATUS.CLEAR, null);
      this._fire("onProducerIdentifiedClear", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    handleProducerIdentifiedReserved(sm) {
      this._fire("onProducerIdentifiedReserved", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    // -------------------------------------------------------------------------
    // Events Identify (global + addressed)
    // -------------------------------------------------------------------------
    handleEventsIdentify(sm) {
      if (this._identifyProducers(sm)) return;
      this._identifyConsumers(sm);
    }
    handleEventsIdentifyDest(sm) {
      if (sm.node.alias === sm.incoming.msg.destAlias || sm.node.id === sm.incoming.msg.destId) {
        this.handleEventsIdentify(sm);
        return;
      }
      sm.outgoing.valid = false;
    }
    // -------------------------------------------------------------------------
    // Event Learn / PCER / PCER-with-payload
    // -------------------------------------------------------------------------
    handleEventLearn(sm) {
      this._fire("onEventLearn", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    handlePcEventReport(sm) {
      this._testForConsumedEventPcer(sm, null);
      this._fire("onPcEventReport", sm.node, extractEventIdFromPayload(sm.incoming.msg));
      sm.outgoing.valid = false;
    }
    handlePcEventReportWithPayload(sm) {
      const msg = sm.incoming.msg;
      if (msg.payloadCount <= LEN_EVENT_ID2) {
        sm.outgoing.valid = false;
        return;
      }
      const eventId = extractEventIdFromPayload(msg);
      const payloadCount = msg.payloadCount - LEN_EVENT_ID2;
      const payload = msg.payload.subarray(LEN_EVENT_ID2, LEN_EVENT_ID2 + payloadCount);
      this._testForConsumedEventPcer(sm, payload);
      this._fire("onPcEventReportWithPayload", sm.node, eventId, payloadCount, payload);
      sm.outgoing.valid = false;
    }
  };

  // src/protocol/datagram-handler.js
  var DATAGRAM_TIMEOUT_TICKS = 30;
  var DATAGRAM_MAX_RETRIES = 3;
  var SUBCOMMAND_TABLE = /* @__PURE__ */ new Map([
    // Read (datagram transport)
    [CONFIG_MEM_READ_SPACE_IN_BYTE_6, { op: "read", space: "inByte6" }],
    [CONFIG_MEM_READ_SPACE_FD, { op: "read", space: "FD" }],
    [CONFIG_MEM_READ_SPACE_FE, { op: "read", space: "FE" }],
    [CONFIG_MEM_READ_SPACE_FF, { op: "read", space: "FF" }],
    [CONFIG_MEM_READ_REPLY_OK_SPACE_IN_BYTE_6, { op: "readReplyOk", space: "inByte6" }],
    [CONFIG_MEM_READ_REPLY_OK_SPACE_FD, { op: "readReplyOk", space: "FD" }],
    [CONFIG_MEM_READ_REPLY_OK_SPACE_FE, { op: "readReplyOk", space: "FE" }],
    [CONFIG_MEM_READ_REPLY_OK_SPACE_FF, { op: "readReplyOk", space: "FF" }],
    [CONFIG_MEM_READ_REPLY_FAIL_SPACE_IN_BYTE_6, { op: "readReplyFail", space: "inByte6" }],
    [CONFIG_MEM_READ_REPLY_FAIL_SPACE_FD, { op: "readReplyFail", space: "FD" }],
    [CONFIG_MEM_READ_REPLY_FAIL_SPACE_FE, { op: "readReplyFail", space: "FE" }],
    [CONFIG_MEM_READ_REPLY_FAIL_SPACE_FF, { op: "readReplyFail", space: "FF" }],
    // Read (stream transport)
    [CONFIG_MEM_READ_STREAM_SPACE_IN_BYTE_6, { op: "readStream", space: "inByte6" }],
    [CONFIG_MEM_READ_STREAM_SPACE_FD, { op: "readStream", space: "FD" }],
    [CONFIG_MEM_READ_STREAM_SPACE_FE, { op: "readStream", space: "FE" }],
    [CONFIG_MEM_READ_STREAM_SPACE_FF, { op: "readStream", space: "FF" }],
    [CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_IN_BYTE_6, { op: "readStreamReplyOk", space: "inByte6" }],
    [CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FD, { op: "readStreamReplyOk", space: "FD" }],
    [CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FE, { op: "readStreamReplyOk", space: "FE" }],
    [CONFIG_MEM_READ_STREAM_REPLY_OK_SPACE_FF, { op: "readStreamReplyOk", space: "FF" }],
    [CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_IN_BYTE_6, { op: "readStreamReplyFail", space: "inByte6" }],
    [CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FD, { op: "readStreamReplyFail", space: "FD" }],
    [CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FE, { op: "readStreamReplyFail", space: "FE" }],
    [CONFIG_MEM_READ_STREAM_REPLY_FAIL_SPACE_FF, { op: "readStreamReplyFail", space: "FF" }],
    // Write (datagram transport)
    [CONFIG_MEM_WRITE_SPACE_IN_BYTE_6, { op: "write", space: "inByte6" }],
    [CONFIG_MEM_WRITE_SPACE_FD, { op: "write", space: "FD" }],
    [CONFIG_MEM_WRITE_SPACE_FE, { op: "write", space: "FE" }],
    [CONFIG_MEM_WRITE_SPACE_FF, { op: "write", space: "FF" }],
    [CONFIG_MEM_WRITE_REPLY_OK_SPACE_IN_BYTE_6, { op: "writeReplyOk", space: "inByte6" }],
    [CONFIG_MEM_WRITE_REPLY_OK_SPACE_FD, { op: "writeReplyOk", space: "FD" }],
    [CONFIG_MEM_WRITE_REPLY_OK_SPACE_FE, { op: "writeReplyOk", space: "FE" }],
    [CONFIG_MEM_WRITE_REPLY_OK_SPACE_FF, { op: "writeReplyOk", space: "FF" }],
    [CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_IN_BYTE_6, { op: "writeReplyFail", space: "inByte6" }],
    [CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FD, { op: "writeReplyFail", space: "FD" }],
    [CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FE, { op: "writeReplyFail", space: "FE" }],
    [CONFIG_MEM_WRITE_REPLY_FAIL_SPACE_FF, { op: "writeReplyFail", space: "FF" }],
    // Write under mask
    [CONFIG_MEM_WRITE_UNDER_MASK_SPACE_IN_BYTE_6, { op: "writeUnderMask", space: "inByte6" }],
    [CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FD, { op: "writeUnderMask", space: "FD" }],
    [CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FE, { op: "writeUnderMask", space: "FE" }],
    [CONFIG_MEM_WRITE_UNDER_MASK_SPACE_FF, { op: "writeUnderMask", space: "FF" }],
    // Write (stream transport)
    [CONFIG_MEM_WRITE_STREAM_SPACE_IN_BYTE_6, { op: "writeStream", space: "inByte6" }],
    [CONFIG_MEM_WRITE_STREAM_SPACE_FD, { op: "writeStream", space: "FD" }],
    [CONFIG_MEM_WRITE_STREAM_SPACE_FE, { op: "writeStream", space: "FE" }],
    [CONFIG_MEM_WRITE_STREAM_SPACE_FF, { op: "writeStream", space: "FF" }],
    [CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_IN_BYTE_6, { op: "writeStreamReplyOk", space: "inByte6" }],
    [CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FD, { op: "writeStreamReplyOk", space: "FD" }],
    [CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FE, { op: "writeStreamReplyOk", space: "FE" }],
    [CONFIG_MEM_WRITE_STREAM_REPLY_OK_SPACE_FF, { op: "writeStreamReplyOk", space: "FF" }],
    [CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_IN_BYTE_6, { op: "writeStreamReplyFail", space: "inByte6" }],
    [CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FD, { op: "writeStreamReplyFail", space: "FD" }],
    [CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FE, { op: "writeStreamReplyFail", space: "FE" }],
    [CONFIG_MEM_WRITE_STREAM_REPLY_FAIL_SPACE_FF, { op: "writeStreamReplyFail", space: "FF" }],
    // Global config-memory operations — no space nibble.
    [CONFIG_MEM_OPTIONS_CMD, { op: "options", space: null }],
    [CONFIG_MEM_OPTIONS_REPLY, { op: "optionsReply", space: null }],
    [CONFIG_MEM_GET_ADDRESS_SPACE_INFO_CMD, { op: "getAddressSpaceInfo", space: null }],
    [CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_NOT_PRESENT, { op: "getAddressSpaceInfoReplyNotPresent", space: null }],
    [CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_PRESENT, { op: "getAddressSpaceInfoReplyPresent", space: null }],
    [CONFIG_MEM_RESERVE_LOCK, { op: "reserveLock", space: null }],
    [CONFIG_MEM_RESERVE_LOCK_REPLY, { op: "reserveLockReply", space: null }],
    [CONFIG_MEM_GET_UNIQUE_ID, { op: "getUniqueId", space: null }],
    [CONFIG_MEM_GET_UNIQUE_ID_REPLY, { op: "getUniqueIdReply", space: null }],
    [CONFIG_MEM_UNFREEZE, { op: "unfreeze", space: null }],
    [CONFIG_MEM_FREEZE, { op: "freeze", space: null }],
    [CONFIG_MEM_UPDATE_COMPLETE, { op: "updateComplete", space: null }],
    [CONFIG_MEM_RESET_REBOOT, { op: "resetReboot", space: null }],
    [CONFIG_MEM_FACTORY_RESET, { op: "factoryReset", space: null }]
  ]);
  function resolveSpaceByte(spaceByte) {
    switch (spaceByte) {
      case CONFIG_MEM_SPACE_CONFIGURATION_DEFINITION_INFO:
        return "configDescriptionInfo";
      case CONFIG_MEM_SPACE_ALL:
        return "all";
      case CONFIG_MEM_SPACE_CONFIGURATION_MEMORY:
        return "configurationMemory";
      case CONFIG_MEM_SPACE_ACDI_MANUFACTURER_ACCESS:
        return "acdiManufacturer";
      case CONFIG_MEM_SPACE_ACDI_USER_ACCESS:
        return "acdiUser";
      case CONFIG_MEM_SPACE_TRAIN_FUNCTION_DEFINITION_INFO:
        return "trainFunctionDefinitionInfo";
      case CONFIG_MEM_SPACE_TRAIN_FUNCTION_CONFIGURATION_MEMORY:
        return "trainFunctionConfigMemory";
      case CONFIG_MEM_SPACE_FIRMWARE:
        return "firmwareUpgrade";
      default:
        return null;
    }
  }
  function shortSpaceToName(tag) {
    switch (tag) {
      case "FD":
        return "configurationMemory";
      case "FE":
        return "all";
      case "FF":
        return "configDescriptionInfo";
      default:
        return null;
    }
  }
  function callbackName(op, spaceName) {
    const cap = spaceName.charAt(0).toUpperCase() + spaceName.slice(1);
    return `memory${op.charAt(0).toUpperCase() + op.slice(1)}Space${cap}`;
  }
  var ProtocolDatagramHandler = class {
    /**
     * @param {Object} [deps]
     * @param {NodePool} [deps.nodePool] required for timeout scanning
     * @param {Object}   [deps.callbacks] memory-op callbacks keyed by `memoryReadSpaceAll`,
     *                                    `memoryWriteSpaceConfigurationMemory`, etc.
     *                                    Also: `memoryOptions`, `memoryFreeze`, …
     */
    constructor(deps = {}) {
      this._nodePool = deps.nodePool ?? null;
      this._cb = deps.callbacks ?? {};
    }
    // -------------------------------------------------------------------------
    // Outgoing reply builders
    // -------------------------------------------------------------------------
    /** @param {number} replyPendingTimeInSeconds 0 ⇒ no timeout; else rounded up to 2^N */
    loadDatagramReceivedOkMessage(sm, replyPendingTimeInSeconds) {
      let exponent = 0;
      if (replyPendingTimeInSeconds > 0) {
        exponent = Math.min(15, Math.ceil(Math.log2(Math.max(2, replyPendingTimeInSeconds))));
      }
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        sm.incoming.msg.sourceAlias,
        sm.incoming.msg.sourceId,
        MTI_DATAGRAM_OK_REPLY
      );
      copyByteToPayload(sm.outgoing.msg, exponent | DATAGRAM_OK_REPLY_PENDING, 0);
      sm.outgoing.valid = true;
    }
    loadDatagramRejectedMessage(sm, returnCode) {
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        sm.incoming.msg.sourceAlias,
        sm.incoming.msg.sourceId,
        MTI_DATAGRAM_REJECTED_REPLY
      );
      copyWordToPayload(sm.outgoing.msg, returnCode, 0);
      sm.outgoing.valid = true;
    }
    // -------------------------------------------------------------------------
    // Incoming: datagram / datagram OK / datagram rejected
    // -------------------------------------------------------------------------
    /** Dispatch an incoming datagram to its handler, or reject. */
    handleDatagram(sm) {
      const p0 = sm.incoming.msg.payload[0];
      if (p0 !== CONFIG_MEM_CONFIGURATION) {
        this.loadDatagramRejectedMessage(sm, ERROR_PERMANENT_NOT_IMPLEMENTED_COMMAND_UNKNOWN);
        return;
      }
      const subCmd = sm.incoming.msg.payload[1];
      const descriptor = SUBCOMMAND_TABLE.get(subCmd);
      if (!descriptor) {
        this.loadDatagramRejectedMessage(sm, ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN);
        return;
      }
      let cbName;
      if (descriptor.space === null) {
        cbName = `memory${descriptor.op.charAt(0).toUpperCase() + descriptor.op.slice(1)}`;
      } else {
        let spaceName;
        if (descriptor.space === "inByte6") {
          spaceName = resolveSpaceByte(sm.incoming.msg.payload[6]);
          if (!spaceName) {
            this.loadDatagramRejectedMessage(sm, ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN);
            return;
          }
        } else {
          spaceName = shortSpaceToName(descriptor.space);
        }
        cbName = callbackName(descriptor.op, spaceName);
      }
      const handler = this._cb[cbName];
      if (!handler) {
        this.loadDatagramRejectedMessage(sm, ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN);
        return;
      }
      handler(sm);
    }
    /** Acknowledgement — clear resend / free buffer. */
    handleDatagramReceivedOk(sm) {
      this.clearResendDatagramMessage(sm.node);
      sm.outgoing.valid = false;
    }
    /**
     * Negative reply — if temporary, mark the stored datagram for resend
     * (up to DATAGRAM_MAX_RETRIES). On permanent errors or max retries, free.
     */
    handleDatagramRejected(sm) {
      const errorCode = extractWordFromPayload(sm.incoming.msg, 0);
      if ((errorCode & ERROR_TEMPORARY) === ERROR_TEMPORARY) {
        const last = sm.node.lastReceivedDatagram;
        if (last) {
          const retries = (last.timer.retryCount ?? 0) + 1;
          if (retries < DATAGRAM_MAX_RETRIES) {
            last.timer.retryCount = retries;
            last.timer.tickSnapshot = sm.currentTick & 31;
            sm.node.state.resendDatagram = true;
          } else {
            this.clearResendDatagramMessage(sm.node);
          }
        }
      } else {
        this.clearResendDatagramMessage(sm.node);
      }
      sm.outgoing.valid = false;
    }
    /** Free the stored datagram (if any) and clear the resend flag. */
    clearResendDatagramMessage(node) {
      node.lastReceivedDatagram = null;
      node.state.resendDatagram = false;
    }
    // -------------------------------------------------------------------------
    // Periodic timeout scan
    // -------------------------------------------------------------------------
    /**
     * Walk the node pool looking for stuck or retry-exhausted datagrams.
     * Call from the main loop with the current 100ms tick.
     */
    checkTimeouts(currentTick) {
      if (!this._nodePool) return;
      let node = this._nodePool.getFirst(DATAGRAM_TIMEOUT_ENUM_KEY);
      while (node) {
        const last = node.lastReceivedDatagram;
        if (last) {
          const snapshot = last.timer.tickSnapshot ?? 0;
          const retries = last.timer.retryCount ?? 0;
          const elapsed = currentTick - snapshot & 31;
          if (elapsed >= DATAGRAM_TIMEOUT_TICKS || retries >= DATAGRAM_MAX_RETRIES) {
            node.lastReceivedDatagram = null;
            node.state.resendDatagram = false;
          }
        }
        node = this._nodePool.getNext(DATAGRAM_TIMEOUT_ENUM_KEY);
      }
    }
  };

  // src/protocol/config-mem-read.js
  function extractReadRequest(sm) {
    const msg = sm.incoming.msg;
    const inByte6 = msg.payload[1] === CONFIG_MEM_READ_SPACE_IN_BYTE_6;
    return {
      address: extractDwordFromPayload(msg, 2),
      encoding: inByte6 ? ADDRESS_SPACE_ENCODING.IN_BYTE_6 : ADDRESS_SPACE_ENCODING.IN_BYTE_1,
      bytes: inByte6 ? msg.payload[7] : msg.payload[6],
      dataStart: inByte6 ? 7 : 6,
      spaceInfo: null,
      readSpaceFunc: null
    };
  }
  function validate(req) {
    if (!req.readSpaceFunc) return ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN;
    if (!req.spaceInfo || !req.spaceInfo.present) return ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN;
    if (req.bytes === 0 || req.bytes > 64) return ERROR_PERMANENT_INVALID_ARGUMENTS;
    return S_OK;
  }
  function clampOverrun(req) {
    if (req.address + req.bytes >= req.spaceInfo.highestAddress) {
      req.bytes = req.spaceInfo.highestAddress - req.address + 1;
      if (req.bytes < 0) req.bytes = 0;
    }
  }
  var ProtocolConfigMemRead = class {
    /**
     * @param {Object} deps
     * @param {(sm, seconds) => void} deps.loadDatagramReceivedOk       required
     * @param {(sm, errorCode) => void} deps.loadDatagramRejected       required
     * @param {(node, address, count, buffer) => number} [deps.configMemoryRead]
     *        Reads `count` bytes into `buffer` (Uint8Array-like). Returns bytes read.
     * @param {Object} [deps.snip]
     *        SNIP field loaders (functions) for 0xFC/0xFB dispatch.
     *        Keys: loadManufacturerVersionId, loadName, loadModel, loadHardwareVersion,
     *              loadSoftwareVersion, loadUserVersionId, loadUserName, loadUserDescription.
     *        Each has signature (node, msg, offset, byteCount) => offset.
     * @param {(sm, req) => void} [deps.readRequestConfigDefinitionInfo] custom CDI reader
     * @param {(sm, req) => void} [deps.readRequestAll]
     * @param {(sm, req) => void} [deps.readRequestConfigMem]
     * @param {(sm, req) => void} [deps.readRequestAcdiManufacturer]
     * @param {(sm, req) => void} [deps.readRequestAcdiUser]
     * @param {(sm, req) => void} [deps.readRequestTrainFunctionConfigDefinitionInfo]
     * @param {(sm, req) => void} [deps.readRequestTrainFunctionConfigMemory]
     * @param {(sm, req) => number} [deps.delayedReplyTime]
     * @param {(node) => Object|null} [deps.getTrainState]
     */
    constructor(deps = {}) {
      this._d = deps;
    }
    // -------------------------------------------------------------------------
    // Per-space entry points
    // -------------------------------------------------------------------------
    readSpaceConfigDescriptionInfo(sm) {
      this._readSpace(
        sm,
        sm.node.parameters.addressSpaceConfigurationDefinitionInfo,
        this._d.readRequestConfigDefinitionInfo ?? this._defaultReadCdi.bind(this)
      );
    }
    readSpaceAll(sm) {
      this._readSpace(
        sm,
        sm.node.parameters.addressSpaceAll,
        this._d.readRequestAll ?? this._defaultReadAll.bind(this)
      );
    }
    readSpaceConfigMemory(sm) {
      this._readSpace(
        sm,
        sm.node.parameters.addressSpaceConfigMemory,
        this._d.readRequestConfigMem ?? this._defaultReadConfigMem.bind(this)
      );
    }
    readSpaceAcdiManufacturer(sm) {
      this._readSpace(
        sm,
        sm.node.parameters.addressSpaceAcdiManufacturer,
        this._d.readRequestAcdiManufacturer ?? this._defaultReadAcdiManufacturer.bind(this)
      );
    }
    readSpaceAcdiUser(sm) {
      this._readSpace(
        sm,
        sm.node.parameters.addressSpaceAcdiUser,
        this._d.readRequestAcdiUser ?? this._defaultReadAcdiUser.bind(this)
      );
    }
    readSpaceTrainFunctionDefinitionInfo(sm) {
      this._readSpace(
        sm,
        sm.node.parameters.addressSpaceTrainFunctionDefinitionInfo,
        this._d.readRequestTrainFunctionConfigDefinitionInfo ?? this._defaultReadTrainFdi.bind(this)
      );
    }
    readSpaceTrainFunctionConfigMemory(sm) {
      this._readSpace(
        sm,
        sm.node.parameters.addressSpaceTrainFunctionConfigMemory,
        this._d.readRequestTrainFunctionConfigMemory ?? this._defaultReadTrainFnCfg.bind(this)
      );
    }
    // -------------------------------------------------------------------------
    // Shared two-phase driver
    // -------------------------------------------------------------------------
    _readSpace(sm, spaceInfo, readFunc) {
      const req = extractReadRequest(sm);
      req.spaceInfo = spaceInfo;
      req.readSpaceFunc = readFunc;
      if (!sm.node.state.openlcbDatagramAckSent) {
        const err = validate(req);
        if (err !== S_OK) {
          this._d.loadDatagramRejected(sm, err);
          return;
        }
        const delay = this._d.delayedReplyTime ? this._d.delayedReplyTime(sm, req) : 0;
        this._d.loadDatagramReceivedOk(sm, delay);
        sm.node.state.openlcbDatagramAckSent = true;
        sm.incoming.enumerate = true;
        return;
      }
      if (req.address > spaceInfo.highestAddress) {
        loadConfigMemReplyReadFailHeader(sm, req, ERROR_PERMANENT_CONFIG_MEM_OUT_OF_BOUNDS_INVALID_ADDRESS);
        sm.outgoing.valid = true;
      } else {
        clampOverrun(req);
        readFunc(sm, req);
      }
      sm.node.state.openlcbDatagramAckSent = false;
      sm.incoming.enumerate = false;
    }
    // -------------------------------------------------------------------------
    // Default per-space readers
    // -------------------------------------------------------------------------
    _defaultReadCdi(sm, req) {
      const cdi = sm.node.parameters.cdi;
      if (!cdi) {
        loadConfigMemReplyReadFailHeader(sm, req, ERROR_PERMANENT_INVALID_ARGUMENTS);
        sm.outgoing.valid = true;
        return;
      }
      loadConfigMemReplyReadOkHeader(sm, req);
      copyByteArrayToPayload(sm.outgoing.msg, cdi.subarray(req.address, req.address + req.bytes), req.dataStart, req.bytes);
      sm.outgoing.valid = true;
    }
    _defaultReadAll(sm, req) {
      this._defaultReadConfigMem(sm, req);
    }
    _defaultReadConfigMem(sm, req) {
      if (!this._d.configMemoryRead) {
        loadConfigMemReplyReadFailHeader(sm, req, ERROR_PERMANENT_INVALID_ARGUMENTS);
        sm.outgoing.valid = true;
        return;
      }
      loadConfigMemReplyReadOkHeader(sm, req);
      const buffer = sm.outgoing.msg.payload.subarray(req.dataStart, req.dataStart + req.bytes);
      const readCount = this._d.configMemoryRead(sm.node, req.address, req.bytes, buffer);
      sm.outgoing.msg.payloadCount += readCount;
      if (readCount < req.bytes) {
        loadConfigMemReplyReadFailHeader(sm, req, ERROR_TEMPORARY_TRANSFER_ERROR);
      }
      sm.outgoing.valid = true;
    }
    _defaultReadTrainFdi(sm, req) {
      const fdi = sm.node.parameters.fdi;
      if (!fdi) {
        loadConfigMemReplyReadFailHeader(sm, req, ERROR_PERMANENT_INVALID_ARGUMENTS);
        sm.outgoing.valid = true;
        return;
      }
      loadConfigMemReplyReadOkHeader(sm, req);
      copyByteArrayToPayload(sm.outgoing.msg, fdi.subarray(req.address, req.address + req.bytes), req.dataStart, req.bytes);
      sm.outgoing.valid = true;
    }
    _defaultReadTrainFnCfg(sm, req) {
      loadConfigMemReplyReadOkHeader(sm, req);
      const state = this._d.getTrainState ? this._d.getTrainState(sm.node) : null;
      if (state && state.functions) {
        for (let i = 0; i < req.bytes; i++) {
          const fnIndex = Math.floor((req.address + i) / 2);
          const byteSel = (req.address + i) % 2;
          let val = 0;
          if (fnIndex < state.functions.length) {
            val = byteSel === 0 ? state.functions[fnIndex] >>> 8 & 255 : state.functions[fnIndex] & 255;
          }
          copyByteToPayload(sm.outgoing.msg, val, req.dataStart + i);
        }
      }
      sm.outgoing.valid = true;
    }
    _defaultReadAcdiManufacturer(sm, req) {
      loadConfigMemReplyReadOkHeader(sm, req);
      const snip = this._d.snip ?? {};
      const loader = this._acdiMfgLoader(req.address, snip);
      if (!loader) {
        loadConfigMemReplyReadFailHeader(
          sm,
          req,
          req.address <= CONFIG_MEM_ACDI_SOFTWARE_VERSION_ADDRESS ? ERROR_PERMANENT_INVALID_ARGUMENTS : ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN
        );
      } else {
        loader(sm.node, sm.outgoing.msg, req.dataStart, req.bytes);
      }
      sm.outgoing.valid = true;
    }
    _acdiMfgLoader(address, snip) {
      switch (address) {
        case CONFIG_MEM_ACDI_MANUFACTURER_VERSION_ADDRESS:
          return snip.loadManufacturerVersionId;
        case CONFIG_MEM_ACDI_MANUFACTURER_ADDRESS:
          return snip.loadName;
        case CONFIG_MEM_ACDI_MODEL_ADDRESS:
          return snip.loadModel;
        case CONFIG_MEM_ACDI_HARDWARE_VERSION_ADDRESS:
          return snip.loadHardwareVersion;
        case CONFIG_MEM_ACDI_SOFTWARE_VERSION_ADDRESS:
          return snip.loadSoftwareVersion;
        default:
          return null;
      }
    }
    _defaultReadAcdiUser(sm, req) {
      loadConfigMemReplyReadOkHeader(sm, req);
      const snip = this._d.snip ?? {};
      const loader = this._acdiUserLoader(req.address, snip);
      if (!loader) {
        loadConfigMemReplyReadFailHeader(
          sm,
          req,
          req.address <= CONFIG_MEM_ACDI_USER_DESCRIPTION_ADDRESS ? ERROR_PERMANENT_INVALID_ARGUMENTS : ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN
        );
      } else {
        loader(sm.node, sm.outgoing.msg, req.dataStart, req.bytes);
      }
      sm.outgoing.valid = true;
    }
    _acdiUserLoader(address, snip) {
      switch (address) {
        case CONFIG_MEM_ACDI_USER_VERSION_ADDRESS:
          return snip.loadUserVersionId;
        case CONFIG_MEM_ACDI_USER_NAME_ADDRESS:
          return snip.loadUserName;
        case CONFIG_MEM_ACDI_USER_DESCRIPTION_ADDRESS:
          return snip.loadUserDescription;
        default:
          return null;
      }
    }
  };

  // src/protocol/config-mem-write.js
  function extractWriteRequest(sm) {
    const msg = sm.incoming.msg;
    const inByte6 = msg.payload[1] === CONFIG_MEM_WRITE_SPACE_IN_BYTE_6;
    const dataStart = inByte6 ? 7 : 6;
    return {
      address: extractDwordFromPayload(msg, 2),
      encoding: inByte6 ? ADDRESS_SPACE_ENCODING.IN_BYTE_6 : ADDRESS_SPACE_ENCODING.IN_BYTE_1,
      bytes: msg.payloadCount - dataStart,
      dataStart,
      writeBuffer: msg.payload.subarray(dataStart, dataStart + (msg.payloadCount - dataStart)),
      spaceInfo: null,
      writeSpaceFunc: null
    };
  }
  function extractWriteUnderMaskRequest(sm) {
    const msg = sm.incoming.msg;
    const inByte6 = msg.payload[1] === CONFIG_MEM_WRITE_UNDER_MASK_SPACE_IN_BYTE_6;
    const header = inByte6 ? 7 : 6;
    const totalDataMask = msg.payloadCount - header;
    return {
      address: extractDwordFromPayload(msg, 2),
      encoding: inByte6 ? ADDRESS_SPACE_ENCODING.IN_BYTE_6 : ADDRESS_SPACE_ENCODING.IN_BYTE_1,
      bytes: Math.floor(totalDataMask / 2),
      dataStart: header,
      // (Mask, Data) pairs live in the payload starting at `header`.
      writeBuffer: msg.payload.subarray(header, header + totalDataMask),
      totalDataMask,
      spaceInfo: null
    };
  }
  function validateWrite(req) {
    if (!req.writeSpaceFunc) return ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN;
    if (!req.spaceInfo || !req.spaceInfo.present) return ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN;
    if (req.spaceInfo.readOnly) return ERROR_PERMANENT_CONFIG_MEM_ADDRESS_WRITE_TO_READ_ONLY;
    if (req.bytes === 0 || req.bytes > 64) return ERROR_PERMANENT_INVALID_ARGUMENTS;
    return S_OK;
  }
  function validateWriteUnderMask(req) {
    if (!req.spaceInfo || !req.spaceInfo.present) return ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN;
    if (req.spaceInfo.readOnly) return ERROR_PERMANENT_CONFIG_MEM_ADDRESS_WRITE_TO_READ_ONLY;
    if (req.bytes === 0 || req.bytes > 64) return ERROR_PERMANENT_INVALID_ARGUMENTS;
    if (req.totalDataMask % 2 !== 0) return ERROR_PERMANENT_INVALID_ARGUMENTS;
    return S_OK;
  }
  function clampOverrun2(req) {
    if (req.address + req.bytes >= req.spaceInfo.highestAddress) {
      req.bytes = req.spaceInfo.highestAddress - req.address + 1;
      if (req.bytes < 0) req.bytes = 0;
    }
  }
  var ProtocolConfigMemWrite = class {
    /**
     * @param {Object} deps
     * @param {(sm, seconds) => void} deps.loadDatagramReceivedOk       required
     * @param {(sm, errorCode) => void} deps.loadDatagramRejected       required
     * @param {(node, address, count, buffer) => number} [deps.configMemoryWrite]
     *        Writes `count` bytes from `buffer` to the node's storage. Returns written.
     * @param {(node, address, count, buffer) => number} [deps.configMemoryRead]
     *        Required for write-under-mask (read-modify-write).
     * @param {Object} [deps.writeRequestOverrides] optional per-space handler overrides
     * @param {(sm, req) => number} [deps.writeRequestFirmware]
     *        Firmware-upgrade space (0xEF). Callback signature differs — it
     *        invokes its completion via the returned promise / direct `sm.outgoing`
     *        manipulation; mirrors the C write_result callback.
     * @param {(sm, req) => number} [deps.delayedReplyTime]
     */
    constructor(deps = {}) {
      this._d = deps;
    }
    // -------------------------------------------------------------------------
    // Per-space entry points (plain write)
    // -------------------------------------------------------------------------
    writeSpaceConfigDescriptionInfo(sm) {
      this._writeSpace(
        sm,
        sm.node.parameters.addressSpaceConfigurationDefinitionInfo,
        this._d.writeRequestOverrides?.configDefinitionInfo ?? null
      );
    }
    writeSpaceAll(sm) {
      this._writeSpace(
        sm,
        sm.node.parameters.addressSpaceAll,
        this._d.writeRequestOverrides?.all ?? this._defaultWriteConfigMem.bind(this)
      );
    }
    writeSpaceConfigMemory(sm) {
      this._writeSpace(
        sm,
        sm.node.parameters.addressSpaceConfigMemory,
        this._d.writeRequestOverrides?.configMem ?? this._defaultWriteConfigMem.bind(this)
      );
    }
    writeSpaceAcdiManufacturer(sm) {
      this._writeSpace(
        sm,
        sm.node.parameters.addressSpaceAcdiManufacturer,
        this._d.writeRequestOverrides?.acdiManufacturer ?? null
      );
    }
    writeSpaceAcdiUser(sm) {
      this._writeSpace(
        sm,
        sm.node.parameters.addressSpaceAcdiUser,
        this._d.writeRequestOverrides?.acdiUser ?? this._defaultWriteAcdiUser.bind(this)
      );
    }
    writeSpaceTrainFunctionDefinitionInfo(sm) {
      this._writeSpace(
        sm,
        sm.node.parameters.addressSpaceTrainFunctionDefinitionInfo,
        this._d.writeRequestOverrides?.trainFunctionDefinitionInfo ?? null
      );
    }
    writeSpaceTrainFunctionConfigMemory(sm) {
      this._writeSpace(
        sm,
        sm.node.parameters.addressSpaceTrainFunctionConfigMemory,
        this._d.writeRequestOverrides?.trainFunctionConfigMemory ?? this._defaultWriteConfigMem.bind(this)
      );
    }
    writeSpaceFirmware(sm) {
      this._writeSpace(
        sm,
        sm.node.parameters.addressSpaceFirmware,
        this._d.writeRequestFirmware ?? null
      );
    }
    // -------------------------------------------------------------------------
    // Per-space entry points (write-under-mask)
    // -------------------------------------------------------------------------
    writeUnderMaskSpaceConfigDescriptionInfo(sm) {
      this._writeUnderMaskSpace(sm, sm.node.parameters.addressSpaceConfigurationDefinitionInfo);
    }
    writeUnderMaskSpaceAll(sm) {
      this._writeUnderMaskSpace(sm, sm.node.parameters.addressSpaceAll);
    }
    writeUnderMaskSpaceConfigMemory(sm) {
      this._writeUnderMaskSpace(sm, sm.node.parameters.addressSpaceConfigMemory);
    }
    writeUnderMaskSpaceAcdiManufacturer(sm) {
      this._writeUnderMaskSpace(sm, sm.node.parameters.addressSpaceAcdiManufacturer);
    }
    writeUnderMaskSpaceAcdiUser(sm) {
      this._writeUnderMaskSpace(sm, sm.node.parameters.addressSpaceAcdiUser);
    }
    writeUnderMaskSpaceTrainFunctionDefinitionInfo(sm) {
      this._writeUnderMaskSpace(sm, sm.node.parameters.addressSpaceTrainFunctionDefinitionInfo);
    }
    writeUnderMaskSpaceTrainFunctionConfigMemory(sm) {
      this._writeUnderMaskSpace(sm, sm.node.parameters.addressSpaceTrainFunctionConfigMemory);
    }
    writeUnderMaskSpaceFirmware(sm) {
      this._writeUnderMaskSpace(sm, sm.node.parameters.addressSpaceFirmware);
    }
    // -------------------------------------------------------------------------
    // Shared two-phase drivers
    // -------------------------------------------------------------------------
    _writeSpace(sm, spaceInfo, writeFunc) {
      const req = extractWriteRequest(sm);
      req.spaceInfo = spaceInfo;
      req.writeSpaceFunc = writeFunc;
      if (!sm.node.state.openlcbDatagramAckSent) {
        const err = validateWrite(req);
        if (err !== S_OK) {
          this._d.loadDatagramRejected(sm, err);
          return;
        }
        const delay = this._d.delayedReplyTime ? this._d.delayedReplyTime(sm, req) : 0;
        this._d.loadDatagramReceivedOk(sm, delay);
        sm.node.state.openlcbDatagramAckSent = true;
        sm.incoming.enumerate = true;
        return;
      }
      if (req.address > spaceInfo.highestAddress) {
        loadConfigMemReplyWriteFailHeader(sm, req, ERROR_PERMANENT_CONFIG_MEM_OUT_OF_BOUNDS_INVALID_ADDRESS);
        sm.outgoing.valid = true;
      } else {
        clampOverrun2(req);
        writeFunc(sm, req);
      }
      sm.node.state.openlcbDatagramAckSent = false;
      sm.incoming.enumerate = false;
    }
    _writeUnderMaskSpace(sm, spaceInfo) {
      const req = extractWriteUnderMaskRequest(sm);
      req.spaceInfo = spaceInfo;
      if (!sm.node.state.openlcbDatagramAckSent) {
        const err = validateWriteUnderMask(req);
        if (err !== S_OK) {
          this._d.loadDatagramRejected(sm, err);
          return;
        }
        const delay = this._d.delayedReplyTime ? this._d.delayedReplyTime(sm, req) : 0;
        this._d.loadDatagramReceivedOk(sm, delay);
        sm.node.state.openlcbDatagramAckSent = true;
        sm.incoming.enumerate = true;
        return;
      }
      if (req.address > spaceInfo.highestAddress) {
        loadConfigMemReplyWriteFailHeader(sm, req, ERROR_PERMANENT_CONFIG_MEM_OUT_OF_BOUNDS_INVALID_ADDRESS);
        sm.outgoing.valid = true;
      } else {
        clampOverrun2(req);
        loadConfigMemReplyWriteOkHeader(sm, req);
        this._writeDataUnderMask(sm, req);
      }
      sm.node.state.openlcbDatagramAckSent = false;
      sm.incoming.enumerate = false;
    }
    // -------------------------------------------------------------------------
    // Default writers
    // -------------------------------------------------------------------------
    _defaultWriteConfigMem(sm, req) {
      loadConfigMemReplyWriteOkHeader(sm, req);
      this._writeData(sm, req, req.address, req.writeBuffer.subarray(0, req.bytes));
    }
    _defaultWriteAcdiUser(sm, req) {
      let configAddress;
      switch (req.address) {
        case CONFIG_MEM_ACDI_USER_NAME_ADDRESS:
          configAddress = CONFIG_MEM_CONFIG_USER_NAME_OFFSET;
          break;
        case CONFIG_MEM_ACDI_USER_DESCRIPTION_ADDRESS:
          configAddress = CONFIG_MEM_CONFIG_USER_DESCRIPTION_OFFSET;
          break;
        default:
          loadConfigMemReplyWriteFailHeader(sm, req, ERROR_PERMANENT_CONFIG_MEM_OUT_OF_BOUNDS_INVALID_ADDRESS);
          sm.outgoing.valid = true;
          return;
      }
      const cfg = sm.node.parameters.addressSpaceConfigMemory;
      if (cfg && cfg.lowAddressValid) configAddress += cfg.lowAddress;
      const writeCount = this._d.configMemoryWrite ? this._d.configMemoryWrite(sm.node, configAddress, req.bytes, req.writeBuffer.subarray(0, req.bytes)) : 0;
      if (!this._d.configMemoryWrite || writeCount < req.bytes) {
        loadConfigMemReplyWriteFailHeader(
          sm,
          req,
          this._d.configMemoryWrite ? ERROR_TEMPORARY_TRANSFER_ERROR : ERROR_PERMANENT_INVALID_ARGUMENTS
        );
      } else {
        loadConfigMemReplyWriteOkHeader(sm, req);
      }
      sm.outgoing.valid = true;
    }
    _writeData(sm, req, address, buffer) {
      if (!this._d.configMemoryWrite) {
        loadConfigMemReplyWriteFailHeader(sm, req, ERROR_PERMANENT_INVALID_ARGUMENTS);
        sm.outgoing.valid = true;
        return 0;
      }
      const writeCount = this._d.configMemoryWrite(sm.node, address, buffer.length, buffer);
      if (writeCount < buffer.length) {
        loadConfigMemReplyWriteFailHeader(sm, req, ERROR_TEMPORARY_TRANSFER_ERROR);
      }
      sm.outgoing.valid = true;
      return writeCount;
    }
    _writeDataUnderMask(sm, req) {
      if (!this._d.configMemoryRead) {
        loadConfigMemReplyWriteFailHeader(sm, req, ERROR_PERMANENT_INVALID_ARGUMENTS);
        sm.outgoing.valid = true;
        return 0;
      }
      const current = new Uint8Array(req.bytes);
      const readCount = this._d.configMemoryRead(sm.node, req.address, req.bytes, current);
      if (readCount < req.bytes) {
        loadConfigMemReplyWriteFailHeader(sm, req, ERROR_TEMPORARY_TRANSFER_ERROR);
        sm.outgoing.valid = true;
        return 0;
      }
      const pairs = req.writeBuffer;
      for (let i = 0; i < req.bytes; i++) {
        const mask = pairs[i * 2];
        const data = pairs[i * 2 + 1];
        current[i] = current[i] & ~mask | data & mask;
      }
      if (!this._d.configMemoryWrite) {
        loadConfigMemReplyWriteFailHeader(sm, req, ERROR_PERMANENT_INVALID_ARGUMENTS);
        sm.outgoing.valid = true;
        return 0;
      }
      const writeCount = this._d.configMemoryWrite(sm.node, req.address, req.bytes, current);
      if (writeCount < req.bytes) {
        loadConfigMemReplyWriteFailHeader(sm, req, ERROR_TEMPORARY_TRANSFER_ERROR);
      }
      sm.outgoing.valid = true;
      return writeCount;
    }
  };

  // src/protocol/config-mem-operations.js
  function decodeSpaceInfo(sm, offset) {
    const byte = sm.incoming.msg.payload[offset];
    const p = sm.node.parameters;
    switch (byte) {
      case CONFIG_MEM_SPACE_CONFIGURATION_DEFINITION_INFO:
        return p.addressSpaceConfigurationDefinitionInfo;
      case CONFIG_MEM_SPACE_ALL:
        return p.addressSpaceAll;
      case CONFIG_MEM_SPACE_CONFIGURATION_MEMORY:
        return p.addressSpaceConfigMemory;
      case CONFIG_MEM_SPACE_ACDI_MANUFACTURER_ACCESS:
        return p.addressSpaceAcdiManufacturer;
      case CONFIG_MEM_SPACE_ACDI_USER_ACCESS:
        return p.addressSpaceAcdiUser;
      case CONFIG_MEM_SPACE_TRAIN_FUNCTION_DEFINITION_INFO:
        return p.addressSpaceTrainFunctionDefinitionInfo;
      case CONFIG_MEM_SPACE_TRAIN_FUNCTION_CONFIGURATION_MEMORY:
        return p.addressSpaceTrainFunctionConfigMemory;
      case CONFIG_MEM_SPACE_FIRMWARE:
        return p.addressSpaceFirmware;
      default:
        return null;
    }
  }
  function loadReplyHeader(sm) {
    sm.outgoing.msg.payloadCount = 0;
    loadOpenlcbMessage(
      sm.outgoing.msg,
      sm.node.alias,
      sm.node.id,
      sm.incoming.msg.sourceAlias,
      sm.incoming.msg.sourceId,
      MTI_DATAGRAM
    );
    copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_CONFIGURATION, 0);
    sm.outgoing.valid = false;
  }
  var ProtocolConfigMemOperations = class {
    /**
     * @param {Object} deps
     * @param {(sm, seconds) => void}    deps.loadDatagramReceivedOk required
     * @param {(sm, errorCode) => void}  deps.loadDatagramRejected   required
     * @param {Object} [deps.overrides] optional per-op handler overrides; keys:
     *                                  optionsCmd, getAddressSpaceInfo, unfreeze,
     *                                  freeze, reserveLock, getUniqueId,
     *                                  updateComplete, factoryReset, resetReboot,
     *                                  and `...Reply` variants.
     * @param {() => void} [deps.cleanupBeforeHandoff]  called pre-reboot / pre-firmware-handoff
     */
    constructor(deps = {}) {
      this._d = deps;
    }
    // -------------------------------------------------------------------------
    // Shared two-phase driver
    // -------------------------------------------------------------------------
    _runTwoPhase(sm, handler) {
      if (!sm.node.state.openlcbDatagramAckSent) {
        if (handler) {
          this._d.loadDatagramReceivedOk(sm, 0);
          sm.node.state.openlcbDatagramAckSent = true;
          sm.incoming.enumerate = true;
        } else {
          this._d.loadDatagramRejected(sm, ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN);
          sm.node.state.openlcbDatagramAckSent = false;
          sm.incoming.enumerate = false;
        }
        return;
      }
      handler(sm);
      sm.node.state.openlcbDatagramAckSent = false;
      sm.incoming.enumerate = false;
    }
    // -------------------------------------------------------------------------
    // Default handlers
    // -------------------------------------------------------------------------
    _defaultOptionsCmd(sm) {
      const params = sm.node.parameters;
      const opts = params.configurationOptions ?? {};
      let commandFlags = 0;
      if (opts.writeUnderMaskSupported) commandFlags |= CONFIG_OPTIONS_COMMANDS_WRITE_UNDER_MASK;
      if (opts.unalignedReadsSupported) commandFlags |= CONFIG_OPTIONS_COMMANDS_UNALIGNED_READS;
      if (opts.unalignedWritesSupported) commandFlags |= CONFIG_OPTIONS_COMMANDS_UNALIGNED_WRITES;
      if (opts.readFromManufacturerSpace0xFcSupported) commandFlags |= CONFIG_OPTIONS_COMMANDS_ACDI_MANUFACTURER_READ;
      if (opts.readFromUserSpace0xFbSupported) commandFlags |= CONFIG_OPTIONS_COMMANDS_ACDI_USER_READ;
      if (opts.writeToUserSpace0xFbSupported) commandFlags |= CONFIG_OPTIONS_COMMANDS_ACDI_USER_WRITE;
      let writeFlags = CONFIG_OPTIONS_WRITE_LENGTH_RESERVED;
      if (opts.streamReadWriteSupported) writeFlags |= CONFIG_OPTIONS_WRITE_LENGTH_STREAM_READ_WRITE;
      loadReplyHeader(sm);
      copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_OPTIONS_REPLY, 1);
      copyWordToPayload(sm.outgoing.msg, commandFlags, 2);
      copyByteToPayload(sm.outgoing.msg, writeFlags, 4);
      copyByteToPayload(sm.outgoing.msg, opts.highAddressSpace ?? 255, 5);
      copyByteToPayload(sm.outgoing.msg, opts.lowAddressSpace ?? 251, 6);
      const desc = opts.description ?? "";
      if (desc.length > 0) {
        copyStringToPayload(sm.outgoing.msg, desc, sm.outgoing.msg.payloadCount);
      }
      sm.outgoing.valid = true;
    }
    _defaultGetAddressSpaceInfo(sm) {
      const spaceInfo = decodeSpaceInfo(sm, 2);
      loadReplyHeader(sm);
      if (spaceInfo && spaceInfo.present) {
        copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_PRESENT, 1);
        copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[2], 2);
        copyDwordToPayload(sm.outgoing.msg, spaceInfo.highestAddress, 3);
        let flags = 0;
        if (spaceInfo.readOnly) flags |= CONFIG_OPTIONS_SPACE_INFO_FLAG_READ_ONLY;
        if (spaceInfo.lowAddressValid) flags |= CONFIG_OPTIONS_SPACE_INFO_FLAG_USE_LOW_ADDRESS;
        copyByteToPayload(sm.outgoing.msg, flags, 7);
        let descOffset = 8;
        if (spaceInfo.lowAddressValid) {
          copyDwordToPayload(sm.outgoing.msg, spaceInfo.lowAddress, 8);
          descOffset = 12;
        }
        if (spaceInfo.description && spaceInfo.description.length > 0) {
          copyStringToPayload(sm.outgoing.msg, spaceInfo.description, descOffset);
        }
      } else {
        copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_GET_ADDRESS_SPACE_INFO_REPLY_NOT_PRESENT, 1);
        copyByteToPayload(sm.outgoing.msg, sm.incoming.msg.payload[2], 2);
        sm.outgoing.msg.payloadCount = 8;
      }
      sm.outgoing.valid = true;
    }
    _defaultReserveLock(sm) {
      const newNodeId = extractNodeIdFromPayload(sm.incoming.msg, 2);
      if (sm.node.ownerNode === 0n) {
        sm.node.ownerNode = newNodeId;
      } else if (newNodeId === 0n || newNodeId === sm.node.ownerNode) {
        sm.node.ownerNode = 0n;
      }
      loadReplyHeader(sm);
      copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_RESERVE_LOCK_REPLY, 1);
      copyNodeIdToPayload(sm.outgoing.msg, sm.node.ownerNode, 2);
      sm.outgoing.valid = true;
    }
    // -------------------------------------------------------------------------
    // Public entry points
    // -------------------------------------------------------------------------
    optionsCmd(sm) {
      this._runTwoPhase(sm, this._d.overrides?.optionsCmd ?? this._defaultOptionsCmd.bind(this));
    }
    optionsReply(sm) {
      this._runTwoPhase(sm, this._d.overrides?.optionsReply ?? null);
    }
    getAddressSpaceInfo(sm) {
      this._runTwoPhase(sm, this._d.overrides?.getAddressSpaceInfo ?? this._defaultGetAddressSpaceInfo.bind(this));
    }
    getAddressSpaceInfoReplyNotPresent(sm) {
      this._runTwoPhase(sm, this._d.overrides?.getAddressSpaceInfoReplyNotPresent ?? null);
    }
    getAddressSpaceInfoReplyPresent(sm) {
      this._runTwoPhase(sm, this._d.overrides?.getAddressSpaceInfoReplyPresent ?? null);
    }
    unfreeze(sm) {
      this._runTwoPhase(sm, this._d.overrides?.unfreeze ?? null);
    }
    freeze(sm) {
      const spaceInfo = decodeSpaceInfo(sm, 2);
      const handler = this._d.overrides?.freeze ?? null;
      if (sm.node.state.openlcbDatagramAckSent && spaceInfo && spaceInfo.addressSpace === CONFIG_MEM_SPACE_FIRMWARE && this._d.cleanupBeforeHandoff) {
        this._d.cleanupBeforeHandoff();
      }
      this._runTwoPhase(sm, handler);
    }
    reserveLock(sm) {
      this._runTwoPhase(sm, this._d.overrides?.reserveLock ?? this._defaultReserveLock.bind(this));
    }
    reserveLockReply(sm) {
      this._runTwoPhase(sm, this._d.overrides?.reserveLockReply ?? null);
    }
    getUniqueId(sm) {
      this._runTwoPhase(sm, this._d.overrides?.getUniqueId ?? null);
    }
    getUniqueIdReply(sm) {
      this._runTwoPhase(sm, this._d.overrides?.getUniqueIdReply ?? null);
    }
    updateComplete(sm) {
      this._runTwoPhase(sm, this._d.overrides?.updateComplete ?? null);
    }
    factoryReset(sm) {
      this._runTwoPhase(sm, this._d.overrides?.factoryReset ?? null);
    }
    /**
     * Reset/Reboot — no ACK; the subsequent Initialization Complete serves as
     * acknowledgment per MemoryConfigurationS §4.24.
     */
    resetReboot(sm) {
      const handler = this._d.overrides?.resetReboot ?? null;
      if (!handler) {
        this._d.loadDatagramRejected(sm, ERROR_PERMANENT_NOT_IMPLEMENTED_SUBCOMMAND_UNKNOWN);
        return;
      }
      sm.outgoing.valid = false;
      if (this._d.cleanupBeforeHandoff) this._d.cleanupBeforeHandoff();
      handler(sm);
      sm.node.state.openlcbDatagramAckSent = false;
      sm.incoming.enumerate = false;
    }
  };

  // src/protocol/stream-handler.js
  var STREAM_STATE = Object.freeze({
    CLOSED: 0,
    INITIATED: 1,
    OPEN: 2
  });
  var ProtocolStreamHandler = class {
    /**
     * @param {Object} [deps]
     * @param {number} [deps.maxConcurrentStreams=4]
     * @param {(sm, stream) => boolean} [deps.onInitiateRequest]
     * @param {(sm, stream) => void}    [deps.onInitiateReply]
     * @param {(sm, stream) => void}    [deps.onDataReceived]
     * @param {(sm, stream) => void}    [deps.onDataProceed]
     * @param {(sm, stream) => void}    [deps.onComplete]
     */
    constructor(deps = {}) {
      this._d = deps;
      this._table = new Array(deps.maxConcurrentStreams ?? 4).fill(null).map(() => ({
        state: STREAM_STATE.CLOSED,
        sourceStreamId: 0,
        destStreamId: 0,
        remoteNodeId: 0n,
        remoteAlias: 0,
        maxBufferSize: 0,
        bytesTransferred: 0,
        bytesRemaining: 0,
        isSource: false,
        contentUid: new Uint8Array(6),
        context: null
      }));
      this._nextDestStreamId = 0;
      this._nextSourceStreamId = 0;
    }
    // -------------------------------------------------------------------------
    // Stream table management
    // -------------------------------------------------------------------------
    _allocateStream() {
      for (const s of this._table) {
        if (s.state === STREAM_STATE.CLOSED) return s;
      }
      return null;
    }
    _freeStream(stream) {
      stream.state = STREAM_STATE.CLOSED;
      stream.remoteNodeId = 0n;
      stream.remoteAlias = 0;
      stream.sourceStreamId = 0;
      stream.destStreamId = 0;
      stream.bytesTransferred = 0;
      stream.bytesRemaining = 0;
      stream.maxBufferSize = 0;
      stream.isSource = false;
      stream.contentUid.fill(0);
      stream.context = null;
    }
    /**
     * Find a stream by (remoteAlias, streamId). Matches on alias rather than
     * node ID because the CAN RX layer leaves msg.sourceId=0n on stream and
     * addressed single-frame messages — only sourceAlias is guaranteed
     * populated. `matchSourceId` selects source vs dest stream ID.
     */
    _findStream(remoteAlias, streamId, matchSourceId) {
      for (const s of this._table) {
        if (s.state === STREAM_STATE.CLOSED) continue;
        if (s.remoteAlias !== remoteAlias) continue;
        const id = matchSourceId ? s.sourceStreamId : s.destStreamId;
        if (id === streamId) return s;
      }
      return null;
    }
    _assignDestStreamId() {
      const id = this._nextDestStreamId;
      this._nextDestStreamId = this._nextDestStreamId + 1 >= STREAM_ID_RESERVED ? 0 : this._nextDestStreamId + 1;
      return id;
    }
    _assignSourceStreamId() {
      const id = this._nextSourceStreamId;
      this._nextSourceStreamId = this._nextSourceStreamId + 1 >= STREAM_ID_RESERVED ? 0 : this._nextSourceStreamId + 1;
      return id;
    }
    // -------------------------------------------------------------------------
    // Outgoing message builders
    // -------------------------------------------------------------------------
    _loadInitiateReply(sm, maxBufferSize, flagsOrError, sourceStreamId, destStreamId) {
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        sm.incoming.msg.sourceAlias,
        sm.incoming.msg.sourceId,
        MTI_STREAM_INIT_REPLY
      );
      clearOpenlcbMessagePayload(sm.outgoing.msg);
      copyWordToPayload(sm.outgoing.msg, maxBufferSize, 0);
      copyWordToPayload(sm.outgoing.msg, flagsOrError, 2);
      copyByteToPayload(sm.outgoing.msg, sourceStreamId, 4);
      copyByteToPayload(sm.outgoing.msg, destStreamId, 5);
      sm.outgoing.msg.payloadCount = 6;
      sm.outgoing.valid = true;
    }
    _loadInitiateRequest(sm, proposedBufferSize, sourceStreamId, suggestedDestStreamId, destAlias, destId, contentUid) {
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        destAlias,
        destId,
        MTI_STREAM_INIT_REQUEST
      );
      clearOpenlcbMessagePayload(sm.outgoing.msg);
      copyWordToPayload(sm.outgoing.msg, proposedBufferSize, 0);
      copyWordToPayload(sm.outgoing.msg, 0, 2);
      copyByteToPayload(sm.outgoing.msg, sourceStreamId, 4);
      if (contentUid) {
        copyByteToPayload(sm.outgoing.msg, suggestedDestStreamId, 5);
        for (let i = 0; i < 6; i++) copyByteToPayload(sm.outgoing.msg, contentUid[i], 6 + i);
        sm.outgoing.msg.payloadCount = 12;
      } else if (suggestedDestStreamId !== STREAM_ID_RESERVED) {
        copyByteToPayload(sm.outgoing.msg, suggestedDestStreamId, 5);
        sm.outgoing.msg.payloadCount = 6;
      } else {
        sm.outgoing.msg.payloadCount = 5;
      }
      sm.outgoing.valid = true;
    }
    _loadDataProceed(sm, stream) {
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        sm.incoming.msg.sourceAlias,
        sm.incoming.msg.sourceId,
        MTI_STREAM_PROCEED
      );
      clearOpenlcbMessagePayload(sm.outgoing.msg);
      copyByteToPayload(sm.outgoing.msg, stream.sourceStreamId, 0);
      copyByteToPayload(sm.outgoing.msg, stream.destStreamId, 1);
      sm.outgoing.msg.payloadCount = 2;
      sm.outgoing.valid = true;
    }
    _loadDataSend(sm, stream, data, dataLen) {
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        stream.remoteAlias,
        stream.remoteNodeId,
        MTI_STREAM_SEND
      );
      clearOpenlcbMessagePayload(sm.outgoing.msg);
      copyByteToPayload(sm.outgoing.msg, stream.destStreamId, 0);
      for (let i = 0; i < dataLen; i++) copyByteToPayload(sm.outgoing.msg, data[i], 1 + i);
      sm.outgoing.msg.payloadCount = 1 + dataLen;
      sm.outgoing.valid = true;
    }
    _loadDataComplete(sm, stream) {
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        stream.remoteAlias,
        stream.remoteNodeId,
        MTI_STREAM_COMPLETE
      );
      clearOpenlcbMessagePayload(sm.outgoing.msg);
      copyByteToPayload(sm.outgoing.msg, stream.sourceStreamId, 0);
      copyByteToPayload(sm.outgoing.msg, stream.destStreamId, 1);
      if (stream.bytesTransferred > 0) {
        copyWordToPayload(sm.outgoing.msg, 0, 2);
        copyByteToPayload(sm.outgoing.msg, stream.bytesTransferred >>> 16 & 255, 4);
        copyByteToPayload(sm.outgoing.msg, stream.bytesTransferred >>> 8 & 255, 5);
        copyByteToPayload(sm.outgoing.msg, stream.bytesTransferred & 255, 6);
        sm.outgoing.msg.payloadCount = 7;
      } else {
        sm.outgoing.msg.payloadCount = 2;
      }
      sm.outgoing.valid = true;
    }
    _loadTerminateDueToError(sm, stream, errorCode, rejectedMti) {
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        stream.remoteAlias,
        stream.remoteNodeId,
        MTI_TERMINATE_DUE_TO_ERROR
      );
      clearOpenlcbMessagePayload(sm.outgoing.msg);
      copyWordToPayload(sm.outgoing.msg, errorCode, 0);
      copyWordToPayload(sm.outgoing.msg, rejectedMti, 2);
      sm.outgoing.msg.payloadCount = 4;
      sm.outgoing.valid = true;
    }
    // -------------------------------------------------------------------------
    // Incoming message handlers (public MTI entry points)
    // -------------------------------------------------------------------------
    initiateRequest(sm) {
      const msg = sm.incoming.msg;
      const proposedBufferSize = extractWordFromPayload(msg, 0);
      const sourceStreamId = extractByteFromPayload(msg, 4);
      if (!this._d.onInitiateRequest) {
        this._loadInitiateReply(sm, 0, ERROR_PERMANENT_STREAMS_NOT_SUPPORTED, sourceStreamId, 0);
        return;
      }
      const stream = this._allocateStream();
      if (!stream) {
        this._loadInitiateReply(sm, 0, ERROR_TEMPORARY_BUFFER_UNAVAILABLE, sourceStreamId, 0);
        return;
      }
      stream.state = STREAM_STATE.INITIATED;
      stream.sourceStreamId = sourceStreamId;
      stream.destStreamId = this._assignDestStreamId();
      stream.remoteNodeId = msg.sourceId;
      stream.remoteAlias = msg.sourceAlias;
      stream.isSource = false;
      stream.bytesTransferred = 0;
      stream.maxBufferSize = Math.min(proposedBufferSize, LEN_MESSAGE_BYTES_STREAM);
      stream.bytesRemaining = stream.maxBufferSize;
      if (msg.payloadCount >= 12) {
        for (let i = 0; i < 6; i++) stream.contentUid[i] = extractByteFromPayload(msg, 6 + i);
      }
      const accepted = this._d.onInitiateRequest(sm, stream);
      if (accepted) {
        stream.state = STREAM_STATE.OPEN;
        this._loadInitiateReply(sm, stream.maxBufferSize, STREAM_REPLY_ACCEPT, stream.sourceStreamId, stream.destStreamId);
      } else {
        const sid = stream.sourceStreamId;
        this._freeStream(stream);
        this._loadInitiateReply(sm, 0, ERROR_PERMANENT_STREAMS_NOT_SUPPORTED, sid, 0);
      }
    }
    initiateReply(sm) {
      const msg = sm.incoming.msg;
      const negotiatedBufferSize = extractWordFromPayload(msg, 0);
      const flags = extractWordFromPayload(msg, 2);
      const sourceStreamId = extractByteFromPayload(msg, 4);
      const destStreamId = extractByteFromPayload(msg, 5);
      const stream = this._findStream(msg.sourceAlias, sourceStreamId, true);
      if (!stream) return;
      const accepted = negotiatedBufferSize > 0 && ((flags & STREAM_REPLY_ACCEPT) !== 0 || flags === 0);
      if (accepted) {
        stream.state = STREAM_STATE.OPEN;
        stream.destStreamId = destStreamId;
        stream.maxBufferSize = negotiatedBufferSize;
        stream.bytesRemaining = negotiatedBufferSize;
        this._d.onInitiateReply?.(sm, stream);
      } else {
        this._d.onInitiateReply?.(sm, stream);
        this._freeStream(stream);
      }
    }
    dataSend(sm) {
      const msg = sm.incoming.msg;
      const destStreamId = extractByteFromPayload(msg, 0);
      const stream = this._findStream(msg.sourceAlias, destStreamId, false);
      if (!stream || stream.state !== STREAM_STATE.OPEN) return;
      const dataLen = msg.payloadCount > 1 ? msg.payloadCount - 1 : 0;
      stream.bytesTransferred += dataLen;
      stream.bytesRemaining = Math.max(0, stream.bytesRemaining - dataLen);
      this._d.onDataReceived?.(sm, stream);
      if (stream.bytesRemaining === 0) {
        stream.bytesRemaining = stream.maxBufferSize;
        this._loadDataProceed(sm, stream);
      }
    }
    dataProceed(sm) {
      const msg = sm.incoming.msg;
      const sourceStreamId = extractByteFromPayload(msg, 0);
      const stream = this._findStream(msg.sourceAlias, sourceStreamId, true);
      if (!stream || stream.state !== STREAM_STATE.OPEN) return;
      stream.bytesRemaining += stream.maxBufferSize;
      this._d.onDataProceed?.(sm, stream);
    }
    dataComplete(sm) {
      const msg = sm.incoming.msg;
      const sourceStreamId = extractByteFromPayload(msg, 0);
      const destStreamId = extractByteFromPayload(msg, 1);
      let stream = this._findStream(msg.sourceAlias, sourceStreamId, true);
      if (!stream) stream = this._findStream(msg.sourceAlias, destStreamId, false);
      if (!stream) return;
      this._d.onComplete?.(sm, stream);
      this._freeStream(stream);
    }
    handleTerminateDueToError(sm) {
      const msg = sm.incoming.msg;
      if (msg.payloadCount < 4) return;
      const rejectedMti = extractWordFromPayload(msg, 2);
      if (rejectedMti !== MTI_STREAM_SEND && rejectedMti !== MTI_STREAM_PROCEED) return;
      for (const stream of this._table) {
        if (stream.state === STREAM_STATE.CLOSED) continue;
        if (stream.remoteNodeId !== msg.sourceId) continue;
        this._d.onComplete?.(sm, stream);
        this._freeStream(stream);
      }
    }
    // -------------------------------------------------------------------------
    // Source-side outbound API
    // -------------------------------------------------------------------------
    initiateOutbound(sm, destAlias, destId, proposedBufferSize, suggestedDestStreamId = STREAM_ID_RESERVED, contentUid = null) {
      const stream = this._allocateStream();
      if (!stream) return null;
      stream.state = STREAM_STATE.INITIATED;
      stream.sourceStreamId = this._assignSourceStreamId();
      stream.destStreamId = 0;
      stream.remoteNodeId = destId;
      stream.remoteAlias = destAlias;
      stream.isSource = true;
      stream.maxBufferSize = proposedBufferSize;
      stream.bytesTransferred = 0;
      stream.bytesRemaining = 0;
      if (contentUid) {
        for (let i = 0; i < 6; i++) stream.contentUid[i] = contentUid[i];
      }
      this._loadInitiateRequest(sm, proposedBufferSize, stream.sourceStreamId, suggestedDestStreamId, destAlias, destId, contentUid);
      return stream;
    }
    sendData(sm, stream, data, dataLen) {
      if (stream.state !== STREAM_STATE.OPEN || !stream.isSource) return false;
      if (dataLen > stream.bytesRemaining) return false;
      this._loadDataSend(sm, stream, data, dataLen);
      stream.bytesTransferred += dataLen;
      stream.bytesRemaining -= dataLen;
      return true;
    }
    sendComplete(sm, stream) {
      this._loadDataComplete(sm, stream);
      this._freeStream(stream);
    }
    sendTerminate(sm, stream, errorCode) {
      const rejectedMti = stream.isSource ? MTI_STREAM_PROCEED : MTI_STREAM_SEND;
      this._loadTerminateDueToError(sm, stream, errorCode, rejectedMti);
      this._freeStream(stream);
    }
    sendEarlyProceed(sm, stream) {
      if (stream.state !== STREAM_STATE.OPEN || stream.isSource) return false;
      this._loadDataProceed(sm, stream);
      return true;
    }
  };

  // src/protocol/config-mem-stream.js
  var CONFIG_MEM_STREAM_PHASE = Object.freeze({
    IDLE: 0,
    ALLOCATED: 1,
    WAIT_INITIATE_REPLY: 2,
    SEND_REPLY_DATAGRAM: 3,
    PUMPING: 4,
    SEND_COMPLETE: 5,
    WRITE_WAIT_STREAM_INITIATE: 6,
    WRITE_RECEIVING: 7,
    WRITE_SEND_REPLY: 8
  });
  var ProtocolConfigMemStream = class {
    /**
     * @param {Object} deps
     * @param {ProtocolStreamHandler} deps.streamHandler required
     * @param {(sm, seconds) => void} deps.loadDatagramReceivedOk required
     * @param {(sm, errorCode) => void} deps.loadDatagramRejected required
     * @param {Object} [deps.readCallbacks]  per-space reads keyed by space-name
     *                                       (e.g. configurationMemory, firmwareUpgrade)
     * @param {Object} [deps.writeCallbacks] per-space writes keyed by space-name
     * @param {number} [deps.maxConcurrentStreams=2]
     */
    constructor(deps) {
      this._d = deps;
      this._table = new Array(deps.maxConcurrentStreams ?? 2).fill(null).map(() => ({
        phase: CONFIG_MEM_STREAM_PHASE.IDLE,
        stream: null,
        node: null,
        spaceInfo: null,
        address: 0,
        remainingBytes: 0,
        totalBytes: 0,
        replyEncoding: ADDRESS_SPACE_ENCODING.IN_BYTE_6,
        replyOrigCmd: 0,
        replySpaceByte: 0,
        isWrite: false,
        readFunc: null,
        writeFunc: null
      }));
    }
    _allocContext() {
      for (const c of this._table) {
        if (c.phase === CONFIG_MEM_STREAM_PHASE.IDLE) return c;
      }
      return null;
    }
    _freeContext(ctx) {
      ctx.phase = CONFIG_MEM_STREAM_PHASE.IDLE;
      ctx.stream = null;
      ctx.node = null;
      ctx.spaceInfo = null;
      ctx.address = 0;
      ctx.remainingBytes = 0;
      ctx.totalBytes = 0;
      ctx.readFunc = null;
      ctx.writeFunc = null;
    }
    // -------------------------------------------------------------------------
    // Incoming datagram entry points (wired into the datagram dispatcher)
    // -------------------------------------------------------------------------
    readStreamSpaceConfigDescriptionInfo(sm) {
      this._startRead(sm, "configDescriptionInfo");
    }
    readStreamSpaceAll(sm) {
      this._startRead(sm, "all");
    }
    readStreamSpaceConfigMemory(sm) {
      this._startRead(sm, "configurationMemory");
    }
    readStreamSpaceAcdiManufacturer(sm) {
      this._startRead(sm, "acdiManufacturer");
    }
    readStreamSpaceAcdiUser(sm) {
      this._startRead(sm, "acdiUser");
    }
    readStreamSpaceTrainFunctionDefinitionInfo(sm) {
      this._startRead(sm, "trainFunctionDefinitionInfo");
    }
    readStreamSpaceTrainFunctionConfigMemory(sm) {
      this._startRead(sm, "trainFunctionConfigMemory");
    }
    writeStreamSpaceConfigMemory(sm) {
      this._startWrite(sm, "configurationMemory");
    }
    writeStreamSpaceAll(sm) {
      this._startWrite(sm, "all");
    }
    writeStreamSpaceConfigDescriptionInfo(sm) {
      this._startWrite(sm, "configDescriptionInfo");
    }
    writeStreamSpaceAcdiManufacturer(sm) {
      this._startWrite(sm, "acdiManufacturer");
    }
    writeStreamSpaceAcdiUser(sm) {
      this._startWrite(sm, "acdiUser");
    }
    writeStreamSpaceTrainFunctionDefinitionInfo(sm) {
      this._startWrite(sm, "trainFunctionDefinitionInfo");
    }
    writeStreamSpaceTrainFunctionConfigMemory(sm) {
      this._startWrite(sm, "trainFunctionConfigMemory");
    }
    writeStreamSpaceFirmware(sm) {
      this._startWrite(sm, "firmwareUpgrade");
    }
    // -------------------------------------------------------------------------
    // Read startup (server side: this node is the source; requester is dest)
    // -------------------------------------------------------------------------
    _startRead(sm, spaceKey) {
      const node = sm.node;
      if (!node.state.openlcbDatagramAckSent) {
        const msg = sm.incoming.msg;
        const inByte6 = msg.payload[1] === CONFIG_MEM_READ_STREAM_SPACE_IN_BYTE_6;
        const lengthStart = inByte6 ? 9 : 8;
        const destStreamIdOff = inByte6 ? 8 : 7;
        const address = extractDwordFromPayload(msg, 2);
        let bytes = msg.payloadCount >= lengthStart + 4 ? extractDwordFromPayload(msg, lengthStart) : 0;
        const suggestedDestStreamId = msg.payloadCount > destStreamIdOff ? msg.payload[destStreamIdOff] : STREAM_ID_RESERVED;
        const spaceInfo = this._spaceInfoByKey(spaceKey, node.parameters);
        if (!spaceInfo || !spaceInfo.present) {
          this._d.loadDatagramRejected(sm, ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN);
          return;
        }
        if (address > spaceInfo.highestAddress) {
          this._d.loadDatagramRejected(sm, ERROR_PERMANENT_CONFIG_MEM_OUT_OF_BOUNDS_INVALID_ADDRESS);
          return;
        }
        if (bytes === 0) {
          bytes = spaceInfo.highestAddress - address + 1 >>> 0;
        }
        const ctx2 = this._allocContext();
        if (!ctx2) {
          this._d.loadDatagramRejected(sm, ERROR_TEMPORARY_TRANSFER_ERROR);
          return;
        }
        ctx2.phase = CONFIG_MEM_STREAM_PHASE.ALLOCATED;
        ctx2.node = node;
        ctx2.spaceInfo = spaceInfo;
        ctx2.address = address;
        ctx2.remainingBytes = bytes;
        ctx2.totalBytes = bytes;
        ctx2.readFunc = this._d.readCallbacks?.[spaceKey] ?? null;
        ctx2.replyEncoding = inByte6 ? ADDRESS_SPACE_ENCODING.IN_BYTE_6 : ADDRESS_SPACE_ENCODING.IN_BYTE_1;
        ctx2.replyOrigCmd = msg.payload[1];
        ctx2.replySpaceByte = inByte6 ? msg.payload[6] : 0;
        ctx2.remoteAlias = msg.sourceAlias;
        ctx2.remoteNodeId = msg.sourceId;
        ctx2.suggestedDestStreamId = suggestedDestStreamId;
        this._d.loadDatagramReceivedOk(sm, 0);
        node.state.openlcbDatagramAckSent = true;
        sm.incoming.enumerate = true;
        return;
      }
      const ctx = this._findAllocatedContext(node);
      node.state.openlcbDatagramAckSent = false;
      sm.incoming.enumerate = false;
      if (!ctx) return;
      const stream = this._d.streamHandler.initiateOutbound(
        sm,
        ctx.remoteAlias,
        ctx.remoteNodeId,
        512,
        // proposed buffer size
        ctx.suggestedDestStreamId ?? STREAM_ID_RESERVED,
        null
      );
      if (!stream) {
        this._freeContext(ctx);
        return;
      }
      ctx.stream = stream;
      stream.context = ctx;
      ctx.phase = CONFIG_MEM_STREAM_PHASE.WAIT_INITIATE_REPLY;
    }
    _findAllocatedContext(node) {
      for (const c of this._table) {
        if (c.phase === CONFIG_MEM_STREAM_PHASE.ALLOCATED && c.node === node) return c;
      }
      return null;
    }
    // -------------------------------------------------------------------------
    // Write startup (server side: this node is the destination; requester is source)
    // -------------------------------------------------------------------------
    _startWrite(sm, spaceKey) {
      const msg = sm.incoming.msg;
      const inByte6 = msg.payload[1] === CONFIG_MEM_WRITE_STREAM_SPACE_IN_BYTE_6;
      const dataStart = inByte6 ? 7 : 6;
      const address = extractDwordFromPayload(msg, 2);
      const spaceInfo = this._spaceInfoByKey(spaceKey, sm.node.parameters);
      if (!spaceInfo || !spaceInfo.present) {
        this._d.loadDatagramRejected(sm, ERROR_PERMANENT_CONFIG_MEM_ADDRESS_SPACE_UNKNOWN);
        return;
      }
      if (spaceInfo.readOnly && spaceKey !== "firmwareUpgrade") {
        this._d.loadDatagramRejected(sm, ERROR_PERMANENT_CONFIG_MEM_ADDRESS_WRITE_TO_READ_ONLY);
        return;
      }
      if (address > spaceInfo.highestAddress) {
        this._d.loadDatagramRejected(sm, ERROR_PERMANENT_CONFIG_MEM_OUT_OF_BOUNDS_INVALID_ADDRESS);
        return;
      }
      const ctx = this._allocContext();
      if (!ctx) {
        this._d.loadDatagramRejected(sm, ERROR_TEMPORARY_TRANSFER_ERROR);
        return;
      }
      ctx.phase = CONFIG_MEM_STREAM_PHASE.WRITE_WAIT_STREAM_INITIATE;
      ctx.node = sm.node;
      ctx.spaceInfo = spaceInfo;
      ctx.address = address;
      ctx.isWrite = true;
      ctx.writeFunc = this._d.writeCallbacks?.[spaceKey] ?? null;
      ctx.replyEncoding = inByte6 ? ADDRESS_SPACE_ENCODING.IN_BYTE_6 : ADDRESS_SPACE_ENCODING.IN_BYTE_1;
      ctx.replyOrigCmd = msg.payload[1];
      ctx.replySpaceByte = inByte6 ? msg.payload[6] : 0;
      this._d.loadDatagramReceivedOk(sm, 0);
    }
    _spaceInfoByKey(key, params) {
      switch (key) {
        case "configDescriptionInfo":
          return params.addressSpaceConfigurationDefinitionInfo;
        case "all":
          return params.addressSpaceAll;
        case "configurationMemory":
          return params.addressSpaceConfigMemory;
        case "acdiManufacturer":
          return params.addressSpaceAcdiManufacturer;
        case "acdiUser":
          return params.addressSpaceAcdiUser;
        case "trainFunctionDefinitionInfo":
          return params.addressSpaceTrainFunctionDefinitionInfo;
        case "trainFunctionConfigMemory":
          return params.addressSpaceTrainFunctionConfigMemory;
        case "firmwareUpgrade":
          return params.addressSpaceFirmware;
        default:
          return null;
      }
    }
    // -------------------------------------------------------------------------
    // Stream-handler callback hooks — must be wired into the stream handler
    // via deps.onInitiateReply, deps.onDataReceived, deps.onDataProceed, deps.onComplete
    // -------------------------------------------------------------------------
    /**
     * Called from config.js's default `onStreamInitiateRequest` hook. If a
     * pending write context is waiting for the peer's Stream Initiate Request
     * (phase = WRITE_WAIT_STREAM_INITIATE) and the incoming request is from
     * the same remote node, bind the stream to the context, move to
     * WRITE_RECEIVING, and return the context so the caller can accept.
     * Returns null otherwise — caller should fall through to user callback.
     */
    claimWriteInitiate(sm, stream) {
      const remoteId = sm.incoming.msg.sourceId;
      for (const ctx of this._table) {
        if (ctx.phase !== CONFIG_MEM_STREAM_PHASE.WRITE_WAIT_STREAM_INITIATE) continue;
        if (ctx.node && ctx.node.id !== sm.node.id) continue;
        ctx.stream = stream;
        ctx.phase = CONFIG_MEM_STREAM_PHASE.WRITE_RECEIVING;
        void remoteId;
        return ctx;
      }
      return null;
    }
    /** Call from stream handler's onInitiateReply for a config-mem-stream read. */
    onInitiateReply(sm, stream) {
      const ctx = stream.context;
      if (!ctx || !this._ownsContext(ctx)) return;
      if (ctx.phase !== CONFIG_MEM_STREAM_PHASE.WAIT_INITIATE_REPLY) return;
      ctx.startAddress = ctx.startAddress ?? ctx.address;
      ctx.phase = CONFIG_MEM_STREAM_PHASE.SEND_REPLY_DATAGRAM;
    }
    /** Pump pending streams — called from the main run loop each tick. */
    pump(sm) {
      for (const ctx of this._table) {
        if (ctx.phase === CONFIG_MEM_STREAM_PHASE.IDLE) continue;
        if (sm.outgoing.valid) return;
        this._pumpOne(sm, ctx);
      }
    }
    _pumpOne(sm, ctx) {
      const savedNode = sm.node;
      sm.node = ctx.node;
      try {
        this._pumpOneImpl(sm, ctx);
      } finally {
        sm.node = savedNode;
      }
    }
    _pumpOneImpl(sm, ctx) {
      switch (ctx.phase) {
        case CONFIG_MEM_STREAM_PHASE.SEND_REPLY_DATAGRAM:
          this._emitReadReplyOkDatagram(sm, ctx);
          ctx.phase = CONFIG_MEM_STREAM_PHASE.PUMPING;
          return;
        case CONFIG_MEM_STREAM_PHASE.PUMPING: {
          const stream = ctx.stream;
          if (!stream) return;
          if (ctx.remainingBytes === 0) {
            ctx.phase = CONFIG_MEM_STREAM_PHASE.SEND_COMPLETE;
            return;
          }
          if (stream.bytesRemaining <= 0) return;
          const maxChunk = LEN_MESSAGE_BYTES_STREAM - 1;
          const chunkSize = Math.min(ctx.remainingBytes, stream.bytesRemaining, maxChunk);
          const buffer = new Uint8Array(chunkSize);
          const readCount = ctx.readFunc ? ctx.readFunc(ctx.node, ctx.address, chunkSize, buffer) : 0;
          if (readCount <= 0) {
            this._d.streamHandler.sendTerminate(sm, stream, ERROR_TEMPORARY_TRANSFER_ERROR);
            this._emitReadReplyFailDatagram(sm, ctx, ERROR_TEMPORARY_TRANSFER_ERROR);
            this._freeContext(ctx);
            return;
          }
          const sendSize = readCount;
          this._d.streamHandler.sendData(sm, stream, buffer, sendSize);
          ctx.address += sendSize;
          if (readCount < chunkSize) {
            ctx.remainingBytes = 0;
          } else {
            ctx.remainingBytes -= sendSize;
          }
          return;
        }
        case CONFIG_MEM_STREAM_PHASE.SEND_COMPLETE:
          this._d.streamHandler.sendComplete(sm, ctx.stream);
          this._freeContext(ctx);
          return;
        case CONFIG_MEM_STREAM_PHASE.WRITE_SEND_REPLY:
          this._emitWriteReplyOkDatagram(sm, ctx);
          this._freeContext(ctx);
          return;
        default:
          return;
      }
    }
    /** Call from stream handler's onDataReceived for a config-mem-stream write. */
    onDataReceived(sm, stream) {
      const ctx = stream.context;
      if (!ctx || !this._ownsContext(ctx) || !ctx.isWrite) return;
      if (ctx.phase !== CONFIG_MEM_STREAM_PHASE.WRITE_RECEIVING) {
        ctx.phase = CONFIG_MEM_STREAM_PHASE.WRITE_RECEIVING;
      }
      const msg = sm.incoming.msg;
      const dataLen = msg.payloadCount - 1;
      if (dataLen <= 0 || !ctx.writeFunc) return;
      const data = msg.payload.subarray(1, 1 + dataLen);
      const written = ctx.writeFunc(ctx.node, ctx.address, dataLen, data);
      ctx.address += written;
    }
    /** Call from stream handler's onComplete for a config-mem-stream transfer. */
    onComplete(sm, stream) {
      const ctx = stream.context;
      if (!ctx || !this._ownsContext(ctx)) return;
      if (ctx.isWrite) {
        this._emitWriteReplyOkDatagram(sm, ctx);
      }
      this._freeContext(ctx);
    }
    _ownsContext(ctx) {
      return this._table.includes(ctx);
    }
    // -------------------------------------------------------------------------
    // Reply-datagram builders
    // -------------------------------------------------------------------------
    _emitReadReplyOkDatagram(sm, ctx) {
      this._emitReplyDatagram(sm, ctx, ctx.replyOrigCmd + CONFIG_MEM_REPLY_OK_OFFSET, null);
    }
    _emitReadReplyFailDatagram(sm, ctx, errorCode) {
      this._emitReplyDatagram(sm, ctx, ctx.replyOrigCmd + CONFIG_MEM_REPLY_FAIL_OFFSET, errorCode);
    }
    _emitWriteReplyOkDatagram(sm, ctx) {
      this._emitReplyDatagram(sm, ctx, ctx.replyOrigCmd + CONFIG_MEM_REPLY_OK_OFFSET, null);
    }
    /**
     * Read Stream Reply OK/Fail datagram. Per MemCfg §4.6 (mirrors C ref
     * protocol_config_mem_stream_handler.c `_load_reply_ok_datagram`):
     *   byte 0: 0x20
     *   byte 1: reply cmd (e.g. 0x70/0x71/0x72/0x73, +0x08 for fail)
     *   bytes 2-5: starting address (the request's, not the advanced one)
     *   byte 6 (byte-6 encoding only): space byte
     *   next byte: source stream ID (ours)
     *   next byte: destination stream ID (remote's)
     *   next 4 bytes: total byte count (from the original request)
     *   [on fail] + 2-byte error code at byte 6 (non-byte6) or 7 (byte6)
     */
    _emitReplyDatagram(sm, ctx, replyCmd, errorCode) {
      sm.outgoing.msg.payloadCount = 0;
      loadOpenlcbMessage(
        sm.outgoing.msg,
        ctx.node.alias,
        ctx.node.id,
        ctx.remoteAlias,
        ctx.remoteNodeId,
        MTI_DATAGRAM
      );
      copyByteToPayload(sm.outgoing.msg, CONFIG_MEM_CONFIGURATION, 0);
      copyByteToPayload(sm.outgoing.msg, replyCmd, 1);
      copyDwordToPayload(sm.outgoing.msg, ctx.startAddress ?? 0, 2);
      let off = 6;
      if (ctx.replyEncoding === ADDRESS_SPACE_ENCODING.IN_BYTE_6) {
        copyByteToPayload(sm.outgoing.msg, ctx.replySpaceByte, off++);
      }
      if (errorCode !== null) {
        copyWordToPayload(sm.outgoing.msg, errorCode, off);
        sm.outgoing.msg.payloadCount = off + 2;
      } else {
        const srcSid = ctx.stream ? ctx.stream.sourceStreamId : 0;
        const dstSid = ctx.stream ? ctx.stream.destStreamId : 0;
        copyByteToPayload(sm.outgoing.msg, srcSid, off++);
        copyByteToPayload(sm.outgoing.msg, dstSid, off++);
        copyDwordToPayload(sm.outgoing.msg, ctx.totalBytes, off);
        sm.outgoing.msg.payloadCount = off + 4;
      }
      sm.outgoing.valid = true;
    }
  };

  // src/openlcb/float16.js
  var float16_exports = {};
  __export(float16_exports, {
    FLOAT16_EXPONENT_MASK: () => FLOAT16_EXPONENT_MASK,
    FLOAT16_MANTISSA_MASK: () => FLOAT16_MANTISSA_MASK,
    FLOAT16_NAN: () => FLOAT16_NAN,
    FLOAT16_NEGATIVE_ZERO: () => FLOAT16_NEGATIVE_ZERO,
    FLOAT16_POSITIVE_ZERO: () => FLOAT16_POSITIVE_ZERO,
    FLOAT16_SIGN_MASK: () => FLOAT16_SIGN_MASK,
    fromFloat: () => fromFloat,
    getDirection: () => getDirection,
    getSpeed: () => getSpeed,
    isNaN16: () => isNaN16,
    isZero: () => isZero,
    negate: () => negate,
    speedWithDirection: () => speedWithDirection,
    toFloat: () => toFloat
  });
  var FLOAT16_POSITIVE_ZERO = 0;
  var FLOAT16_NEGATIVE_ZERO = 32768;
  var FLOAT16_NAN = 32256;
  var FLOAT16_SIGN_MASK = 32768;
  var FLOAT16_EXPONENT_MASK = 31744;
  var FLOAT16_MANTISSA_MASK = 1023;
  var _scratch = new ArrayBuffer(4);
  var _scratchF32 = new Float32Array(_scratch);
  var _scratchU32 = new Uint32Array(_scratch);
  function floatToBits(f) {
    _scratchF32[0] = f;
    return _scratchU32[0];
  }
  function bitsToFloat(bits) {
    _scratchU32[0] = bits >>> 0;
    return _scratchF32[0];
  }
  function fromFloat(value) {
    const fbits = floatToBits(Math.fround(value));
    const sign = fbits >>> 16 & 32768;
    const exponent = (fbits >>> 23 & 255) - 127;
    let mantissa = fbits & 8388607;
    if (exponent === -127 && mantissa === 0) {
      return sign;
    }
    if (exponent === 128 && mantissa !== 0) {
      return (sign | 32256) & 65535;
    }
    if (exponent === 128) {
      return (sign | 31744) & 65535;
    }
    if (exponent > 15) {
      return (sign | 31743) & 65535;
    }
    if (exponent >= -14) {
      const hExp = exponent + 15 << 10 & 65535;
      const hMan = mantissa >>> 13 & 65535;
      return (sign | hExp | hMan) & 65535;
    }
    if (exponent >= -24) {
      mantissa |= 8388608;
      const shift = -14 - exponent;
      const hMan = mantissa >>> 13 + shift & 65535;
      return (sign | hMan) & 65535;
    }
    return sign;
  }
  function toFloat(half) {
    const sign = (half & 32768) << 16;
    let exponent = half >>> 10 & 31;
    let mantissa = half & 1023;
    if (exponent === 0 && mantissa === 0) {
      return bitsToFloat(sign);
    }
    if (exponent === 0) {
      while ((mantissa & 1024) === 0) {
        mantissa <<= 1;
        exponent--;
      }
      exponent++;
      mantissa &= 1023;
      const fExp2 = exponent + 127 - 15 << 23 >>> 0;
      const fMan2 = mantissa << 13;
      return bitsToFloat((sign | fExp2 | fMan2) >>> 0);
    }
    if (exponent === 31) {
      const fExp2 = 255 << 23;
      const fMan2 = mantissa << 13;
      return bitsToFloat((sign | fExp2 | fMan2) >>> 0);
    }
    const fExp = exponent + 127 - 15 << 23 >>> 0;
    const fMan = mantissa << 13;
    return bitsToFloat((sign | fExp | fMan) >>> 0);
  }
  function negate(half) {
    return (half ^ FLOAT16_SIGN_MASK) & 65535;
  }
  function isNaN16(half) {
    const exp = half & FLOAT16_EXPONENT_MASK;
    const man = half & FLOAT16_MANTISSA_MASK;
    return exp === FLOAT16_EXPONENT_MASK && man !== 0;
  }
  function isZero(half) {
    return (half & 32767) === 0;
  }
  function speedWithDirection(speed, reverse) {
    const magnitude = speed < 0 ? -speed : speed;
    let half = fromFloat(magnitude) & 32767;
    if (reverse) half |= FLOAT16_SIGN_MASK;
    return half & 65535;
  }
  function getSpeed(half) {
    return toFloat(half & 32767);
  }
  function getDirection(half) {
    return (half & FLOAT16_SIGN_MASK) !== 0;
  }

  // src/protocol/train-handler.js
  function extractFnAddress(msg, offset) {
    return (msg.payload[offset] << 16 | msg.payload[offset + 1] << 8 | msg.payload[offset + 2]) >>> 0;
  }
  function copyFnAddress(msg, fnAddress, offset) {
    copyByteToPayload(msg, fnAddress >>> 16 & 255, offset);
    copyByteToPayload(msg, fnAddress >>> 8 & 255, offset + 1);
    copyByteToPayload(msg, fnAddress & 255, offset + 2);
  }
  var ProtocolTrainHandler = class {
    /** @param {Object} [deps] — callback bag per interface_protocol_train_handler_t */
    constructor(deps = {}) {
      this._cb = deps;
    }
    // -------------------------------------------------------------------------
    // Reply-message builder (instruction byte + sub-command at byte 0)
    // -------------------------------------------------------------------------
    _startReply(sm, instructionByte) {
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        sm.incoming.msg.sourceAlias,
        sm.incoming.msg.sourceId,
        MTI_TRAIN_REPLY
      );
      copyByteToPayload(sm.outgoing.msg, instructionByte, 0);
    }
    // -------------------------------------------------------------------------
    // Public entry points
    // -------------------------------------------------------------------------
    /** MTI_TRAIN_PROTOCOL (0x05EB) — incoming command to a train node. */
    handleTrainCommand(sm) {
      if (!sm.node.trainState) return;
      const instruction = extractByteFromPayload(sm.incoming.msg, 0) & 127;
      switch (instruction) {
        case TRAIN_SET_SPEED_DIRECTION:
          return this._handleSetSpeed(sm);
        case TRAIN_SET_FUNCTION:
          return this._handleSetFunction(sm);
        case TRAIN_EMERGENCY_STOP:
          return this._handleEmergencyStop(sm);
        case TRAIN_QUERY_SPEEDS:
          return this._handleQuerySpeeds(sm);
        case TRAIN_QUERY_FUNCTION:
          return this._handleQueryFunction(sm);
        case TRAIN_CONTROLLER_CONFIG:
          return this._handleControllerConfig(sm);
        case TRAIN_LISTENER_CONFIG:
          return this._handleListenerConfig(sm);
        case TRAIN_MANAGEMENT:
          return this._handleManagement(sm);
        default:
          return;
      }
    }
    /** MTI_TRAIN_REPLY (0x01E9) — incoming reply on the throttle side. */
    handleTrainReply(sm) {
      const instruction = extractByteFromPayload(sm.incoming.msg, 0) & 127;
      switch (instruction) {
        case TRAIN_QUERY_SPEEDS:
          return this._handleQuerySpeedsReply(sm);
        case TRAIN_QUERY_FUNCTION:
          return this._handleQueryFunctionReply(sm);
        case TRAIN_CONTROLLER_CONFIG:
          return this._handleControllerConfigReply(sm);
        case TRAIN_LISTENER_CONFIG:
          return this._handleListenerConfigReply(sm);
        case TRAIN_MANAGEMENT:
          return this._handleManagementReply(sm);
        default:
          return;
      }
    }
    /** Global or addressed emergency PCER. */
    handleEmergencyEvent(sm, eventId) {
      const state = sm.node.trainState;
      if (!state) return;
      switch (eventId) {
        case EVENT_ID_EMERGENCY_OFF:
          state.globalEoffActive = true;
          this._cb.onEmergencyEntered?.(sm.node, TRAIN_EMERGENCY_TYPE.GLOBAL_OFF);
          break;
        case EVENT_ID_CLEAR_EMERGENCY_OFF:
          state.globalEoffActive = false;
          this._cb.onEmergencyExited?.(sm.node, TRAIN_EMERGENCY_TYPE.GLOBAL_OFF);
          break;
        case EVENT_ID_EMERGENCY_STOP:
          state.globalEstopActive = true;
          this._cb.onEmergencyEntered?.(sm.node, TRAIN_EMERGENCY_TYPE.GLOBAL_STOP);
          break;
        case EVENT_ID_CLEAR_EMERGENCY_STOP:
          state.globalEstopActive = false;
          this._cb.onEmergencyExited?.(sm.node, TRAIN_EMERGENCY_TYPE.GLOBAL_STOP);
          break;
      }
    }
    /** True iff `eventId` is one of the four well-known emergency events. */
    static isEmergencyEvent(eventId) {
      return eventId === EVENT_ID_EMERGENCY_OFF || eventId === EVENT_ID_CLEAR_EMERGENCY_OFF || eventId === EVENT_ID_EMERGENCY_STOP || eventId === EVENT_ID_CLEAR_EMERGENCY_STOP;
    }
    // -------------------------------------------------------------------------
    // Command handlers (train-node side)
    // -------------------------------------------------------------------------
    _handleSetSpeed(sm) {
      const state = sm.node.trainState;
      const speed = extractWordFromPayload(sm.incoming.msg, 1);
      if (state) {
        state.setSpeed = speed;
        state.estopActive = false;
        if (state.heartbeatTimeoutS > 0) {
          state.heartbeatCounter100ms = isZero(speed) ? 0 : state.heartbeatTimeoutS * 10;
        }
      }
      this._cb.onSpeedChanged?.(sm.node, speed);
    }
    _handleSetFunction(sm) {
      const state = sm.node.trainState;
      const fnAddress = extractFnAddress(sm.incoming.msg, 1);
      const fnValue = extractWordFromPayload(sm.incoming.msg, 4);
      if (state && fnAddress < state.functions.length) {
        state.functions[fnAddress] = fnValue;
      }
      this._cb.onFunctionChanged?.(sm.node, fnAddress, fnValue);
    }
    _handleEmergencyStop(sm) {
      const state = sm.node.trainState;
      if (state) {
        state.estopActive = true;
        const reverse = getDirection(state.setSpeed);
        state.setSpeed = reverse ? FLOAT16_NEGATIVE_ZERO : FLOAT16_POSITIVE_ZERO;
        state.heartbeatCounter100ms = 0;
      }
      this._cb.onEmergencyEntered?.(sm.node, TRAIN_EMERGENCY_TYPE.ESTOP);
    }
    _handleQuerySpeeds(sm) {
      const state = sm.node.trainState;
      const setSpeed = state ? state.setSpeed : 0;
      const status = state && state.estopActive ? 1 : 0;
      const commanded = state ? state.commandedSpeed : FLOAT16_NAN;
      const actual = state ? state.actualSpeed : FLOAT16_NAN;
      this._startReply(sm, TRAIN_QUERY_SPEEDS);
      copyWordToPayload(sm.outgoing.msg, setSpeed, 1);
      copyByteToPayload(sm.outgoing.msg, status, 3);
      copyWordToPayload(sm.outgoing.msg, commanded, 4);
      copyWordToPayload(sm.outgoing.msg, actual, 6);
      sm.outgoing.valid = true;
    }
    _handleQueryFunction(sm) {
      const state = sm.node.trainState;
      const fnAddress = extractFnAddress(sm.incoming.msg, 1);
      const fnValue = state && fnAddress < state.functions.length ? state.functions[fnAddress] : 0;
      this._startReply(sm, TRAIN_QUERY_FUNCTION);
      copyFnAddress(sm.outgoing.msg, fnAddress, 1);
      copyWordToPayload(sm.outgoing.msg, fnValue, 4);
      sm.outgoing.valid = true;
    }
    _handleControllerConfig(sm) {
      const state = sm.node.trainState;
      const msg = sm.incoming.msg;
      const sub = extractByteFromPayload(msg, 1);
      switch (sub) {
        case TRAIN_CONTROLLER_ASSIGN: {
          const requesting = extractNodeIdFromPayload(msg, 3);
          let accepted = true;
          if (state) {
            if (state.controllerNodeId === 0n || state.controllerNodeId === requesting) {
              state.controllerNodeId = requesting;
              state.controllerAlias = msg.sourceAlias;
            } else {
              accepted = this._cb.onControllerAssignRequest ? this._cb.onControllerAssignRequest(sm.node, state.controllerNodeId, requesting) : true;
              if (accepted) {
                state.controllerNodeId = requesting;
                state.controllerAlias = msg.sourceAlias;
              }
            }
            if (accepted && state.heartbeatTimeoutS > 0) {
              state.heartbeatCounter100ms = state.heartbeatTimeoutS * 10;
            }
          }
          this._startReply(sm, TRAIN_CONTROLLER_CONFIG);
          copyByteToPayload(sm.outgoing.msg, TRAIN_CONTROLLER_ASSIGN, 1);
          copyByteToPayload(sm.outgoing.msg, accepted ? 0 : 1, 2);
          if (!accepted && state) {
            copyNodeIdToPayload(sm.outgoing.msg, state.controllerNodeId, 3);
          }
          sm.outgoing.valid = true;
          if (accepted) this._cb.onControllerAssigned?.(sm.node, requesting);
          break;
        }
        case TRAIN_CONTROLLER_RELEASE: {
          const releasing = extractNodeIdFromPayload(msg, 3);
          if (state && (state.controllerNodeId === releasing || releasing === 0n)) {
            state.controllerNodeId = 0n;
            state.controllerAlias = 0;
          }
          this._startReply(sm, TRAIN_CONTROLLER_CONFIG);
          copyByteToPayload(sm.outgoing.msg, TRAIN_CONTROLLER_RELEASE, 1);
          copyByteToPayload(sm.outgoing.msg, 0, 2);
          sm.outgoing.valid = true;
          this._cb.onControllerReleased?.(sm.node);
          break;
        }
        case TRAIN_CONTROLLER_QUERY: {
          this._startReply(sm, TRAIN_CONTROLLER_CONFIG);
          copyByteToPayload(sm.outgoing.msg, TRAIN_CONTROLLER_QUERY, 1);
          const flags = state && state.controllerNodeId !== 0n ? 1 : 0;
          copyByteToPayload(sm.outgoing.msg, flags, 2);
          copyNodeIdToPayload(sm.outgoing.msg, state ? state.controllerNodeId : 0n, 3);
          sm.outgoing.valid = true;
          break;
        }
        case TRAIN_CONTROLLER_CHANGED: {
          const newController = extractNodeIdFromPayload(msg, 3);
          const accepted = this._cb.onControllerChangedRequest ? this._cb.onControllerChangedRequest(sm.node, newController) : true;
          if (state && accepted) {
            state.controllerNodeId = newController;
            state.controllerAlias = msg.sourceAlias;
          }
          this._startReply(sm, TRAIN_CONTROLLER_CONFIG);
          copyByteToPayload(sm.outgoing.msg, TRAIN_CONTROLLER_CHANGED, 1);
          copyByteToPayload(sm.outgoing.msg, accepted ? 0 : 1, 2);
          sm.outgoing.valid = true;
          if (accepted) this._cb.onControllerAssigned?.(sm.node, newController);
          break;
        }
      }
    }
    _handleListenerConfig(sm) {
      const state = sm.node.trainState;
      const msg = sm.incoming.msg;
      const sub = extractByteFromPayload(msg, 1);
      switch (sub) {
        case TRAIN_LISTENER_ATTACH: {
          const flags = extractByteFromPayload(msg, 2);
          const listenerId = extractNodeIdFromPayload(msg, 3);
          let result = 0;
          if (state && listenerId !== 0n) {
            const max = state.listeners.length;
            let slot = -1;
            for (let i = 0; i < state.listenerCount; i++) {
              if (state.listeners[i].nodeId === listenerId) {
                slot = i;
                break;
              }
            }
            if (slot >= 0) {
              state.listeners[slot].flags = flags;
            } else if (state.listenerCount < max) {
              state.listeners[state.listenerCount].nodeId = listenerId;
              state.listeners[state.listenerCount].flags = flags;
              state.listenerCount++;
            } else {
              result = 255;
            }
          } else {
            result = 255;
          }
          this._startReply(sm, TRAIN_LISTENER_CONFIG);
          copyByteToPayload(sm.outgoing.msg, TRAIN_LISTENER_ATTACH, 1);
          copyNodeIdToPayload(sm.outgoing.msg, listenerId, 2);
          copyByteToPayload(sm.outgoing.msg, result, 8);
          sm.outgoing.valid = true;
          if (result === 0) this._cb.onListenerChanged?.(sm.node);
          break;
        }
        case TRAIN_LISTENER_DETACH: {
          const listenerId = extractNodeIdFromPayload(msg, 3);
          let result = 0;
          if (state && listenerId !== 0n) {
            let idx = -1;
            for (let i = 0; i < state.listenerCount; i++) {
              if (state.listeners[i].nodeId === listenerId) {
                idx = i;
                break;
              }
            }
            if (idx >= 0) {
              for (let j = idx; j < state.listenerCount - 1; j++) {
                state.listeners[j].nodeId = state.listeners[j + 1].nodeId;
                state.listeners[j].flags = state.listeners[j + 1].flags;
              }
              state.listenerCount--;
              state.listeners[state.listenerCount].nodeId = 0n;
              state.listeners[state.listenerCount].flags = 0;
            } else {
              result = 255;
            }
          } else {
            result = 255;
          }
          this._startReply(sm, TRAIN_LISTENER_CONFIG);
          copyByteToPayload(sm.outgoing.msg, TRAIN_LISTENER_DETACH, 1);
          copyNodeIdToPayload(sm.outgoing.msg, listenerId, 2);
          copyByteToPayload(sm.outgoing.msg, result, 8);
          sm.outgoing.valid = true;
          if (result === 0) this._cb.onListenerChanged?.(sm.node);
          break;
        }
        case TRAIN_LISTENER_QUERY: {
          const requestedIndex = extractByteFromPayload(msg, 2);
          const count = state ? state.listenerCount : 0;
          let entryFlags = 0;
          let entryNodeId = 0n;
          if (count > 0 && requestedIndex < count) {
            entryFlags = state.listeners[requestedIndex].flags;
            entryNodeId = state.listeners[requestedIndex].nodeId;
          }
          this._startReply(sm, TRAIN_LISTENER_CONFIG);
          copyByteToPayload(sm.outgoing.msg, TRAIN_LISTENER_QUERY, 1);
          copyByteToPayload(sm.outgoing.msg, count, 2);
          copyByteToPayload(sm.outgoing.msg, requestedIndex, 3);
          copyByteToPayload(sm.outgoing.msg, entryFlags, 4);
          copyNodeIdToPayload(sm.outgoing.msg, entryNodeId, 5);
          sm.outgoing.valid = true;
          break;
        }
      }
    }
    _handleManagement(sm) {
      const state = sm.node.trainState;
      const sub = extractByteFromPayload(sm.incoming.msg, 1);
      switch (sub) {
        case TRAIN_MGMT_RESERVE: {
          const result = state && state.reservedByNodeId === 0n ? 0 : 1;
          if (state && result === 0) {
            state.reservedByNodeId = sm.incoming.msg.sourceId;
          }
          this._startReply(sm, TRAIN_MANAGEMENT);
          copyByteToPayload(sm.outgoing.msg, TRAIN_MGMT_RESERVE, 1);
          copyByteToPayload(sm.outgoing.msg, result, 2);
          sm.outgoing.valid = true;
          break;
        }
        case TRAIN_MGMT_RELEASE: {
          if (state) state.reservedByNodeId = 0n;
          this._startReply(sm, TRAIN_MANAGEMENT);
          copyByteToPayload(sm.outgoing.msg, TRAIN_MGMT_RELEASE, 1);
          copyByteToPayload(sm.outgoing.msg, 0, 2);
          sm.outgoing.valid = true;
          break;
        }
        case TRAIN_MGMT_NOOP: {
          if (state && state.heartbeatTimeoutS > 0) {
            state.heartbeatCounter100ms = state.heartbeatTimeoutS * 10;
          }
          this._startReply(sm, TRAIN_MANAGEMENT);
          copyByteToPayload(sm.outgoing.msg, TRAIN_MGMT_NOOP, 1);
          copyByteToPayload(sm.outgoing.msg, 0, 2);
          sm.outgoing.valid = true;
          break;
        }
      }
    }
    // -------------------------------------------------------------------------
    // Reply handlers (throttle side)
    // -------------------------------------------------------------------------
    _handleQuerySpeedsReply(sm) {
      const msg = sm.incoming.msg;
      const setSpeed = extractWordFromPayload(msg, 1);
      const status = extractByteFromPayload(msg, 3);
      const commanded = extractWordFromPayload(msg, 4);
      const actual = extractWordFromPayload(msg, 6);
      this._cb.onQuerySpeedsReply?.(sm.node, setSpeed, status, commanded, actual);
    }
    _handleQueryFunctionReply(sm) {
      const msg = sm.incoming.msg;
      const fnAddress = extractFnAddress(msg, 1);
      const fnValue = extractWordFromPayload(msg, 4);
      this._cb.onQueryFunctionReply?.(sm.node, fnAddress, fnValue);
    }
    _handleControllerConfigReply(sm) {
      const msg = sm.incoming.msg;
      const sub = extractByteFromPayload(msg, 1);
      const result = extractByteFromPayload(msg, 2);
      switch (sub) {
        case TRAIN_CONTROLLER_ASSIGN: {
          const current = result === 0 ? 0n : extractNodeIdFromPayload(msg, 3);
          this._cb.onControllerAssignReply?.(sm.node, result, current);
          break;
        }
        case TRAIN_CONTROLLER_QUERY: {
          const flags = result;
          const controller = extractNodeIdFromPayload(msg, 3);
          this._cb.onControllerQueryReply?.(sm.node, flags, controller);
          break;
        }
        case TRAIN_CONTROLLER_CHANGED: {
          this._cb.onControllerChangedNotifyReply?.(sm.node, result);
          break;
        }
      }
    }
    _handleListenerConfigReply(sm) {
      const msg = sm.incoming.msg;
      const sub = extractByteFromPayload(msg, 1);
      const result = extractByteFromPayload(msg, 2);
      switch (sub) {
        case TRAIN_LISTENER_ATTACH: {
          const nodeId = extractNodeIdFromPayload(msg, 3);
          this._cb.onListenerAttachReply?.(sm.node, nodeId, result);
          break;
        }
        case TRAIN_LISTENER_DETACH: {
          const nodeId = extractNodeIdFromPayload(msg, 3);
          this._cb.onListenerDetachReply?.(sm.node, nodeId, result);
          break;
        }
        case TRAIN_LISTENER_QUERY: {
          const count = result;
          const index = extractByteFromPayload(msg, 3);
          const flags = extractByteFromPayload(msg, 4);
          const nodeId = extractNodeIdFromPayload(msg, 5);
          this._cb.onListenerQueryReply?.(sm.node, count, index, flags, nodeId);
          break;
        }
      }
    }
    _handleManagementReply(sm) {
      const msg = sm.incoming.msg;
      const sub = extractByteFromPayload(msg, 1);
      const result = extractByteFromPayload(msg, 2);
      if (sub === TRAIN_MGMT_RESERVE) {
        this._cb.onReserveReply?.(sm.node, result);
      } else if (sub === TRAIN_MGMT_NOOP) {
        const timeoutSeconds = msg.payloadCount >= 7 ? (msg.payload[3] << 24 | msg.payload[4] << 16 | msg.payload[5] << 8 | msg.payload[6]) >>> 0 : 0;
        this._cb.onHeartbeatRequest?.(sm.node, timeoutSeconds);
      }
    }
  };

  // src/protocol/train-search-handler.js
  function isSearchEvent(eventId) {
    return (eventId & TRAIN_SEARCH_MASK) === EVENT_TRAIN_SEARCH_SPACE;
  }
  function extractDigits(eventId) {
    const lower = Number(eventId & 0xFFFFFFFFn) >>> 0;
    return [
      lower >>> 28 & 15,
      lower >>> 24 & 15,
      lower >>> 20 & 15,
      lower >>> 16 & 15,
      lower >>> 12 & 15,
      lower >>> 8 & 15
    ];
  }
  function extractFlags(eventId) {
    return Number(eventId & 0xFFn);
  }
  function digitsToAddress(digits) {
    let address = 0;
    for (let i = 0; i < 6; i++) {
      if (digits[i] <= 9) address = address * 10 + digits[i];
    }
    return address;
  }
  function createEventId(address, flags) {
    const digits = [15, 15, 15, 15, 15, 15];
    if (address === 0) {
      digits[5] = 0;
    } else {
      let i = 5;
      let a = address;
      while (a > 0 && i >= 0) {
        digits[i] = a % 10;
        a = Math.floor(a / 10);
        i--;
      }
    }
    const lower = (digits[0] << 28 | digits[1] << 24 | digits[2] << 20 | digits[3] << 16 | digits[4] << 12 | digits[5] << 8 | flags & 255) >>> 0;
    return EVENT_TRAIN_SEARCH_SPACE | BigInt(lower);
  }
  function hasReservedValues(digits, flags) {
    for (let i = 0; i < 6; i++) {
      if (digits[i] >= 10 && digits[i] <= 14) return true;
    }
    if (flags & 16) return true;
    if (!(flags & TRAIN_SEARCH_FLAG_DCC) && flags & 7) return true;
    return false;
  }
  function addressDigits(address) {
    if (address === 0) return [0];
    const rev = [];
    let t = address;
    while (t > 0 && rev.length < 6) {
      rev.push(t % 10);
      t = Math.floor(t / 10);
    }
    return rev.reverse();
  }
  function searchDigitsNoPadding(digits) {
    const out = [];
    for (const d of digits) {
      if (d <= 9) out.push(d);
    }
    return out;
  }
  function doesAddressMatch(trainAddress, digits, flags) {
    const trainDigits = addressDigits(trainAddress);
    const searchDigits = searchDigitsNoPadding(digits);
    if (searchDigits.length === 0) return false;
    if (flags & TRAIN_SEARCH_FLAG_EXACT) {
      if (searchDigits.length !== trainDigits.length) return false;
      for (let i = 0; i < searchDigits.length; i++) {
        if (searchDigits[i] !== trainDigits[i]) return false;
      }
      return true;
    }
    if (searchDigits.length > trainDigits.length) return false;
    for (let i = 0; i < searchDigits.length; i++) {
      if (searchDigits[i] !== trainDigits[i]) return false;
    }
    return true;
  }
  function doesNameMatch(name, digits, flags) {
    if (!name || name.length === 0) return false;
    let i = 0;
    while (i < 6) {
      if (digits[i] > 9) {
        i++;
        continue;
      }
      const seq = [];
      while (i < 6 && digits[i] <= 9) {
        seq.push(digits[i]);
        i++;
      }
      let seqMatched = false;
      for (let p = 0; p < name.length && !seqMatched; p++) {
        if (name[p] < "0" || name[p] > "9") continue;
        if (p > 0 && name[p - 1] >= "0" && name[p - 1] <= "9") continue;
        let si = 0, np = p;
        while (si < seq.length && np < name.length) {
          if (name[np] < "0" || name[np] > "9") {
            np++;
            continue;
          }
          if (name.charCodeAt(np) - 48 !== seq[si]) break;
          si++;
          np++;
        }
        if (si === seq.length) {
          if (flags & TRAIN_SEARCH_FLAG_EXACT) {
            while (np < name.length && (name[np] < "0" || name[np] > "9")) np++;
            if (np >= name.length || name[np] < "0" || name[np] > "9") seqMatched = true;
          } else {
            seqMatched = true;
          }
        }
      }
      if (!seqMatched) return false;
    }
    return true;
  }
  function doesTrainMatch(trainState, digits, searchAddress, flags, nodeName) {
    if (flags & TRAIN_SEARCH_FLAG_DCC) {
      if (flags & TRAIN_SEARCH_FLAG_LONG_ADDR) {
        if (!trainState.isLongAddress) return false;
      } else if (!(flags & TRAIN_SEARCH_FLAG_ALLOCATE)) {
        if (searchAddress < TRAIN_MAX_DCC_SHORT_ADDRESS && trainState.isLongAddress) return false;
        if (searchAddress >= TRAIN_MAX_DCC_SHORT_ADDRESS && !trainState.isLongAddress) return false;
      }
    }
    if (doesAddressMatch(trainState.dccAddress, digits, flags)) return true;
    if (!(flags & TRAIN_SEARCH_FLAG_ADDRESS_ONLY) && nodeName) {
      if (doesNameMatch(nodeName, digits, flags)) return true;
    }
    return false;
  }
  var ProtocolTrainSearchHandler = class {
    /**
     * @param {Object} [deps]
     * @param {(node, searchAddress, flags) => void} [deps.onSearchMatched]
     * @param {(searchAddress, flags) => Object|null} [deps.onSearchNoMatch]
     *        Returns a newly-allocated train node to reply on behalf of, or null.
     */
    constructor(deps = {}) {
      this._cb = deps;
    }
    /** Called per train node while enumerating a train-search event. */
    handleSearchEvent(sm, eventId) {
      if (!sm.node || !sm.node.trainState) return;
      const digits = extractDigits(eventId);
      const flags = extractFlags(eventId);
      if (hasReservedValues(digits, flags)) return;
      const searchAddress = digitsToAddress(digits);
      const nodeName = sm.node.parameters?.snip?.name ?? "";
      if (!doesTrainMatch(sm.node.trainState, digits, searchAddress, flags, nodeName)) return;
      loadOpenlcbMessage(
        sm.outgoing.msg,
        sm.node.alias,
        sm.node.id,
        0,
        0n,
        MTI_PRODUCER_IDENTIFIED_SET
      );
      copyEventIdToPayload(sm.outgoing.msg, eventId);
      sm.outgoing.valid = true;
      this._cb.onSearchMatched?.(sm.node, searchAddress, flags);
    }
    /** Called after enumerating all train nodes without a match. */
    handleSearchNoMatch(sm, eventId) {
      if (!sm) return;
      const digits = extractDigits(eventId);
      const flags = extractFlags(eventId);
      if (hasReservedValues(digits, flags)) return;
      if (!(flags & TRAIN_SEARCH_FLAG_ALLOCATE)) return;
      if (!this._cb.onSearchNoMatch) return;
      const searchAddress = digitsToAddress(digits);
      const newNode = this._cb.onSearchNoMatch(searchAddress, flags);
      if (!newNode || !newNode.trainState) return;
      loadOpenlcbMessage(
        sm.outgoing.msg,
        newNode.alias,
        newNode.id,
        0,
        0n,
        MTI_PRODUCER_IDENTIFIED_SET
      );
      copyEventIdToPayload(sm.outgoing.msg, eventId);
      sm.outgoing.valid = true;
    }
  };

  // src/protocol/broadcast-time-handler.js
  var KNOWN_CLOCK_IDS = /* @__PURE__ */ new Set([
    BROADCAST_TIME_ID_DEFAULT_FAST_CLOCK,
    BROADCAST_TIME_ID_DEFAULT_REALTIME_CLOCK,
    BROADCAST_TIME_ID_ALTERNATE_CLOCK_1,
    BROADCAST_TIME_ID_ALTERNATE_CLOCK_2
  ]);
  function isWellKnownClock(clockId) {
    return KNOWN_CLOCK_IDS.has(clockId);
  }
  function extractClockId(eventId) {
    return eventId & BROADCAST_TIME_MASK_CLOCK_ID;
  }
  function commandData(eventId) {
    return Number(eventId & BROADCAST_TIME_MASK_COMMAND_DATA) & 65535;
  }
  function getEventType(eventId) {
    const cd = commandData(eventId);
    if (cd === BROADCAST_TIME_QUERY) return BROADCAST_TIME_EVENT_TYPE.QUERY;
    if (cd === BROADCAST_TIME_STOP) return BROADCAST_TIME_EVENT_TYPE.STOP;
    if (cd === BROADCAST_TIME_START) return BROADCAST_TIME_EVENT_TYPE.START;
    if (cd === BROADCAST_TIME_DATE_ROLLOVER) return BROADCAST_TIME_EVENT_TYPE.DATE_ROLLOVER;
    if (cd >= BROADCAST_TIME_SET_RATE_BASE && cd <= 53247) return BROADCAST_TIME_EVENT_TYPE.SET_RATE;
    if (cd >= BROADCAST_TIME_SET_YEAR_BASE && cd <= 49151) return BROADCAST_TIME_EVENT_TYPE.SET_YEAR;
    if (cd >= BROADCAST_TIME_SET_DATE_BASE && cd <= 44287) return BROADCAST_TIME_EVENT_TYPE.SET_DATE;
    if (cd >= BROADCAST_TIME_SET_TIME_BASE && cd <= 38911) return BROADCAST_TIME_EVENT_TYPE.SET_TIME;
    if (cd >= BROADCAST_TIME_REPORT_RATE_BASE && cd <= 20479) return BROADCAST_TIME_EVENT_TYPE.REPORT_RATE;
    if (cd >= BROADCAST_TIME_REPORT_YEAR_BASE && cd <= 16383) return BROADCAST_TIME_EVENT_TYPE.REPORT_YEAR;
    if (cd >= BROADCAST_TIME_REPORT_DATE_BASE && cd <= 11519) return BROADCAST_TIME_EVENT_TYPE.REPORT_DATE;
    if (cd <= 6143) return BROADCAST_TIME_EVENT_TYPE.REPORT_TIME;
    return BROADCAST_TIME_EVENT_TYPE.UNKNOWN;
  }
  function stripSetOffset(cd) {
    return cd >= BROADCAST_TIME_SET_COMMAND_OFFSET ? cd - BROADCAST_TIME_SET_COMMAND_OFFSET : cd;
  }
  function extractTime(eventId) {
    const cd = stripSetOffset(commandData(eventId));
    const hour = cd >>> 8 & 255;
    const minute = cd & 255;
    if (hour >= 24 || minute >= 60) return null;
    return { hour, minute };
  }
  function extractDate(eventId) {
    const cd = stripSetOffset(commandData(eventId));
    const month = (cd >>> 8 & 255) - 32;
    const day = cd & 255;
    if (month < 1 || month > 12 || day < 1 || day > 31) return null;
    return { month, day };
  }
  function extractYear(eventId) {
    const cd = stripSetOffset(commandData(eventId));
    const y = cd - BROADCAST_TIME_REPORT_YEAR_BASE;
    if (y < 0 || y > 4095) return null;
    return y;
  }
  function extractRate(eventId) {
    const cd = stripSetOffset(commandData(eventId));
    const raw = cd - BROADCAST_TIME_REPORT_RATE_BASE & 4095;
    return raw & 2048 ? raw | 4294963200 | 0 : raw;
  }
  function createTimeEventId(clockId, hour, minute, isSet) {
    let cd = (hour & 255) << 8 | minute & 255;
    if (isSet) cd += BROADCAST_TIME_SET_COMMAND_OFFSET;
    return clockId & BROADCAST_TIME_MASK_CLOCK_ID | BigInt(cd);
  }
  function createDateEventId(clockId, month, day, isSet) {
    let cd = (32 + month & 255) << 8 | day & 255;
    if (isSet) cd += BROADCAST_TIME_SET_COMMAND_OFFSET;
    return clockId & BROADCAST_TIME_MASK_CLOCK_ID | BigInt(cd);
  }
  function createYearEventId(clockId, year, isSet) {
    let cd = BROADCAST_TIME_REPORT_YEAR_BASE + (year & 65535);
    if (isSet) cd += BROADCAST_TIME_SET_COMMAND_OFFSET;
    return clockId & BROADCAST_TIME_MASK_CLOCK_ID | BigInt(cd);
  }
  function createRateEventId(clockId, rate, isSet) {
    let cd = BROADCAST_TIME_REPORT_RATE_BASE + (rate & 4095);
    if (isSet) cd += BROADCAST_TIME_SET_COMMAND_OFFSET;
    return clockId & BROADCAST_TIME_MASK_CLOCK_ID | BigInt(cd);
  }
  function createCommandEventId(clockId, command) {
    let cd = 0;
    switch (command) {
      case BROADCAST_TIME_EVENT_TYPE.QUERY:
        cd = BROADCAST_TIME_QUERY;
        break;
      case BROADCAST_TIME_EVENT_TYPE.STOP:
        cd = BROADCAST_TIME_STOP;
        break;
      case BROADCAST_TIME_EVENT_TYPE.START:
        cd = BROADCAST_TIME_START;
        break;
      case BROADCAST_TIME_EVENT_TYPE.DATE_ROLLOVER:
        cd = BROADCAST_TIME_DATE_ROLLOVER;
        break;
    }
    return clockId & BROADCAST_TIME_MASK_CLOCK_ID | BigInt(cd);
  }
  var ProtocolBroadcastTimeHandler = class {
    /**
     * @param {Object} deps
     * @param {(clockId: bigint) => Object|null} deps.getClock
     *        Returns the clock-state object (with .time/.date/.year/.rate/.isRunning)
     *        for the given 48-bit clock ID, or null if not registered.
     * @param {(clockId) => boolean} [deps.isProducer]
     *        Returns true if this node produces time events for the given clock.
     * @param {Object} [deps.callbacks] Notifier callbacks:
     *        onTimeReceived, onDateReceived, onYearReceived, onRateReceived,
     *        onClockStarted, onClockStopped, onDateRollover.
     * @param {(node, clockId, hour, minute) => void} [deps.sendReportTime]
     *        Emit a Report Time PCER (typically via the app layer).
     * @param {(clockId) => void} [deps.triggerSyncDelay]
     *        Start the 3-second coalescing timer for Set commands.
     * @param {(clockId) => void} [deps.triggerQueryReply]
     *        Run the full query-reply sequence for this producer clock.
     */
    constructor(deps = {}) {
      this._d = deps;
      this._cb = deps.callbacks ?? {};
    }
    /** True iff the event is a broadcast-time event for any known or registered clock. */
    isTimeEvent(eventId) {
      const clockId = extractClockId(eventId);
      if (isWellKnownClock(clockId)) return true;
      return this._d.getClock ? this._d.getClock(clockId) !== null : false;
    }
    /** Handle one broadcast-time event on node index 0. */
    handleTimeEvent(sm, eventId) {
      if (!sm?.node || sm.node.index !== 0) return;
      const clockId = extractClockId(eventId);
      const clock = this._d.getClock ? this._d.getClock(clockId) : null;
      if (!clock) return;
      const type = getEventType(eventId);
      switch (type) {
        case BROADCAST_TIME_EVENT_TYPE.REPORT_TIME:
          this._applyTime(sm.node, clock, eventId);
          break;
        case BROADCAST_TIME_EVENT_TYPE.REPORT_DATE:
          this._applyDate(sm.node, clock, eventId);
          break;
        case BROADCAST_TIME_EVENT_TYPE.REPORT_YEAR:
          this._applyYear(sm.node, clock, eventId);
          break;
        case BROADCAST_TIME_EVENT_TYPE.REPORT_RATE:
          this._applyRate(sm.node, clock, eventId);
          break;
        case BROADCAST_TIME_EVENT_TYPE.SET_TIME:
          if (this._d.isProducer?.(clockId)) {
            this._applyTime(sm.node, clock, eventId);
            this._d.sendReportTime?.(sm.node, clockId, clock.time.hour, clock.time.minute);
            this._d.triggerSyncDelay?.(clockId);
          }
          break;
        case BROADCAST_TIME_EVENT_TYPE.SET_DATE:
          if (this._d.isProducer?.(clockId)) {
            this._applyDate(sm.node, clock, eventId);
            this._d.triggerSyncDelay?.(clockId);
          }
          break;
        case BROADCAST_TIME_EVENT_TYPE.SET_YEAR:
          if (this._d.isProducer?.(clockId)) {
            this._applyYear(sm.node, clock, eventId);
            this._d.triggerSyncDelay?.(clockId);
          }
          break;
        case BROADCAST_TIME_EVENT_TYPE.SET_RATE:
          if (this._d.isProducer?.(clockId)) {
            this._applyRate(sm.node, clock, eventId);
            this._d.triggerSyncDelay?.(clockId);
          }
          break;
        case BROADCAST_TIME_EVENT_TYPE.START:
          clock.isRunning = true;
          clock.msAccumulator = 0;
          this._cb.onClockStarted?.(sm.node, clock);
          if (this._d.isProducer?.(clockId)) this._d.triggerSyncDelay?.(clockId);
          break;
        case BROADCAST_TIME_EVENT_TYPE.STOP:
          clock.isRunning = false;
          this._cb.onClockStopped?.(sm.node, clock);
          if (this._d.isProducer?.(clockId)) this._d.triggerSyncDelay?.(clockId);
          break;
        case BROADCAST_TIME_EVENT_TYPE.DATE_ROLLOVER:
          this._cb.onDateRollover?.(sm.node, clock);
          break;
        case BROADCAST_TIME_EVENT_TYPE.QUERY:
          if (this._d.isProducer?.(clockId)) this._d.triggerQueryReply?.(clockId);
          break;
        default:
          break;
      }
    }
    // -------------------------------------------------------------------------
    // Apply helpers
    // -------------------------------------------------------------------------
    _applyTime(node, clock, eventId) {
      const hm = extractTime(eventId);
      if (!hm) return;
      clock.time.hour = hm.hour;
      clock.time.minute = hm.minute;
      clock.time.valid = true;
      clock.msAccumulator = 0;
      this._cb.onTimeReceived?.(node, clock);
    }
    _applyDate(node, clock, eventId) {
      const md = extractDate(eventId);
      if (!md) return;
      clock.date.month = md.month;
      clock.date.day = md.day;
      clock.date.valid = true;
      this._cb.onDateReceived?.(node, clock);
    }
    _applyYear(node, clock, eventId) {
      const y = extractYear(eventId);
      if (y === null) return;
      clock.year.year = y;
      clock.year.valid = true;
      this._cb.onYearReceived?.(node, clock);
    }
    _applyRate(node, clock, eventId) {
      const r = extractRate(eventId);
      if (r === null) return;
      clock.rate.rate = r;
      clock.rate.valid = true;
      clock.msAccumulator = 0;
      this._cb.onRateReceived?.(node, clock);
    }
  };

  // src/openlcb/application.js
  var REGISTRATION_FULL = 65535;
  var OpenLcbApplication = class {
    /**
     * @param {Object} deps
     * @param {(msg) => boolean} deps.sendOpenlcbMsg required
     * @param {(node, address, count, buffer) => number} [deps.configMemoryRead]
     * @param {(node, address, count, buffer) => number} [deps.configMemoryWrite]
     */
    constructor(deps) {
      this._sendOpenlcbMsg = deps.sendOpenlcbMsg;
      this._configMemoryRead = deps.configMemoryRead ?? null;
      this._configMemoryWrite = deps.configMemoryWrite ?? null;
    }
    // -------------------------------------------------------------------------
    // Consumer / producer event registration
    // -------------------------------------------------------------------------
    clearConsumerEventIds(node) {
      node.consumers.count = 0;
      node.consumers.list.length = 0;
    }
    clearProducerEventIds(node) {
      node.producers.count = 0;
      node.producers.list.length = 0;
    }
    /** Register a consumer event. Returns the 0-based index. */
    registerConsumerEventId(node, eventId, eventStatus) {
      const idx = node.consumers.count;
      node.consumers.list[idx] = createEvent(eventId, eventStatus);
      node.consumers.count = idx + 1;
      return idx;
    }
    /** Register a producer event. Returns the 0-based index. */
    registerProducerEventId(node, eventId, eventStatus) {
      const idx = node.producers.count;
      node.producers.list[idx] = createEvent(eventId, eventStatus);
      node.producers.count = idx + 1;
      return idx;
    }
    clearConsumerRanges(node) {
      node.consumers.rangeCount = 0;
      node.consumers.rangeList.length = 0;
    }
    clearProducerRanges(node) {
      node.producers.rangeCount = 0;
      node.producers.rangeList.length = 0;
    }
    /** Register a consumer range (base event ID + size). */
    registerConsumerRange(node, eventIdBase, rangeSize) {
      const idx = node.consumers.rangeCount;
      node.consumers.rangeList[idx] = createEventRange(eventIdBase, rangeSize);
      node.consumers.rangeCount = idx + 1;
      return true;
    }
    /** Register a producer range (base event ID + size). */
    registerProducerRange(node, eventIdBase, rangeSize) {
      const idx = node.producers.rangeCount;
      node.producers.rangeList[idx] = createEventRange(eventIdBase, rangeSize);
      node.producers.rangeCount = idx + 1;
      return true;
    }
    // -------------------------------------------------------------------------
    // Event transmission
    // -------------------------------------------------------------------------
    /** Send a global event message with an arbitrary MTI (8-byte event ID payload). */
    sendEventWithMti(node, eventId, mti) {
      const msg = createMessage({ payloadType: PAYLOAD_TYPE.BASIC });
      loadOpenlcbMessage(msg, node.alias, node.id, 0, NULL_NODE_ID, mti);
      copyEventIdToPayload(msg, eventId);
      return this._sendOpenlcbMsg(msg);
    }
    /** Send a Producer/Consumer Event Report (MTI 0x05B4). */
    sendEventPcReport(node, eventId) {
      return this.sendEventWithMti(node, eventId, MTI_PC_EVENT_REPORT);
    }
    /** Send a Learn Event / teach message (MTI 0x0594). */
    sendTeachEvent(node, eventId) {
      return this.sendEventWithMti(node, eventId, MTI_EVENT_LEARN);
    }
    /**
     * Send Initialization Complete (full or simple variant based on PSI_SIMPLE).
     * 6-byte payload = the node's Node ID.
     */
    sendInitializationEvent(node) {
      const mti = node.parameters && node.parameters.protocolSupport & PSI_SIMPLE ? MTI_INITIALIZATION_COMPLETE_SIMPLE : MTI_INITIALIZATION_COMPLETE;
      const msg = createMessage({ payloadType: PAYLOAD_TYPE.BASIC });
      loadOpenlcbMessage(msg, node.alias, node.id, 0, NULL_NODE_ID, mti);
      copyNodeIdToPayload(msg, node.id, 0);
      msg.payloadCount = 6;
      return this._sendOpenlcbMsg(msg);
    }
    // -------------------------------------------------------------------------
    // Configuration memory pass-through
    // -------------------------------------------------------------------------
    readConfigurationMemory(node, address, count, buffer) {
      if (!this._configMemoryRead) return REGISTRATION_FULL;
      return this._configMemoryRead(node, address, count, buffer);
    }
    writeConfigurationMemory(node, address, count, buffer) {
      if (!this._configMemoryWrite) return REGISTRATION_FULL;
      return this._configMemoryWrite(node, address, count, buffer);
    }
  };

  // src/openlcb/application-train.js
  var DEFAULT_FUNCTION_COUNT = 29;
  var DEFAULT_MAX_LISTENERS = 8;
  function createTrainState() {
    return {
      setSpeed: 0,
      commandedSpeed: FLOAT16_NAN,
      actualSpeed: FLOAT16_NAN,
      estopActive: false,
      globalEstopActive: false,
      globalEoffActive: false,
      controllerNodeId: 0n,
      controllerAlias: 0,
      reservedNodeCount: 0,
      reservedByNodeId: 0n,
      heartbeatTimeoutS: 0,
      heartbeatCounter100ms: 0,
      listeners: Array.from({ length: DEFAULT_MAX_LISTENERS }, () => ({ nodeId: 0n, flags: 0 })),
      listenerCount: 0,
      listenerEnumIndex: 0,
      functions: new Uint16Array(DEFAULT_FUNCTION_COUNT),
      dccAddress: 0,
      isLongAddress: false,
      speedSteps: 0,
      heartbeatSendPending: false,
      estopForwardPending: false,
      ownerNode: null
    };
  }
  var OpenLcbApplicationTrain = class {
    /**
     * @param {Object} deps
     * @param {OpenLcbApplication} deps.application  required — used for event registration
     * @param {(msg) => boolean} deps.sendOpenlcbMsg required
     * @param {(node) => void}   [deps.onHeartbeatTimeout]
     */
    constructor(deps) {
      this._application = deps.application;
      this._sendOpenlcbMsg = deps.sendOpenlcbMsg;
      this._onHeartbeatTimeout = deps.onHeartbeatTimeout ?? null;
      this._trainStates = [];
      this._lastHeartbeatTick = 0;
    }
    // -------------------------------------------------------------------------
    // State allocation
    // -------------------------------------------------------------------------
    /**
     * Allocate a train state slot, attach it to the node, and register the
     * standard train event IDs. Returns the existing state if already set.
     */
    setup(node) {
      if (!node) return null;
      if (node.trainState) return node.trainState;
      const state = createTrainState();
      state.ownerNode = node;
      node.trainState = state;
      this._trainStates.push(state);
      this._application.registerProducerEventId(node, EVENT_ID_TRAIN, EVENT_STATUS.SET);
      this._application.registerConsumerEventId(node, EVENT_ID_EMERGENCY_OFF, EVENT_STATUS.SET);
      this._application.registerConsumerEventId(node, EVENT_ID_EMERGENCY_STOP, EVENT_STATUS.SET);
      this._application.registerConsumerEventId(node, EVENT_ID_CLEAR_EMERGENCY_OFF, EVENT_STATUS.SET);
      this._application.registerConsumerEventId(node, EVENT_ID_CLEAR_EMERGENCY_STOP, EVENT_STATUS.SET);
      return state;
    }
    getState(node) {
      return node ? node.trainState : null;
    }
    // -------------------------------------------------------------------------
    // Heartbeat countdown (invoke from main loop each 100ms tick)
    // -------------------------------------------------------------------------
    timerTick(currentTick) {
      const ticksElapsed = currentTick - this._lastHeartbeatTick & 255;
      if (ticksElapsed === 0) return;
      this._lastHeartbeatTick = currentTick & 255;
      for (const state of this._trainStates) {
        if (state.heartbeatTimeoutS === 0) continue;
        if (state.controllerNodeId === 0n) continue;
        if (state.heartbeatSendPending) {
          if (this._sendHeartbeatRequest(state)) state.heartbeatSendPending = false;
        }
        if (state.estopForwardPending) {
          if (this._forwardEstopToOneListener(state)) state.listenerEnumIndex++;
          if (state.listenerEnumIndex >= state.listenerCount) state.estopForwardPending = false;
          continue;
        }
        const oldCounter = state.heartbeatCounter100ms;
        state.heartbeatCounter100ms = Math.max(0, state.heartbeatCounter100ms - ticksElapsed);
        const halfway = state.heartbeatTimeoutS * 10 >>> 1;
        if (oldCounter > halfway && state.heartbeatCounter100ms <= halfway) {
          if (!this._sendHeartbeatRequest(state)) state.heartbeatSendPending = true;
        }
        if (state.heartbeatCounter100ms === 0 && oldCounter > 0) {
          state.estopActive = true;
          state.setSpeed = getDirection(state.setSpeed) ? FLOAT16_NEGATIVE_ZERO : FLOAT16_POSITIVE_ZERO;
          if (state.listenerCount > 0) {
            state.estopForwardPending = true;
            state.listenerEnumIndex = 0;
          }
          this._onHeartbeatTimeout?.(state.ownerNode);
        }
      }
    }
    _sendHeartbeatRequest(state) {
      const node = state.ownerNode;
      if (!node || state.controllerNodeId === 0n) return false;
      const msg = createMessage({ payloadType: PAYLOAD_TYPE.BASIC });
      loadOpenlcbMessage(
        msg,
        node.alias,
        node.id,
        state.controllerAlias,
        state.controllerNodeId,
        MTI_TRAIN_REPLY
      );
      copyByteToPayload(msg, TRAIN_MANAGEMENT, 0);
      copyByteToPayload(msg, TRAIN_MGMT_NOOP, 1);
      const to = state.heartbeatTimeoutS;
      copyByteToPayload(msg, to >>> 16 & 255, 2);
      copyByteToPayload(msg, to >>> 8 & 255, 3);
      copyByteToPayload(msg, to & 255, 4);
      return this._sendOpenlcbMsg(msg);
    }
    _forwardEstopToOneListener(state) {
      const node = state.ownerNode;
      if (!node || state.listenerEnumIndex >= state.listenerCount) return false;
      const entry = state.listeners[state.listenerEnumIndex];
      const msg = createMessage({ payloadType: PAYLOAD_TYPE.BASIC });
      loadOpenlcbMessage(msg, node.alias, node.id, 0, entry.nodeId, MTI_TRAIN_PROTOCOL);
      let speed = state.setSpeed;
      if (entry.flags & TRAIN_LISTENER_FLAG_REVERSE) speed ^= 32768;
      copyByteToPayload(msg, TRAIN_SET_SPEED_DIRECTION | TRAIN_INSTRUCTION_P_BIT, 0);
      copyWordToPayload(msg, speed, 1);
      return this._sendOpenlcbMsg(msg);
    }
    // -------------------------------------------------------------------------
    // Throttle-side send helpers
    // -------------------------------------------------------------------------
    _prepareCommand(node, trainAlias, trainNodeId) {
      const msg = createMessage({ payloadType: PAYLOAD_TYPE.BASIC });
      loadOpenlcbMessage(msg, node.alias, node.id, trainAlias, trainNodeId, MTI_TRAIN_PROTOCOL);
      return msg;
    }
    sendSetSpeed(node, trainAlias, trainNodeId, speed) {
      const msg = this._prepareCommand(node, trainAlias, trainNodeId);
      copyByteToPayload(msg, TRAIN_SET_SPEED_DIRECTION, 0);
      copyWordToPayload(msg, speed, 1);
      this._sendOpenlcbMsg(msg);
    }
    sendSetFunction(node, trainAlias, trainNodeId, fnAddress, fnValue) {
      const msg = this._prepareCommand(node, trainAlias, trainNodeId);
      copyByteToPayload(msg, TRAIN_SET_FUNCTION, 0);
      copyByteToPayload(msg, fnAddress >>> 16 & 255, 1);
      copyByteToPayload(msg, fnAddress >>> 8 & 255, 2);
      copyByteToPayload(msg, fnAddress & 255, 3);
      copyWordToPayload(msg, fnValue, 4);
      this._sendOpenlcbMsg(msg);
    }
    sendEmergencyStop(node, trainAlias, trainNodeId) {
      const msg = this._prepareCommand(node, trainAlias, trainNodeId);
      copyByteToPayload(msg, TRAIN_EMERGENCY_STOP, 0);
      this._sendOpenlcbMsg(msg);
    }
    sendQuerySpeeds(node, trainAlias, trainNodeId) {
      const msg = this._prepareCommand(node, trainAlias, trainNodeId);
      copyByteToPayload(msg, TRAIN_QUERY_SPEEDS, 0);
      this._sendOpenlcbMsg(msg);
    }
    sendQueryFunction(node, trainAlias, trainNodeId, fnAddress) {
      const msg = this._prepareCommand(node, trainAlias, trainNodeId);
      copyByteToPayload(msg, TRAIN_QUERY_FUNCTION, 0);
      copyByteToPayload(msg, fnAddress >>> 16 & 255, 1);
      copyByteToPayload(msg, fnAddress >>> 8 & 255, 2);
      copyByteToPayload(msg, fnAddress & 255, 3);
      this._sendOpenlcbMsg(msg);
    }
    sendAssignController(node, trainAlias, trainNodeId) {
      const msg = this._prepareCommand(node, trainAlias, trainNodeId);
      copyByteToPayload(msg, TRAIN_CONTROLLER_CONFIG, 0);
      copyByteToPayload(msg, TRAIN_CONTROLLER_ASSIGN, 1);
      copyNodeIdToPayload(msg, node.id, 2);
      this._sendOpenlcbMsg(msg);
    }
    sendReleaseController(node, trainAlias, trainNodeId) {
      const msg = this._prepareCommand(node, trainAlias, trainNodeId);
      copyByteToPayload(msg, TRAIN_CONTROLLER_CONFIG, 0);
      copyByteToPayload(msg, TRAIN_CONTROLLER_RELEASE, 1);
      copyNodeIdToPayload(msg, node.id, 2);
      this._sendOpenlcbMsg(msg);
    }
    sendNoop(node, trainAlias, trainNodeId) {
      const msg = this._prepareCommand(node, trainAlias, trainNodeId);
      copyByteToPayload(msg, TRAIN_MANAGEMENT, 0);
      copyByteToPayload(msg, TRAIN_MGMT_NOOP, 1);
      this._sendOpenlcbMsg(msg);
    }
    // -------------------------------------------------------------------------
    // Accessors
    // -------------------------------------------------------------------------
    setDccAddress(node, dccAddress, isLongAddress) {
      if (!node?.trainState) return;
      node.trainState.dccAddress = dccAddress;
      node.trainState.isLongAddress = !!isLongAddress;
    }
    getDccAddress(node) {
      return node?.trainState ? node.trainState.dccAddress : 0;
    }
    isLongAddress(node) {
      return node?.trainState ? !!node.trainState.isLongAddress : false;
    }
    setSpeedSteps(node, speedSteps) {
      if (!node?.trainState) return;
      node.trainState.speedSteps = speedSteps;
    }
    getSpeedSteps(node) {
      return node?.trainState ? node.trainState.speedSteps : 0;
    }
  };

  // src/openlcb/application-broadcast-time.js
  var WELL_KNOWN_CLOCKS = [
    BROADCAST_TIME_ID_DEFAULT_FAST_CLOCK,
    BROADCAST_TIME_ID_DEFAULT_REALTIME_CLOCK,
    BROADCAST_TIME_ID_ALTERNATE_CLOCK_1,
    BROADCAST_TIME_ID_ALTERNATE_CLOCK_2
  ];
  var SYNC_DELAY_TICKS = 30;
  var REPORT_COOLDOWN_TICKS = 600;
  function createClock(clockId) {
    return {
      state: {
        clockId,
        time: { hour: 0, minute: 0, valid: false },
        date: { month: 1, day: 1, valid: false },
        year: { year: 2024, valid: false },
        rate: { rate: 0, valid: false },
        isRunning: false,
        msAccumulator: 0
      },
      isConsumer: false,
      isProducer: false,
      isAllocated: true,
      queryReplyPending: false,
      syncPending: false,
      sendQueryReplyState: 0,
      syncDelayTicks: 0,
      reportCooldownTicks: 0,
      previousRunState: 0,
      producerNode: null
    };
  }
  var OpenLcbApplicationBroadcastTime = class {
    /**
     * @param {Object} deps
     * @param {OpenLcbApplication} deps.application required — event range registration
     * @param {(msg) => boolean}   deps.sendOpenlcbMsg required
     * @param {Object}             [deps.callbacks]
     * @param {number}             [deps.maxCustomClocks=4]
     */
    constructor(deps) {
      this._application = deps.application;
      this._sendOpenlcbMsg = deps.sendOpenlcbMsg;
      this._cb = deps.callbacks ?? {};
      this._clocks = /* @__PURE__ */ new Map();
      this._maxCustomClocks = deps.maxCustomClocks ?? 4;
      this._lastTick = 0;
    }
    // -------------------------------------------------------------------------
    // Slot allocation
    // -------------------------------------------------------------------------
    /** Allocate or return existing clock slot, marking as consumer, registering event ranges. */
    setupConsumer(node, clockId) {
      const clock = this._getOrCreate(clockId);
      if (!clock) return null;
      clock.isConsumer = true;
      if (node) this._registerRanges(node, clockId);
      return clock.state;
    }
    /** Same, producer side — also records the producer node for self-originated events. */
    setupProducer(node, clockId) {
      const clock = this._getOrCreate(clockId);
      if (!clock) return null;
      clock.isProducer = true;
      clock.producerNode = node;
      if (node) this._registerRanges(node, clockId);
      return clock.state;
    }
    _getOrCreate(clockId) {
      const masked = clockId & BROADCAST_TIME_MASK_CLOCK_ID;
      let clock = this._clocks.get(masked);
      if (clock) return clock;
      const isWellKnown = WELL_KNOWN_CLOCKS.includes(masked);
      if (!isWellKnown) {
        let customCount = 0;
        for (const [id] of this._clocks) {
          if (!WELL_KNOWN_CLOCKS.includes(id)) customCount++;
        }
        if (customCount >= this._maxCustomClocks) return null;
      }
      clock = createClock(masked);
      this._clocks.set(masked, clock);
      return clock;
    }
    _registerRanges(node, clockId) {
      const masked = clockId & BROADCAST_TIME_MASK_CLOCK_ID;
      this._application.registerConsumerRange(node, masked | 0x0000n, EVENT_RANGE_COUNT.C_32768);
      this._application.registerConsumerRange(node, masked | 0x8000n, EVENT_RANGE_COUNT.C_32768);
      this._application.registerProducerRange(node, masked | 0x0000n, EVENT_RANGE_COUNT.C_32768);
      this._application.registerProducerRange(node, masked | 0x8000n, EVENT_RANGE_COUNT.C_32768);
    }
    getClock(clockId) {
      const clock = this._clocks.get(clockId & BROADCAST_TIME_MASK_CLOCK_ID);
      return clock ? clock.state : null;
    }
    isConsumer(clockId) {
      const clock = this._clocks.get(clockId & BROADCAST_TIME_MASK_CLOCK_ID);
      return !!(clock && clock.isConsumer);
    }
    isProducer(clockId) {
      const clock = this._clocks.get(clockId & BROADCAST_TIME_MASK_CLOCK_ID);
      return !!(clock && clock.isProducer);
    }
    start(clockId) {
      const clock = this._clocks.get(clockId & BROADCAST_TIME_MASK_CLOCK_ID);
      if (clock) {
        clock.state.isRunning = true;
        clock.state.msAccumulator = 0;
      }
    }
    stop(clockId) {
      const clock = this._clocks.get(clockId & BROADCAST_TIME_MASK_CLOCK_ID);
      if (clock) clock.state.isRunning = false;
    }
    /** Compose a clock ID from a 48-bit unique identifier (Node ID etc.). */
    static makeClockId(uniqueId48) {
      return BigInt.asUintN(48, uniqueId48) << 16n;
    }
    triggerQueryReply(clockId) {
      const clock = this._clocks.get(clockId & BROADCAST_TIME_MASK_CLOCK_ID);
      if (clock && clock.isProducer) {
        clock.queryReplyPending = true;
        clock.sendQueryReplyState = 0;
      }
    }
    triggerSyncDelay(clockId) {
      const clock = this._clocks.get(clockId & BROADCAST_TIME_MASK_CLOCK_ID);
      if (clock && clock.isProducer) {
        clock.syncPending = true;
        clock.syncDelayTicks = SYNC_DELAY_TICKS;
      }
    }
    // -------------------------------------------------------------------------
    // 100ms tick — advance running consumer clocks, drive sync/query reply
    // -------------------------------------------------------------------------
    timerTick(currentTick) {
      const ticksElapsed = currentTick - this._lastTick & 255;
      if (ticksElapsed === 0) return;
      this._lastTick = currentTick & 255;
      for (const clock of this._clocks.values()) {
        if (clock.isConsumer) this._advanceConsumer(clock, ticksElapsed);
        if (clock.isProducer) {
          this._advanceProducer(clock, ticksElapsed);
          if (clock.syncPending && clock.syncDelayTicks > 0) {
            clock.syncDelayTicks = Math.max(0, clock.syncDelayTicks - ticksElapsed);
            if (clock.syncDelayTicks === 0) {
              clock.syncPending = false;
              clock.queryReplyPending = true;
              clock.sendQueryReplyState = 0;
            }
          }
          if (clock.queryReplyPending && clock.producerNode) {
            this._driveQueryReply(clock);
          }
          const node = clock.producerNode;
          if (node) {
            const curRunState = node.state.runState;
            if (clock.previousRunState < RUNSTATE_RUN && curRunState >= RUNSTATE_RUN) {
              clock.queryReplyPending = true;
              clock.sendQueryReplyState = 0;
            }
            clock.previousRunState = curRunState;
          }
        }
      }
    }
    /**
     * Advance a producer clock. Same fast-minute accounting as the consumer
     * path, plus:
     *   - Periodic Report Time PCER every minute boundary, rate-limited to
     *     one emission per REPORT_COOLDOWN_TICKS (60 real seconds).
     *   - 23:59→00:00 rollover: emit Date Rollover + Report Year + Report Date.
     * Mirrors OpenLcbCLib openlcb_application_broadcast_time.c:680-770.
     */
    _advanceProducer(clock, ticksElapsed) {
      const s = clock.state;
      const node = clock.producerNode;
      if (clock.reportCooldownTicks > 0) {
        clock.reportCooldownTicks = Math.max(0, clock.reportCooldownTicks - ticksElapsed);
      }
      if (!s.isRunning || !s.rate.valid || s.rate.rate === 0) return;
      const delta = ticksElapsed * 100 * s.rate.rate;
      s.msAccumulator += delta;
      const oneMinute = 24e4;
      while (s.msAccumulator >= oneMinute) {
        s.msAccumulator -= oneMinute;
        const prevHour = s.time.hour;
        s.time.minute++;
        if (s.time.minute >= 60) {
          s.time.minute = 0;
          s.time.hour = (s.time.hour + 1) % 24;
          if (prevHour === 23 && s.time.hour === 0) {
            this._advanceDate(s);
            if (node) {
              this.sendDateRollover(node, s.clockId);
              this.sendReportYear(node, s.clockId, s.year.year);
              this.sendReportDate(node, s.clockId, s.date.month, s.date.day);
            }
          }
        }
        if (node && clock.reportCooldownTicks === 0) {
          this.sendReportTime(node, s.clockId, s.time.hour, s.time.minute);
          clock.reportCooldownTicks = REPORT_COOLDOWN_TICKS;
        }
        this._cb.onTimeChanged?.(clock);
      }
    }
    _advanceDate(s) {
      const daysInMonth = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
      const y = s.year.year;
      const leap = y % 4 === 0 && y % 100 !== 0 || y % 400 === 0;
      const limit = s.date.month === 2 && leap ? 29 : daysInMonth[s.date.month - 1];
      s.date.day++;
      if (s.date.day > limit) {
        s.date.day = 1;
        s.date.month++;
        if (s.date.month > 12) {
          s.date.month = 1;
          s.year.year++;
        }
      }
    }
    /**
     * Advance a consumer clock's time by `ticksElapsed`. The rate is a 10.2
     * signed fixed-point value: rate/4 × real minutes per tick = fast minutes.
     * Fires `onTimeChanged` each time a minute boundary is crossed.
     */
    _advanceConsumer(clock, ticksElapsed) {
      const s = clock.state;
      if (!s.isRunning || !s.rate.valid || s.rate.rate === 0) return;
      const delta = ticksElapsed * 100 * s.rate.rate;
      s.msAccumulator += delta;
      const oneMinute = 24e4;
      while (s.msAccumulator >= oneMinute) {
        s.msAccumulator -= oneMinute;
        s.time.minute++;
        if (s.time.minute >= 60) {
          s.time.minute = 0;
          s.time.hour = (s.time.hour + 1) % 24;
          if (s.time.hour === 0) {
            this._cb.onDateRollover?.(clock.producerNode, s);
          }
        }
        this._cb.onTimeChanged?.(clock);
      }
    }
    // 6-message sync sequence per the Broadcast Time Standard:
    //   0 → Start/Stop command
    //   1 → Rate as Producer Identified Set
    //   2 → Year as Producer Identified Set
    //   3 → Date as Producer Identified Set
    //   4 → current Time as Producer Identified Set
    //   5 → next-minute Time as PC Event Report
    _driveQueryReply(clock) {
      const s = clock.state;
      const node = clock.producerNode;
      const id = s.clockId;
      switch (clock.sendQueryReplyState) {
        case 0: {
          const eid = createCommandEventId(
            id,
            s.isRunning ? BROADCAST_TIME_EVENT_TYPE.START : BROADCAST_TIME_EVENT_TYPE.STOP
          );
          if (this._sendEvent(node, eid, MTI_PRODUCER_IDENTIFIED_SET)) clock.sendQueryReplyState = 1;
          return;
        }
        case 1: {
          if (this._sendEvent(node, createRateEventId(id, s.rate.rate, false), MTI_PRODUCER_IDENTIFIED_SET)) clock.sendQueryReplyState = 2;
          return;
        }
        case 2: {
          if (this._sendEvent(node, createYearEventId(id, s.year.year, false), MTI_PRODUCER_IDENTIFIED_SET)) clock.sendQueryReplyState = 3;
          return;
        }
        case 3: {
          if (this._sendEvent(node, createDateEventId(id, s.date.month, s.date.day, false), MTI_PRODUCER_IDENTIFIED_SET)) clock.sendQueryReplyState = 4;
          return;
        }
        case 4: {
          if (this._sendEvent(node, createTimeEventId(id, s.time.hour, s.time.minute, false), MTI_PRODUCER_IDENTIFIED_SET)) clock.sendQueryReplyState = 5;
          return;
        }
        case 5: {
          let h = s.time.hour, m = s.time.minute + 1;
          if (m >= 60) {
            m = 0;
            h = (h + 1) % 24;
          }
          if (this._sendEvent(node, createTimeEventId(id, h, m, false), MTI_PC_EVENT_REPORT)) {
            clock.queryReplyPending = false;
            clock.sendQueryReplyState = 0;
          }
          return;
        }
        default:
          clock.queryReplyPending = false;
          clock.sendQueryReplyState = 0;
      }
    }
    // -------------------------------------------------------------------------
    // Send helpers (all event-carrying messages)
    // -------------------------------------------------------------------------
    _sendEvent(node, eventId, mti) {
      const msg = createMessage({ payloadType: PAYLOAD_TYPE.BASIC });
      loadOpenlcbMessage(msg, node.alias, node.id, 0, 0n, mti);
      copyEventIdToPayload(msg, eventId);
      return this._sendOpenlcbMsg(msg);
    }
    _producerOrNoop(clockId) {
      const clock = this._clocks.get(clockId & BROADCAST_TIME_MASK_CLOCK_ID);
      return clock && clock.isProducer ? clock : null;
    }
    sendReportTime(node, clockId, hour, minute) {
      if (!this._producerOrNoop(clockId)) return true;
      return this._sendEvent(node, createTimeEventId(clockId, hour, minute, false), MTI_PC_EVENT_REPORT);
    }
    sendReportDate(node, clockId, month, day) {
      if (!this._producerOrNoop(clockId)) return true;
      return this._sendEvent(node, createDateEventId(clockId, month, day, false), MTI_PC_EVENT_REPORT);
    }
    sendReportYear(node, clockId, year) {
      if (!this._producerOrNoop(clockId)) return true;
      return this._sendEvent(node, createYearEventId(clockId, year, false), MTI_PC_EVENT_REPORT);
    }
    sendReportRate(node, clockId, rate) {
      if (!this._producerOrNoop(clockId)) return true;
      return this._sendEvent(node, createRateEventId(clockId, rate, false), MTI_PC_EVENT_REPORT);
    }
    sendStart(node, clockId) {
      if (!this._producerOrNoop(clockId)) return true;
      return this._sendEvent(node, createCommandEventId(clockId, BROADCAST_TIME_EVENT_TYPE.START), MTI_PC_EVENT_REPORT);
    }
    sendStop(node, clockId) {
      if (!this._producerOrNoop(clockId)) return true;
      return this._sendEvent(node, createCommandEventId(clockId, BROADCAST_TIME_EVENT_TYPE.STOP), MTI_PC_EVENT_REPORT);
    }
    sendDateRollover(node, clockId) {
      if (!this._producerOrNoop(clockId)) return true;
      return this._sendEvent(node, createCommandEventId(clockId, BROADCAST_TIME_EVENT_TYPE.DATE_ROLLOVER), MTI_PC_EVENT_REPORT);
    }
    // Consumer-side: ask a producer for its state.
    sendQuery(node, clockId) {
      return this._sendEvent(node, createCommandEventId(clockId, BROADCAST_TIME_EVENT_TYPE.QUERY), MTI_PC_EVENT_REPORT);
    }
    // Throttle-side Set commands.
    sendSetTime(node, clockId, hour, minute) {
      return this._sendEvent(node, createTimeEventId(clockId, hour, minute, true), MTI_PC_EVENT_REPORT);
    }
    sendSetDate(node, clockId, month, day) {
      return this._sendEvent(node, createDateEventId(clockId, month, day, true), MTI_PC_EVENT_REPORT);
    }
    sendSetYear(node, clockId, year) {
      return this._sendEvent(node, createYearEventId(clockId, year, true), MTI_PC_EVENT_REPORT);
    }
    sendSetRate(node, clockId, rate) {
      return this._sendEvent(node, createRateEventId(clockId, rate, true), MTI_PC_EVENT_REPORT);
    }
    sendCommandStart(node, clockId) {
      return this._sendEvent(node, createCommandEventId(clockId, BROADCAST_TIME_EVENT_TYPE.START), MTI_PC_EVENT_REPORT);
    }
    sendCommandStop(node, clockId) {
      return this._sendEvent(node, createCommandEventId(clockId, BROADCAST_TIME_EVENT_TYPE.STOP), MTI_PC_EVENT_REPORT);
    }
  };

  // src/openlcb/gridconnect.js
  var STATE_SYNC_START = 0;
  var STATE_SYNC_FIND_HEADER = 2;
  var STATE_SYNC_FIND_DATA = 4;
  var IDENTIFIER_START_POS = 2;
  var IDENTIFIER_LEN = 8;
  var NORMAL_FLAG_POS = 10;
  var DATA_START_POS = 11;
  var HEADER_LEN = 12;
  var MAX_GRID_CONNECT_LEN = 29;
  function isValidHexChar(code) {
    return code >= 48 && code <= 57 || // 0-9
    code >= 65 && code <= 70 || // A-F
    code >= 97 && code <= 102;
  }
  var GridConnectParser = class {
    constructor() {
      this._state = STATE_SYNC_START;
      this._idx = 0;
      this._buf = new Uint8Array(MAX_GRID_CONNECT_LEN);
    }
    /** Reset the parser back to looking for ':X'. */
    reset() {
      this._state = STATE_SYNC_START;
      this._idx = 0;
    }
    /**
     * Feed one byte (0-255 Number) or one character (length-1 string) into
     * the parser. Returns a completed GridConnect string (including the
     * terminating ';') when a full frame has been parsed, or null otherwise.
     *
     * Malformed input auto-resets to SYNC_START without surfacing an error.
     */
    feedByte(input) {
      const c = typeof input === "string" ? input.charCodeAt(0) : input & 255;
      switch (this._state) {
        case STATE_SYNC_START:
          if (c === 88 || c === 120) {
            this._idx = 0;
            this._buf[this._idx++] = 58;
            this._buf[this._idx++] = c;
            this._state = STATE_SYNC_FIND_HEADER;
          }
          return null;
        case STATE_SYNC_FIND_HEADER:
          if (this._idx > NORMAL_FLAG_POS) {
            this._state = STATE_SYNC_START;
            return null;
          }
          if (c === 78 || c === 110) {
            if (this._idx === NORMAL_FLAG_POS) {
              this._buf[this._idx++] = c;
              this._state = STATE_SYNC_FIND_DATA;
            } else {
              this._state = STATE_SYNC_START;
            }
          } else if (!isValidHexChar(c)) {
            this._state = STATE_SYNC_START;
          } else {
            this._buf[this._idx++] = c;
          }
          return null;
        case STATE_SYNC_FIND_DATA:
          if (c === 59) {
            if ((this._idx + 1) % 2 !== 0) {
              this._state = STATE_SYNC_START;
              return null;
            }
            this._buf[this._idx] = c;
            this._state = STATE_SYNC_START;
            const len = this._idx + 1;
            let out = "";
            for (let i = 0; i < len; i++) {
              out += String.fromCharCode(this._buf[i]);
            }
            return out;
          }
          if (!isValidHexChar(c)) {
            this._state = STATE_SYNC_START;
            return null;
          }
          this._buf[this._idx++] = c;
          if (this._idx > MAX_GRID_CONNECT_LEN - 1) {
            this._state = STATE_SYNC_START;
          }
          return null;
        default:
          this._state = STATE_SYNC_START;
          return null;
      }
    }
    /**
     * Convenience: feed a chunk (string or Uint8Array) and invoke `onFrame`
     * for every completed GridConnect frame found in the chunk.
     */
    feed(chunk, onFrame) {
      if (typeof chunk === "string") {
        for (let i = 0; i < chunk.length; i++) {
          const frame = this.feedByte(chunk.charCodeAt(i));
          if (frame) onFrame(frame);
        }
      } else {
        for (let i = 0; i < chunk.length; i++) {
          const frame = this.feedByte(chunk[i]);
          if (frame) onFrame(frame);
        }
      }
    }
  };
  function toCanMsg(gridconnectStr, canMsg = null) {
    const msg = canMsg ?? createCanMsg();
    const len = gridconnectStr.length;
    if (len < HEADER_LEN) {
      msg.identifier = 0;
      msg.payloadCount = 0;
      return msg;
    }
    const idStr = gridconnectStr.slice(IDENTIFIER_START_POS, IDENTIFIER_START_POS + IDENTIFIER_LEN);
    msg.identifier = parseInt(idStr, 16) >>> 0;
    const endIndex = gridconnectStr.endsWith(";") ? len - 1 : len;
    const dataCharCount = endIndex - DATA_START_POS;
    const byteCount = Math.min(dataCharCount >> 1, LEN_CAN_BYTE_ARRAY);
    msg.payloadCount = byteCount;
    for (let i = 0; i < byteCount; i++) {
      const off = DATA_START_POS + (i << 1);
      msg.payload[i] = parseInt(gridconnectStr.slice(off, off + 2), 16) & 255;
    }
    for (let i = byteCount; i < LEN_CAN_BYTE_ARRAY; i++) {
      msg.payload[i] = 0;
    }
    return msg;
  }
  var HEX = "0123456789ABCDEF";
  function hex8(n) {
    const v = n >>> 0;
    return HEX[v >>> 28 & 15] + HEX[v >>> 24 & 15] + HEX[v >>> 20 & 15] + HEX[v >>> 16 & 15] + HEX[v >>> 12 & 15] + HEX[v >>> 8 & 15] + HEX[v >>> 4 & 15] + HEX[v & 15];
  }
  function hex2(b) {
    return HEX[b >>> 4 & 15] + HEX[b & 15];
  }
  function fromCanMsg(canMsg) {
    let out = ":X" + hex8(canMsg.identifier) + "N";
    const n = Math.min(canMsg.payloadCount, LEN_CAN_BYTE_ARRAY);
    for (let i = 0; i < n; i++) {
      out += hex2(canMsg.payload[i]);
    }
    return out + ";";
  }

  // src/drivers/websocket/transport.js
  var DEFAULT_RECONNECT_MIN_MS = 500;
  var DEFAULT_RECONNECT_MAX_MS = 3e4;
  var WS_STATE = Object.freeze({
    DISCONNECTED: "disconnected",
    CONNECTING: "connecting",
    CONNECTED: "connected",
    CLOSING: "closing"
  });
  var WebSocketTransport = class {
    /**
     * @param {Object} opts
     * @param {string}   opts.url                    required — WebSocket URL (ws:// or wss://)
     * @param {(canMsg) => void} opts.onCanFrame     required — called for every decoded CAN frame
     * @param {() => void}       [opts.onConnect]    optional lifecycle hook
     * @param {(wasClean, code, reason) => void} [opts.onDisconnect]
     * @param {(err) => void}    [opts.onError]
     * @param {boolean}  [opts.autoReconnect=true]
     * @param {number}   [opts.reconnectMinMs=500]
     * @param {number}   [opts.reconnectMaxMs=30000]
     * @param {Function} [opts.WebSocketImpl]  optional — inject a WebSocket ctor
     *                                         for testing (defaults to the global).
     */
    constructor(opts) {
      if (!opts.url) throw new Error("WebSocketTransport: url is required");
      if (!opts.onCanFrame) throw new Error("WebSocketTransport: onCanFrame is required");
      this._url = opts.url;
      this._onCanFrame = opts.onCanFrame;
      this._onConnect = opts.onConnect ?? null;
      this._onDisconnect = opts.onDisconnect ?? null;
      this._onError = opts.onError ?? null;
      this._autoReconnect = opts.autoReconnect ?? true;
      this._reconnectMinMs = opts.reconnectMinMs ?? DEFAULT_RECONNECT_MIN_MS;
      this._reconnectMaxMs = opts.reconnectMaxMs ?? DEFAULT_RECONNECT_MAX_MS;
      this._WebSocketImpl = opts.WebSocketImpl ?? (typeof WebSocket !== "undefined" ? WebSocket : null);
      this._frameTerminator = opts.frameTerminator ?? "\n";
      this._parser = new GridConnectParser();
      this._ws = null;
      this._state = WS_STATE.DISCONNECTED;
      this._reconnectAttempt = 0;
      this._reconnectTimer = null;
      this._manualClose = false;
    }
    get state() {
      return this._state;
    }
    /** Open the connection. No-op if already connecting / connected. */
    connect() {
      if (!this._WebSocketImpl) {
        throw new Error("WebSocketTransport: no WebSocket implementation available (pass opts.WebSocketImpl)");
      }
      if (this._state === WS_STATE.CONNECTING || this._state === WS_STATE.CONNECTED) return;
      this._manualClose = false;
      this._openSocket();
    }
    /** Close the connection. Suppresses auto-reconnect. */
    disconnect(code, reason) {
      this._manualClose = true;
      this._clearReconnect();
      if (this._ws && (this._state === WS_STATE.CONNECTING || this._state === WS_STATE.CONNECTED)) {
        this._state = WS_STATE.CLOSING;
        try {
          this._ws.close(code, reason);
        } catch (e) {
        }
      } else {
        this._state = WS_STATE.DISCONNECTED;
      }
    }
    /**
     * Send a CAN frame as a GridConnect ASCII string. Returns false if the
     * socket isn't open yet.
     */
    send(canMsg) {
      if (this._state !== WS_STATE.CONNECTED || !this._ws) return false;
      try {
        this._ws.send(fromCanMsg(canMsg) + this._frameTerminator);
        return true;
      } catch (e) {
        this._reportError(e);
        return false;
      }
    }
    // -------------------------------------------------------------------------
    // Internals
    // -------------------------------------------------------------------------
    _openSocket() {
      this._state = WS_STATE.CONNECTING;
      this._parser.reset();
      let ws;
      try {
        ws = new this._WebSocketImpl(this._url);
      } catch (e) {
        this._reportError(e);
        this._scheduleReconnect();
        return;
      }
      this._ws = ws;
      ws.onopen = () => {
        this._state = WS_STATE.CONNECTED;
        this._reconnectAttempt = 0;
        this._onConnect?.();
      };
      ws.onmessage = (event) => {
        this._handleIncoming(event.data);
      };
      ws.onerror = (err) => {
        this._reportError(err);
      };
      ws.onclose = (event) => {
        const wasClean = typeof event?.wasClean === "boolean" ? event.wasClean : true;
        const code = event?.code;
        const reason = event?.reason;
        this._state = WS_STATE.DISCONNECTED;
        this._ws = null;
        this._onDisconnect?.(wasClean, code, reason);
        if (!this._manualClose && this._autoReconnect) {
          this._scheduleReconnect();
        }
      };
    }
    _handleIncoming(data) {
      if (typeof data === "string") {
        this._parser.feed(data, (frameStr) => this._processFrame(frameStr));
      } else if (data instanceof ArrayBuffer) {
        const bytes = new Uint8Array(data);
        this._parser.feed(bytes, (frameStr) => this._processFrame(frameStr));
      }
    }
    _processFrame(gcString) {
      try {
        const canMsg = toCanMsg(gcString);
        this._onCanFrame(canMsg);
      } catch (e) {
        this._reportError(e);
      }
    }
    _reportError(err) {
      if (this._onError) this._onError(err);
    }
    _scheduleReconnect() {
      if (!this._autoReconnect || this._manualClose) return;
      if (this._reconnectTimer) return;
      const backoff = Math.min(
        this._reconnectMaxMs,
        this._reconnectMinMs * Math.pow(2, this._reconnectAttempt)
      );
      this._reconnectAttempt++;
      this._reconnectTimer = setTimeout(() => {
        this._reconnectTimer = null;
        if (this._state === WS_STATE.DISCONNECTED && !this._manualClose) {
          this._openSocket();
        }
      }, backoff);
    }
    _clearReconnect() {
      if (this._reconnectTimer) {
        clearTimeout(this._reconnectTimer);
        this._reconnectTimer = null;
      }
      this._reconnectAttempt = 0;
    }
  };

  // src/openlcb/config.js
  var OpenLcbConfig = class {
    /**
     * @param {Object} opts
     * @param {string}   opts.websocketUrl            required
     * @param {Function} [opts.WebSocketImpl]         optional ctor (test injection)
     * @param {boolean}  [opts.autoReconnect=true]
     *
     * @param {(node, address, count, buffer) => number} [opts.configMemoryRead]
     * @param {(node, address, count, buffer) => number} [opts.configMemoryWrite]
     *
     * @param {Object}   [opts.callbacks]              application callback bag
     *   onLoginComplete(node)
     *   onPcEventReport(node, eventId)
     *   onConsumedEventIdentified(node, index, eventId, status, payload)
     *   onConsumerIdentifiedUnknown/Set/Clear/Reserved(node, eventId)
     *   onProducerIdentifiedUnknown/Set/Clear/Reserved(node, eventId)
     *   onConsumerRangeIdentified(node, eventId)
     *   onProducerRangeIdentified(node, eventId)
     *   onOptionalInteractionRejected(node, sourceId, errorCode, rejectedMti)
     *   onTerminateDueToError(node, sourceId, errorCode, rejectedMti)
     *   onTrainSpeedChanged(node, speed)
     *   onTrainFunctionChanged(node, fnAddress, fnValue)
     *   onTrainEmergencyEntered/Exited(node, type)
     *   onBroadcastTimeChanged(clock), etc.
     *
     * @param {Object}   [opts.snipLoaders]             custom SNIP field loaders
     * @param {number}   [opts.aliasMappingsCapacity=16]
     * @param {number}   [opts.listenerCapacity=16]
     * @param {number}   [opts.maxConcurrentStreams=2]
     */
    constructor(opts) {
      if (!opts || !opts.websocketUrl && !opts.transport) {
        throw new Error("OpenLcbConfig: either websocketUrl or transport factory is required");
      }
      this._opts = opts;
      const cb = opts.callbacks ?? {};
      this.nodePool = new NodePool({ on100msTimerTick: cb.on100msTimerTick });
      this.aliasMappings = new AliasMappings(opts.aliasMappingsCapacity ?? 16);
      this.aliasMappingListener = new AliasMappingListener({ capacity: opts.listenerCapacity ?? 16 });
      this.canTxFifo = new CanBufferFifo();
      this.openlcbRxFifo = new MessageFifo();
      this.openlcbTxFifo = new MessageFifo();
      this._tick = 0;
      const getCurrentTick = () => this._tick;
      const transportHandlers = {
        onCanFrame: (canMsg) => this.canRxStatemachine.handleFrame(canMsg),
        onConnect: cb.onTransportConnect,
        onDisconnect: cb.onTransportDisconnect,
        onError: cb.onTransportError
      };
      this.transport = typeof opts.transport === "function" ? opts.transport(transportHandlers) : new WebSocketTransport({
        url: opts.websocketUrl,
        WebSocketImpl: opts.WebSocketImpl,
        autoReconnect: opts.autoReconnect,
        frameTerminator: opts.frameTerminator,
        ...transportHandlers
      });
      this.canTxMessageHandler = new CanTxMessageHandler({
        transmit: (canMsg) => this.transport.send(canMsg),
        onTransmit: cb.onCanTransmit
      });
      this.canRxMessageHandler = new CanRxMessageHandler({
        aliasMappings: this.aliasMappings,
        canTxFifo: this.canTxFifo,
        openlcbFifo: this.openlcbRxFifo,
        getCurrentTick,
        listener: this.aliasMappingListener
      });
      this.canRxStatemachine = new CanRxStatemachine({
        rxHandler: this.canRxMessageHandler,
        aliasMappings: this.aliasMappings,
        onReceive: cb.onCanReceive
      });
      this.canLoginHandler = new CanLoginMessageHandler({
        aliasMappings: this.aliasMappings,
        onAliasChange: cb.onAliasChange
      });
      this.canLoginStatemachine = new CanLoginStatemachine(this.canLoginHandler);
      this.canMainStatemachine = new CanMainStatemachine({
        nodePool: this.nodePool,
        aliasMappings: this.aliasMappings,
        canTxFifo: this.canTxFifo,
        loginStatemachine: this.canLoginStatemachine,
        sendCanMessage: (canMsg) => this.canTxMessageHandler.canFrame(canMsg),
        getCurrentTick,
        listener: this.aliasMappingListener
      });
      const sendOpenlcbMsg = (msg) => this._sendOpenlcbMsg(msg);
      this.application = new OpenLcbApplication({
        sendOpenlcbMsg,
        configMemoryRead: opts.configMemoryRead,
        configMemoryWrite: opts.configMemoryWrite
      });
      this.applicationTrain = new OpenLcbApplicationTrain({
        application: this.application,
        sendOpenlcbMsg,
        onHeartbeatTimeout: cb.onTrainHeartbeatTimeout
      });
      this.applicationBroadcastTime = new OpenLcbApplicationBroadcastTime({
        application: this.application,
        sendOpenlcbMsg,
        callbacks: {
          onTimeChanged: cb.onBroadcastTimeChanged,
          onTimeReceived: cb.onBroadcastTimeReceived,
          onDateReceived: cb.onBroadcastDateReceived,
          onYearReceived: cb.onBroadcastYearReceived,
          onDateRollover: cb.onBroadcastDateRollover
        }
      });
      this.messageNetworkHandler = new ProtocolMessageNetwork({
        onOptionalInteractionRejected: cb.onOptionalInteractionRejected,
        onTerminateDueToError: cb.onTerminateDueToError
      });
      this.snipHandler = new ProtocolSnip({ configMemoryRead: opts.configMemoryRead });
      this.eventTransport = new ProtocolEventTransport(cb);
      this.streamHandler = new ProtocolStreamHandler({
        maxConcurrentStreams: opts.maxConcurrentStreams ?? 2,
        // Default initiator hook: if a config-mem write context is pending
        // a Stream Initiate Request, bind and auto-accept. Otherwise defer
        // to the user callback (absent = reject, stream handler default).
        onInitiateRequest: (sm, stream) => {
          const ctx = this.configMemStream.claimWriteInitiate(sm, stream);
          if (ctx) return true;
          return cb.onStreamInitiateRequest ? cb.onStreamInitiateRequest(sm, stream) : false;
        },
        onInitiateReply: (sm, stream) => this._streamInitiateReplyFanout(sm, stream),
        onDataReceived: (sm, stream) => this._streamDataReceivedFanout(sm, stream),
        onDataProceed: cb.onStreamDataProceed,
        onComplete: (sm, stream) => this._streamCompleteFanout(sm, stream)
      });
      this.configMemStream = new ProtocolConfigMemStream({
        streamHandler: this.streamHandler,
        loadDatagramReceivedOk: (sm, s) => this.datagramHandler.loadDatagramReceivedOkMessage(sm, s),
        loadDatagramRejected: (sm, e) => this.datagramHandler.loadDatagramRejectedMessage(sm, e),
        readCallbacks: opts.configMemStreamReadCallbacks,
        writeCallbacks: opts.configMemStreamWriteCallbacks
      });
      this.configMemRead = new ProtocolConfigMemRead({
        loadDatagramReceivedOk: (sm, s) => this.datagramHandler.loadDatagramReceivedOkMessage(sm, s),
        loadDatagramRejected: (sm, e) => this.datagramHandler.loadDatagramRejectedMessage(sm, e),
        configMemoryRead: opts.configMemoryRead,
        snip: opts.snipLoaders ?? this._defaultSnipLoaders()
      });
      this.configMemWrite = new ProtocolConfigMemWrite({
        loadDatagramReceivedOk: (sm, s) => this.datagramHandler.loadDatagramReceivedOkMessage(sm, s),
        loadDatagramRejected: (sm, e) => this.datagramHandler.loadDatagramRejectedMessage(sm, e),
        configMemoryRead: opts.configMemoryRead,
        configMemoryWrite: opts.configMemoryWrite
      });
      this.configMemOperations = new ProtocolConfigMemOperations({
        loadDatagramReceivedOk: (sm, s) => this.datagramHandler.loadDatagramReceivedOkMessage(sm, s),
        loadDatagramRejected: (sm, e) => this.datagramHandler.loadDatagramRejectedMessage(sm, e),
        overrides: {
          // MemCfg §4.24 restart: application-level handler flips nodes
          // back to pre-login; the login SM re-emits CID/RID/AMD and
          // Initialization_Complete serves as the implicit ACK.
          resetReboot: cb.onResetReboot ?? null
        }
      });
      this.datagramHandler = new ProtocolDatagramHandler({
        nodePool: this.nodePool,
        callbacks: this._buildDatagramCallbackTable()
      });
      this.trainHandler = new ProtocolTrainHandler(cb);
      this.trainSearchHandler = new ProtocolTrainSearchHandler({
        onSearchMatched: cb.onTrainSearchMatched,
        onSearchNoMatch: cb.onTrainSearchNoMatch
      });
      this.broadcastTimeHandler = new ProtocolBroadcastTimeHandler({
        getClock: (id) => this.applicationBroadcastTime.getClock(id),
        isProducer: (id) => this.applicationBroadcastTime.isProducer(id),
        sendReportTime: (node, id, h, m) => this.applicationBroadcastTime.sendReportTime(node, id, h, m),
        triggerSyncDelay: (id) => this.applicationBroadcastTime.triggerSyncDelay(id),
        triggerQueryReply: (id) => this.applicationBroadcastTime.triggerQueryReply(id),
        callbacks: cb
      });
      this.openlcbLoginStatemachine = new OpenLcbLoginStatemachine({
        nodePool: this.nodePool,
        sendOpenlcbMsg,
        onLoginComplete: cb.onLoginComplete ?? (() => true),
        extractProducerEventStateMti: (node, idx) => ProtocolEventTransport.extractProducerEventStatusMti(node, idx),
        extractConsumerEventStateMti: (node, idx) => ProtocolEventTransport.extractConsumerEventStatusMti(node, idx)
        // processMainStatemachine wired in after mainStatemachine exists (below).
      });
      this.openlcbMainStatemachine = new OpenLcbMainStatemachine({
        nodePool: this.nodePool,
        openlcbRxFifo: this.openlcbRxFifo,
        sendOpenlcbMsg,
        getCurrentTick,
        handlers: this._buildMainDispatchHandlers(cb),
        loadDatagramRejected: (sm, e) => this.datagramHandler.loadDatagramRejectedMessage(sm, e)
      });
      this.openlcbLoginStatemachine._processMainStatemachine = (sibSm) => this.openlcbMainStatemachine.process(sibSm);
      this._running = false;
      this._rafHandle = null;
      this._tickInterval = null;
    }
    // -------------------------------------------------------------------------
    // Public API
    // -------------------------------------------------------------------------
    /** Allocate a virtual node and return it. Matches the plan's API sketch. */
    createNode(nodeId, parameters) {
      return this.nodePool.allocate(nodeId, parameters);
    }
    /** Connect the transport and start the main + 100ms loops. */
    start() {
      if (this._running) return;
      this._running = true;
      this.transport.connect();
      this._tickInterval = setInterval(() => {
        this._tick = this._tick + 1 & 255;
        this.nodePool.timerTick(this._tick);
        this.applicationTrain.timerTick(this._tick);
        this.applicationBroadcastTime.timerTick(this._tick);
        this.datagramHandler.checkTimeouts(this._tick);
      }, 100);
      const schedule = typeof requestAnimationFrame !== "undefined" ? (fn) => requestAnimationFrame(fn) : (fn) => setTimeout(fn, 0);
      const DRAIN_BUDGET_MS = 5;
      const now = typeof performance !== "undefined" && performance.now ? () => performance.now() : () => Date.now();
      const tick = () => {
        if (!this._running) return;
        const deadline = now() + DRAIN_BUDGET_MS;
        do {
          this.canMainStatemachine.run();
          this.openlcbLoginStatemachine.run();
          this.openlcbMainStatemachine.run();
          this.configMemStream.pump(this.openlcbMainStatemachine.getStateInfo());
        } while (now() < deadline);
        this._rafHandle = schedule(tick);
      };
      this._rafHandle = schedule(tick);
    }
    /** Stop the run loops and disconnect the transport. */
    stop() {
      this._running = false;
      if (this._tickInterval) {
        clearInterval(this._tickInterval);
        this._tickInterval = null;
      }
      this.transport.disconnect();
    }
    // -------------------------------------------------------------------------
    // Internal wiring helpers
    // -------------------------------------------------------------------------
    /**
     * Send an OpenLCB message out via the TX path. For global/addressed
     * single-frame messages we delegate directly to the CAN TX handler's
     * segmenter, walking the index until all bytes have been shipped.
     */
    _sendOpenlcbMsg(msg) {
      if (!msg) return false;
      const worker = { state: { allocated: false }, identifier: 0, payloadCount: 0, payload: new Uint8Array(8) };
      const indexRef = { value: 0 };
      const isAddressed = (msg.mti & 8) === 8;
      const mti = msg.mti;
      const isStream = mti === MTI_STREAM_SEND;
      let safety = 0;
      while (safety++ < 64) {
        let ok;
        if (isStream) {
          ok = this.canTxMessageHandler.streamFrame(msg, worker, indexRef);
        } else if (mti === MTI_DATAGRAM) {
          ok = this.canTxMessageHandler.datagramFrame(msg, worker, indexRef);
        } else if (isAddressed) {
          ok = this.canTxMessageHandler.addressedMsgFrame(msg, worker, indexRef);
        } else {
          ok = this.canTxMessageHandler.unaddressedMsgFrame(msg, worker, indexRef);
        }
        if (!ok) return false;
        if (indexRef.value >= msg.payloadCount) return true;
      }
      return false;
    }
    _buildMainDispatchHandlers(cb) {
      const network = this.messageNetworkHandler;
      const snip = this.snipHandler;
      const events = this.eventTransport;
      const datagram = this.datagramHandler;
      const stream = this.streamHandler;
      const train = this.trainHandler;
      const broadcastTime = this.broadcastTimeHandler;
      const trainSearch = this.trainSearchHandler;
      return {
        snipSimpleNodeInfoRequest: (sm) => snip.handleSimpleNodeInfoRequest(sm),
        snipSimpleNodeInfoReply: (sm) => snip.handleSimpleNodeInfoReply(sm),
        messageNetworkInitializationComplete: (sm) => network.handleInitializationComplete(sm),
        messageNetworkInitializationCompleteSimple: (sm) => network.handleInitializationCompleteSimple(sm),
        messageNetworkProtocolSupportInquiry: (sm) => network.handleProtocolSupportInquiry(sm),
        messageNetworkProtocolSupportReply: (sm) => network.handleProtocolSupportReply(sm),
        messageNetworkVerifyNodeIdAddressed: (sm) => network.handleVerifyNodeIdAddressed(sm),
        messageNetworkVerifyNodeIdGlobal: (sm) => network.handleVerifyNodeIdGlobal(sm),
        messageNetworkVerifiedNodeId: (sm) => network.handleVerifiedNodeId(sm),
        messageNetworkOptionalInteractionRejected: (sm) => network.handleOptionalInteractionRejected(sm),
        messageNetworkTerminateDueToError: (sm) => {
          network.handleTerminateDueToError(sm);
          stream.handleTerminateDueToError(sm);
        },
        eventTransportConsumerIdentify: (sm) => events.handleConsumerIdentify(sm),
        eventTransportConsumerRangeIdentified: (sm) => events.handleConsumerRangeIdentified(sm),
        eventTransportConsumerIdentifiedUnknown: (sm) => events.handleConsumerIdentifiedUnknown(sm),
        eventTransportConsumerIdentifiedSet: (sm) => events.handleConsumerIdentifiedSet(sm),
        eventTransportConsumerIdentifiedClear: (sm) => events.handleConsumerIdentifiedClear(sm),
        eventTransportConsumerIdentifiedReserved: (sm) => events.handleConsumerIdentifiedReserved(sm),
        eventTransportProducerIdentify: (sm) => {
          const eid = this._extractEventIdFromIncoming(sm);
          if (isSearchEvent(eid)) {
            if (sm.node.trainState) {
              trainSearch.handleSearchEvent(sm, eid);
              if (sm.outgoing.valid) sm.trainSearchMatchFound = true;
            }
            if (this.nodePool.isLast(sm.enumeratorKey) && !sm.trainSearchMatchFound && !sm.outgoing.valid) {
              trainSearch.handleSearchNoMatch(sm, eid);
            }
            return;
          }
          events.handleProducerIdentify(sm);
        },
        eventTransportProducerRangeIdentified: (sm) => events.handleProducerRangeIdentified(sm),
        eventTransportProducerIdentifiedUnknown: (sm) => events.handleProducerIdentifiedUnknown(sm),
        eventTransportProducerIdentifiedSet: (sm) => {
          const eid = this._extractEventIdFromIncoming(sm);
          if (sm.node.index === 0 && broadcastTime.isTimeEvent(eid)) {
            broadcastTime.handleTimeEvent(sm, eid);
            return;
          }
          events.handleProducerIdentifiedSet(sm);
        },
        eventTransportProducerIdentifiedClear: (sm) => events.handleProducerIdentifiedClear(sm),
        eventTransportProducerIdentifiedReserved: (sm) => events.handleProducerIdentifiedReserved(sm),
        eventTransportIdentifyDest: (sm) => events.handleEventsIdentifyDest(sm),
        eventTransportIdentify: (sm) => events.handleEventsIdentify(sm),
        eventTransportLearn: (sm) => events.handleEventLearn(sm),
        eventTransportPcReport: (sm) => {
          const eid = this._extractEventIdFromIncoming(sm);
          if (sm.node.trainState && ProtocolTrainHandler.isEmergencyEvent(eid)) {
            train.handleEmergencyEvent(sm, eid);
            return;
          }
          if (sm.node.index === 0 && broadcastTime.isTimeEvent(eid)) {
            broadcastTime.handleTimeEvent(sm, eid);
            return;
          }
          events.handlePcEventReport(sm);
        },
        eventTransportPcReportWithPayload: (sm) => events.handlePcEventReportWithPayload(sm),
        trainControlCommand: (sm) => train.handleTrainCommand(sm),
        trainControlReply: (sm) => train.handleTrainReply(sm),
        simpleTrainNodeIdentInfoRequest: (sm) => snip.handleSimpleNodeInfoRequest(sm),
        // Train uses SNIP format
        simpleTrainNodeIdentInfoReply: (sm) => snip.handleSimpleNodeInfoReply(sm),
        datagram: (sm) => datagram.handleDatagram(sm),
        datagramOkReply: (sm) => datagram.handleDatagramReceivedOk(sm),
        datagramRejectedReply: (sm) => datagram.handleDatagramRejected(sm),
        streamInitiateRequest: (sm) => stream.initiateRequest(sm),
        streamInitiateReply: (sm) => stream.initiateReply(sm),
        streamSendData: (sm) => stream.dataSend(sm),
        streamDataProceed: (sm) => stream.dataProceed(sm),
        streamDataComplete: (sm) => stream.dataComplete(sm)
      };
    }
    _extractEventIdFromIncoming(sm) {
      const p = sm.incoming.msg.payload;
      return BigInt(p[0]) << 56n | BigInt(p[1]) << 48n | BigInt(p[2]) << 40n | BigInt(p[3]) << 32n | BigInt(p[4]) << 24n | BigInt(p[5]) << 16n | BigInt(p[6]) << 8n | BigInt(p[7]);
    }
    _defaultSnipLoaders() {
      const snip = this.snipHandler;
      return {
        loadManufacturerVersionId: (node, msg, off, n) => snip.loadManufacturerVersionId(node, msg, off, n),
        loadName: (node, msg, off, n) => snip.loadName(node, msg, off, n),
        loadModel: (node, msg, off, n) => snip.loadModel(node, msg, off, n),
        loadHardwareVersion: (node, msg, off, n) => snip.loadHardwareVersion(node, msg, off, n),
        loadSoftwareVersion: (node, msg, off, n) => snip.loadSoftwareVersion(node, msg, off, n),
        loadUserVersionId: (node, msg, off, n) => snip.loadUserVersionId(node, msg, off, n),
        loadUserName: (node, msg, off, n) => snip.loadUserName(node, msg, off, n),
        loadUserDescription: (node, msg, off, n) => snip.loadUserDescription(node, msg, off, n)
      };
    }
    /** Route per-address-space datagram sub-commands into config-mem-{read,write,operations}. */
    _buildDatagramCallbackTable() {
      const r = this.configMemRead;
      const w = this.configMemWrite;
      const op = this.configMemOperations;
      const s = this.configMemStream;
      return {
        // Read
        memoryReadSpaceConfigDescriptionInfo: (sm) => r.readSpaceConfigDescriptionInfo(sm),
        memoryReadSpaceAll: (sm) => r.readSpaceAll(sm),
        memoryReadSpaceConfigurationMemory: (sm) => r.readSpaceConfigMemory(sm),
        memoryReadSpaceAcdiManufacturer: (sm) => r.readSpaceAcdiManufacturer(sm),
        memoryReadSpaceAcdiUser: (sm) => r.readSpaceAcdiUser(sm),
        memoryReadSpaceTrainFunctionDefinitionInfo: (sm) => r.readSpaceTrainFunctionDefinitionInfo(sm),
        memoryReadSpaceTrainFunctionConfigMemory: (sm) => r.readSpaceTrainFunctionConfigMemory(sm),
        // Read stream
        memoryReadStreamSpaceConfigDescriptionInfo: (sm) => s.readStreamSpaceConfigDescriptionInfo(sm),
        memoryReadStreamSpaceAll: (sm) => s.readStreamSpaceAll(sm),
        memoryReadStreamSpaceConfigurationMemory: (sm) => s.readStreamSpaceConfigMemory(sm),
        memoryReadStreamSpaceAcdiManufacturer: (sm) => s.readStreamSpaceAcdiManufacturer(sm),
        memoryReadStreamSpaceAcdiUser: (sm) => s.readStreamSpaceAcdiUser(sm),
        memoryReadStreamSpaceTrainFunctionDefinitionInfo: (sm) => s.readStreamSpaceTrainFunctionDefinitionInfo(sm),
        memoryReadStreamSpaceTrainFunctionConfigMemory: (sm) => s.readStreamSpaceTrainFunctionConfigMemory(sm),
        // Write
        memoryWriteSpaceConfigDescriptionInfo: (sm) => w.writeSpaceConfigDescriptionInfo(sm),
        memoryWriteSpaceAll: (sm) => w.writeSpaceAll(sm),
        memoryWriteSpaceConfigurationMemory: (sm) => w.writeSpaceConfigMemory(sm),
        memoryWriteSpaceAcdiManufacturer: (sm) => w.writeSpaceAcdiManufacturer(sm),
        memoryWriteSpaceAcdiUser: (sm) => w.writeSpaceAcdiUser(sm),
        memoryWriteSpaceTrainFunctionDefinitionInfo: (sm) => w.writeSpaceTrainFunctionDefinitionInfo(sm),
        memoryWriteSpaceTrainFunctionConfigMemory: (sm) => w.writeSpaceTrainFunctionConfigMemory(sm),
        memoryWriteSpaceFirmwareUpgrade: (sm) => w.writeSpaceFirmware(sm),
        // Write-under-mask
        memoryWriteUnderMaskSpaceConfigDescriptionInfo: (sm) => w.writeUnderMaskSpaceConfigDescriptionInfo(sm),
        memoryWriteUnderMaskSpaceAll: (sm) => w.writeUnderMaskSpaceAll(sm),
        memoryWriteUnderMaskSpaceConfigurationMemory: (sm) => w.writeUnderMaskSpaceConfigMemory(sm),
        memoryWriteUnderMaskSpaceAcdiManufacturer: (sm) => w.writeUnderMaskSpaceAcdiManufacturer(sm),
        memoryWriteUnderMaskSpaceAcdiUser: (sm) => w.writeUnderMaskSpaceAcdiUser(sm),
        memoryWriteUnderMaskSpaceTrainFunctionDefinitionInfo: (sm) => w.writeUnderMaskSpaceTrainFunctionDefinitionInfo(sm),
        memoryWriteUnderMaskSpaceTrainFunctionConfigMemory: (sm) => w.writeUnderMaskSpaceTrainFunctionConfigMemory(sm),
        memoryWriteUnderMaskSpaceFirmwareUpgrade: (sm) => w.writeUnderMaskSpaceFirmware(sm),
        // Write stream
        memoryWriteStreamSpaceConfigDescriptionInfo: (sm) => s.writeStreamSpaceConfigDescriptionInfo(sm),
        memoryWriteStreamSpaceAll: (sm) => s.writeStreamSpaceAll(sm),
        memoryWriteStreamSpaceConfigurationMemory: (sm) => s.writeStreamSpaceConfigMemory(sm),
        memoryWriteStreamSpaceAcdiManufacturer: (sm) => s.writeStreamSpaceAcdiManufacturer(sm),
        memoryWriteStreamSpaceAcdiUser: (sm) => s.writeStreamSpaceAcdiUser(sm),
        memoryWriteStreamSpaceTrainFunctionDefinitionInfo: (sm) => s.writeStreamSpaceTrainFunctionDefinitionInfo(sm),
        memoryWriteStreamSpaceTrainFunctionConfigMemory: (sm) => s.writeStreamSpaceTrainFunctionConfigMemory(sm),
        memoryWriteStreamSpaceFirmwareUpgrade: (sm) => s.writeStreamSpaceFirmware(sm),
        // Operations
        memoryOptions: (sm) => op.optionsCmd(sm),
        memoryOptionsReply: (sm) => op.optionsReply(sm),
        memoryGetAddressSpaceInfo: (sm) => op.getAddressSpaceInfo(sm),
        memoryGetAddressSpaceInfoReplyNotPresent: (sm) => op.getAddressSpaceInfoReplyNotPresent(sm),
        memoryGetAddressSpaceInfoReplyPresent: (sm) => op.getAddressSpaceInfoReplyPresent(sm),
        memoryReserveLock: (sm) => op.reserveLock(sm),
        memoryReserveLockReply: (sm) => op.reserveLockReply(sm),
        memoryGetUniqueId: (sm) => op.getUniqueId(sm),
        memoryGetUniqueIdReply: (sm) => op.getUniqueIdReply(sm),
        memoryUnfreeze: (sm) => op.unfreeze(sm),
        memoryFreeze: (sm) => op.freeze(sm),
        memoryUpdateComplete: (sm) => op.updateComplete(sm),
        memoryResetReboot: (sm) => op.resetReboot(sm),
        memoryFactoryReset: (sm) => op.factoryReset(sm)
      };
    }
    // Stream-handler fan-outs: route to both the config-mem-stream layer
    // (if it owns the stream) and the user callback.
    _streamInitiateReplyFanout(sm, stream) {
      this.configMemStream.onInitiateReply(sm, stream);
      this._opts.callbacks?.onStreamInitiateReply?.(sm, stream);
    }
    _streamDataReceivedFanout(sm, stream) {
      this.configMemStream.onDataReceived(sm, stream);
      this._opts.callbacks?.onStreamDataReceived?.(sm, stream);
    }
    _streamCompleteFanout(sm, stream) {
      this.configMemStream.onComplete(sm, stream);
      this._opts.callbacks?.onStreamComplete?.(sm, stream);
    }
  };

  // src/storage/localstorage-config-memory.js
  var LocalStorageConfigMemory = class {
    /**
     * @param {Object} [opts]
     * @param {string} [opts.keyPrefix='openlcb-cfg:']
     * @param {number} [opts.size=1024]  capacity (bytes) allocated on first write
     * @param {Storage} [opts.storage]  override for testing (default: window.localStorage)
     */
    constructor(opts = {}) {
      this._prefix = opts.keyPrefix ?? "openlcb-cfg:";
      this._size = opts.size ?? 1024;
      this._storage = opts.storage ?? (typeof localStorage !== "undefined" ? localStorage : null);
      if (!this._storage) {
        throw new Error("LocalStorageConfigMemory: no localStorage available (pass opts.storage)");
      }
    }
    /** Matches OpenLcbConfig's `configMemoryRead` signature. */
    read(node, address, count, buffer) {
      const bytes = this._load(node.id);
      for (let i = 0; i < count; i++) {
        buffer[i] = bytes[address + i] ?? 0;
      }
      return count;
    }
    /** Matches OpenLcbConfig's `configMemoryWrite` signature. */
    write(node, address, count, buffer) {
      const bytes = this._load(node.id);
      if (address + count > bytes.length) {
        const grown = new Uint8Array(address + count);
        grown.set(bytes);
        bytes.set(bytes.subarray(0, bytes.length), 0);
        this._save(node.id, grown);
        for (let i = 0; i < count; i++) grown[address + i] = buffer[i];
        this._save(node.id, grown);
        return count;
      }
      for (let i = 0; i < count; i++) {
        bytes[address + i] = buffer[i];
      }
      this._save(node.id, bytes);
      return count;
    }
    /** Clear all stored bytes for `nodeId`. */
    clear(nodeId) {
      this._storage.removeItem(this._key(nodeId));
    }
    _key(nodeId) {
      return this._prefix + nodeId.toString(16).padStart(12, "0");
    }
    _load(nodeId) {
      const encoded = this._storage.getItem(this._key(nodeId));
      if (!encoded) return new Uint8Array(this._size);
      const raw = atob(encoded);
      const out = new Uint8Array(Math.max(this._size, raw.length));
      for (let i = 0; i < raw.length; i++) out[i] = raw.charCodeAt(i);
      return out;
    }
    _save(nodeId, bytes) {
      let s = "";
      for (const b of bytes) s += String.fromCharCode(b);
      this._storage.setItem(this._key(nodeId), btoa(s));
    }
  };
  return __toCommonJS(index_exports);
})();
